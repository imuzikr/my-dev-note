---
date: 2026-04-02
tags:
  - 비대칭암호화
  - 공개키
  - 개인키
  - RSA
  - ECC
  - 전자서명
  - TLS
  - 핸드셰이크
  - 세션키
  - 인증서
  - 디피헬만
  - 순방향비밀성
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---

## 비대칭 암호화의 원리와 실제 작동 방식

> 공개키 · 개인키 · 전자서명 · TLS 핸드셰이크 · 인증서를 하나의 흐름으로 이해하기 대상: 비전공자 · 초보자

---

## 1. 비대칭 암호화란 무엇인가

암호화에는 크게 두 가지 방식이 있습니다. **대칭 암호화**는 암호화와 복호화에 같은 키 하나를 사용하는 방식이고, **비대칭 암호화**는 서로 다른 두 개의 키를 사용하는 방식입니다. 이 두 키를 **공개키(Public Key)**와 **개인키(Private Key)**라고 부르며, 수학적으로 연결되어 있지만 한쪽에서 다른 쪽을 역산하는 것은 현실적으로 불가능합니다.

비유하자면 우체통과 같습니다. 누구나 우체통 투입구(공개키)로 편지를 넣을 수 있지만, 우체통을 열어 편지를 꺼낼 수 있는 사람은 열쇠(개인키)를 가진 소유자뿐입니다. 공개키는 말 그대로 누구에게나 공개해도 안전하고, 개인키는 오직 소유자만 비밀로 보관합니다.

> [!tip] 핵심 정리 비대칭 암호화는 공개키로 잠그고 개인키로 여는 구조입니다. 공개키를 아무리 많은 사람에게 알려줘도, 개인키 없이는 내용을 복호화할 수 없으므로 안전합니다.

---

## 2. 두 방향의 연산: 암호화와 서명

비대칭 암호화의 두 키는 **양방향으로** 사용할 수 있는데, 방향에 따라 목적이 완전히 달라집니다.

**첫 번째 방향은 기밀성 확보입니다.** A가 B에게 비밀 메시지를 보내고 싶다면, B의 공개키로 메시지를 암호화합니다. 이렇게 암호화된 메시지는 오직 B의 개인키로만 복호화할 수 있으므로, 전송 중간에 누군가 가로채더라도 내용을 읽을 수 없습니다.

**두 번째 방향은 인증과 무결성 확보입니다.** A가 자신의 개인키로 메시지에 서명하면, 누구나 A의 공개키로 그 서명을 검증할 수 있습니다. 그런데 A의 공개키는 누구나 갖고 있으므로 이 메시지는 "비밀"이 아닙니다. 대신 "이 메시지는 확실히 A가 보냈고, 중간에 변조되지 않았다"는 사실을 증명합니다. 이것이 **전자서명(Digital Signature)**입니다.

|방향|사용하는 키|보장하는 것|
|---|---|---|
|B의 공개키로 암호화 → B의 개인키로 복호화|수신자의 키 쌍|**기밀성** (B만 읽을 수 있음)|
|A의 개인키로 서명 → A의 공개키로 검증|발신자의 키 쌍|**인증 + 무결성** (A가 보냈고 변조 없음)|

실무에서 둘 다 필요하면 두 연산을 결합합니다. A가 먼저 자신의 개인키로 서명한 뒤, B의 공개키로 그 서명된 메시지를 암호화하면, B만 읽을 수 있으면서 동시에 A가 보낸 것임을 확인할 수 있습니다.

---

## 3. 전자서명은 구체적으로 어떻게 만들어지는가

서명과 암호화는 둘 다 키를 사용하지만 목적이 다릅니다. 암호화는 메시지 자체를 변환하여 내용을 숨기는 것이고, 서명은 메시지는 그대로 평문으로 두되 **별도의 검증용 데이터(서명값)**를 덧붙이는 것입니다. 계약서에 서명하는 것과 같은 원리입니다. 계약서 내용은 공개되어 있고, 서명은 "이 내용에 본인이 동의했다"는 증거입니다.

#### 서명값이 만들어지는 과정

**1단계**로 원본 메시지를 **해시 함수**(예: SHA-256)에 통과시켜 고정된 길이의 요약값(해시)을 만듭니다. 어떤 길이의 메시지든 SHA-256을 거치면 항상 256비트의 짧은 값이 됩니다. 메시지가 단 한 글자만 바뀌어도 해시값이 완전히 달라지는 성질(쇄도 효과)이 있어서 변조 탐지에 유리합니다.

**2단계**로 이 해시값에 서명자의 개인키를 적용하여 수학적 연산을 수행합니다. RSA의 경우 `서명값 = 해시값^d mod n`이라는 수식으로 표현되며, 여기서 `d`는 개인키, `n`은 공개된 큰 수입니다. 이 연산의 결과가 **서명값**입니다.

#### 검증 과정

받는 쪽에서는 두 가지를 독립적으로 수행합니다. 서명값에 A의 공개키를 적용하여 원래의 해시값을 복원하고, 동시에 받은 메시지를 직접 해시 함수에 넣어 해시값을 계산합니다. 이 두 값이 일치하면 서명이 유효한 것이고, 하나라도 다르면 메시지가 변조되었거나 서명자가 다른 사람이라는 뜻입니다.

> [!example] 간단한 숫자 예시 개인키 `d=7`, 공개키 `e=3`, 공유된 수 `n=33`이라고 단순화해서 가정합니다. 해시값이 `4`라면, 서명값은 `4^7 mod 33 = 16`입니다. 검증할 때는 `16^3 mod 33 = 4`를 계산하여 원래 해시값 `4`가 복원되므로 서명이 유효합니다. 메시지가 변조되어 해시값이 `5`로 달라졌다면, 서명에서 복원된 `4`와 일치하지 않으므로 즉시 변조가 감지됩니다.

---

## 4. 공개키와 개인키는 어떤 형태인가

키는 단순한 숫자 하나가 아니라 **여러 수학적 값이 묶인 구조체**입니다. 형태는 알고리즘에 따라 다릅니다.

**RSA**에서는 먼저 매우 큰 소수 두 개(p, q)를 곱하여 `n`을 만들고, 특정 조건을 만족하는 두 지수 `e`와 `d`를 계산합니다. 공개키는 `(n, e)` 쌍이고, 개인키는 `(n, d)` 쌍입니다. 실제 2048비트 RSA의 `n` 값은 10진수로 약 617자리에 달하는 거대한 숫자입니다.

**ECC(타원곡선 암호)**에서는 개인키가 하나의 큰 정수이고, 공개키는 타원곡선 위의 좌표점 `(x, y)`입니다. ECC 256비트 키가 RSA 3072비트 키와 비슷한 보안 강도를 제공하면서 크기가 훨씬 작아서, 자원이 제한된 환경에서 특히 유리합니다.

이런 큰 숫자들을 저장하고 전송할 때는 **ASN.1**이라는 표준 구조로 인코딩한 뒤 **Base64**로 변환합니다. 그래서 실제 키 파일을 열어보면 `-----BEGIN PUBLIC KEY-----`로 시작하는 텍스트 블록이 보이는데, 이것은 해시값이 아니라 큰 정수들을 담고 있는 **포장 형식**일 뿐입니다. 디코딩하면 원래의 수학적 값이 그대로 복원됩니다.

> [!info] RSA가 안전한 이유 큰 소수 두 개를 곱하는 것은 쉽지만, 그 결과를 다시 소인수분해하는 것은 극히 어렵다는 수학적 성질에 기반합니다. 실제 RSA에서 사용하는 수의 크기에서는 현재의 컴퓨터로 수십억 년이 걸려도 소인수분해를 완료할 수 없습니다.

---

## 5. 실제 HTTPS 통신에서 세션키가 만들어지는 과정

비대칭 암호화는 대칭 암호화보다 수백~수천 배 느리기 때문에, 실제 데이터 전송에 직접 사용하기에는 비효율적입니다. 그래서 실무에서는 **하이브리드 방식**을 사용합니다. 비대칭 암호화로 안전하게 **세션키(대칭키)**를 합의한 뒤, 실제 데이터는 빠른 대칭 암호화로 주고받는 것입니다. 이 합의 과정을 **TLS 핸드셰이크**라고 부릅니다.

#### 핵심 아이디어

세션키는 네트워크를 통해 직접 전송되지 않습니다. 양쪽이 각자 **공개적으로 안전한 재료를 교환**하고, 그 재료로부터 동일한 세션키를 **독립적으로 계산**합니다. 세션키 자체가 네트워크를 한 번도 타지 않으므로, 도청하더라도 세션키를 알아낼 수 없습니다.

#### TLS 1.3 핸드셰이크의 단계별 흐름

**1단계 — Client Hello:** 브라우저가 서버에 첫 인사를 보냅니다. 이때 임시 개인키 `a`를 무작위로 생성하고, 타원곡선의 기준점 `G`와 곱하여 **임시 공개값** `A = a × G`를 계산한 뒤 함께 보냅니다.

**2단계 — Server Hello:** 서버도 임시 개인키 `b`를 생성하고 **임시 공개값** `B = b × G`를 계산하여 보냅니다. 동시에 서버의 **인증서**도 함께 전송합니다.

**3단계 — 인증서 검증:** 브라우저가 서버의 인증서를 검증하여 이 서버가 정말로 해당 도메인의 소유자인지 확인합니다. (이 과정은 아래 6번 섹션에서 자세히 다룹니다.)

**4단계 — 공유 비밀 계산:** 여기가 핵심입니다. 클라이언트는 `a × B = a × b × G`를 계산하고, 서버는 `b × A = b × a × G`를 계산합니다. 수학적으로 이 두 값은 동일합니다. 네트워크를 오간 것은 `A`와 `B`뿐인데, 이 값들만으로는 `a`나 `b`를 역산할 수 없으므로 도청자가 공유 비밀을 알아내는 것은 불가능합니다.

**5단계 — 세션키 도출:** 공유 비밀에 양쪽의 난수 등을 조합하여 **키 도출 함수(HKDF)**에 넣으면 실제 통신용 세션키가 만들어집니다.

**6단계 — 암호화 통신 시작:** 이제 양쪽이 동일한 세션키를 보유하고 있으므로, AES 같은 빠른 대칭 암호화 알고리즘으로 데이터를 주고받습니다.

> [!tip] "교환"보다 "합의"가 정확한 표현 세션키 자체는 어느 쪽에서 다른 쪽으로 보내는 것이 아니라, 양쪽이 재료를 공유한 뒤 동일한 결과를 독립적으로 계산해내는 것입니다. 그래서 영어로도 "Key Exchange"보다 **"Key Agreement"**라는 표현이 더 정확합니다.

#### 임시 공개값과 서버 공개키는 다른 것

핸드셰이크에서 등장하는 `A`, `B`는 해당 연결을 위해 **그 순간에만 생성되는 임시 값**이고, 서버 인증서에 들어 있는 공개키는 **오래 유지되는 고정된 키**입니다. 임시 키는 세션키를 만드는 데 사용되고, 서버 공개키는 서버의 신원을 증명하는 데 사용됩니다. 연결이 끝나면 임시 키는 폐기됩니다.

이렇게 매번 임시 키를 새로 만드는 이유는 **순방향 비밀성(Forward Secrecy, 과거 통신도 안전하게 지키는 성질)** 때문입니다. 나중에 서버의 장기 개인키가 유출되더라도, 이미 끝난 과거 세션의 임시 개인키는 존재하지 않으므로 과거 통신 내용을 복호화할 수 없습니다.

---

## 6. 인증서는 어떻게 검증되는가

서버가 보낸 인증서를 브라우저가 검증하는 과정에서, 브라우저는 **매번 인증기관에 공개키를 요청하지 않습니다.** 인증기관(CA)의 공개키는 운영체제와 브라우저에 **이미 내장**되어 있습니다.

#### 루트 인증서 저장소

Windows, macOS, Android, iOS 등 운영체제에는 **신뢰할 수 있는 루트 인증기관의 인증서 목록**이 사전에 설치되어 있으며, 각 인증서 안에 해당 기관의 공개키가 포함되어 있습니다. OS나 브라우저 업데이트를 통해 주기적으로 갱신되므로, 인증서 검증 시 외부에 별도로 요청할 필요가 없습니다.

#### 인증서 체인 구조

실제로는 루트 CA가 서버 인증서를 직접 서명하는 경우가 드뭅니다. 보통 루트 CA가 **중간 CA(Intermediate CA)**의 인증서에 서명하고, 중간 CA가 서버의 인증서에 서명하는 **체인 구조**입니다. 서버는 핸드셰이크에서 자신의 인증서와 중간 CA 인증서를 함께 보내고, 브라우저는 아래에서 위로 올라가며 체인이 끊기지 않고 루트까지 연결되는지 검증합니다.

```
루트 CA (기기에 내장)  ←  가장 위, 자기 자신이 서명
   ↓ 서명
중간 CA               ←  서버와 함께 전송됨
   ↓ 서명
서버 인증서            ←  서버가 전송
```

중간 단계를 두는 이유는 루트 CA의 개인키를 오프라인 금고에 보관하고 일상적인 서명은 중간 CA에 위임하기 위해서입니다. 중간 CA의 키가 유출되면 해당 중간 CA만 폐기하면 되고, 루트 CA는 안전하게 유지됩니다.

> [!question] 루트 인증서 자체는 누가 보증하는가? 
> 루트 인증서는 **자기 자신이 서명한 것(Self-signed)**입니다. 따라서 루트 CA의 신뢰는 암호학적 검증이 아니라, OS 제조사나 브라우저 개발사가 엄격한 심사를 거쳐 "이 기관은 신뢰할 수 있다"고 판단하여 목록에 포함시키는 **제도적 신뢰**에 기반합니다.

---

## 7. 세션키는 통신 중에 계속 같은 것을 쓰는가

하나의 연결이 유지되는 동안 세션키는 기본적으로 유지되지만, TLS 1.3에서는 **키 업데이트(Key Update)** 메커니즘을 통해 연결 도중에도 키를 갱신할 수 있습니다. 하나의 키로 너무 많은 데이터를 암호화하면 통계적 공격 가능성이 높아지기 때문입니다.

연결이 끊겼다가 다시 접속할 때는 **세션 재개(Session Resumption)** 방식을 사용합니다. 첫 번째 연결 시 서버가 **세션 티켓**을 발급하고, 재접속 시 이 티켓을 제시하면 전체 핸드셰이크를 생략하고 빠르게 새로운 세션키를 도출할 수 있습니다. 이때에도 이전과 동일한 키를 재사용하는 것이 아니라 반드시 **새로운 키를 파생**시킵니다.

어떤 경우든 동일한 키가 지나치게 오래 사용되지 않도록 설계되어 있다는 것이 핵심입니다.

---

## 8. 클라이언트는 자신을 어떻게 증명하는가

일반적인 HTTPS 통신에서 **클라이언트는 인증서로 자신을 증명하지 않습니다.** TLS 핸드셰이크에서 인증서 기반 인증은 서버 측에서만 이루어지며, 클라이언트의 신원 확인은 TLS 위의 **애플리케이션 계층**에서 처리합니다. 아이디/비밀번호 로그인, OAuth 소셜 로그인, SMS 인증 등이 모두 이에 해당합니다.

다만 보안 요구가 매우 높은 환경에서는 **상호 TLS 인증(mTLS)**을 사용하여 클라이언트도 인증서로 신원을 증명합니다. 기업 내부 마이크로서비스 간 통신, 금융기관 API 연동, 정부 시스템 접근 등 사용자 범위가 한정되고 보안이 극히 중요한 환경에서 활용됩니다.

---

## 핵심 개념 정리

|개념|설명|비유|
|---|---|---|
|**공개키**|누구에게나 공개하는 키, 암호화 또는 서명 검증에 사용|우체통 투입구|
|**개인키**|소유자만 비밀로 보관하는 키, 복호화 또는 서명 생성에 사용|우체통 열쇠|
|**전자서명**|개인키로 만든 검증용 데이터, 발신자 인증과 변조 탐지|계약서의 서명|
|**서명값**|메시지 해시에 개인키를 적용하여 생성한 고유한 데이터 조각|서명의 실체|
|**TLS 핸드셰이크**|세션키를 합의하고 서버를 인증하는 초기 준비 과정|통화 전 신원 확인과 암호 약속|
|**임시 공개값**|한 번의 연결에서만 쓰이고 폐기되는 일회용 키 교환 재료|일회용 재료|
|**순방향 비밀성**|장기 키가 유출되어도 과거 통신이 안전한 성질|매번 새 자물쇠를 쓰는 것|
|**인증서 체인**|루트 CA → 중간 CA → 서버 인증서로 이어지는 신뢰 사슬|보증인의 보증인|
|**루트 인증서 저장소**|OS/브라우저에 내장된 신뢰할 수 있는 CA 목록|사전에 등록된 신뢰 목록|

---

> [!abstract] 전체 흐름 요약 
> 비대칭 암호화는 공개키와 개인키라는 두 개의 키를 사용하여 기밀성(공개키로 암호화)과 인증(개인키로 서명)을 동시에 달성합니다. 실제 HTTPS 통신에서는 TLS 핸드셰이크를 통해 양쪽이 임시 키 재료를 교환하고 동일한 세션키를 독립적으로 계산한 뒤, 이 세션키로 빠른 대칭 암호화 통신을 수행합니다. 서버의 신원은 인증기관이 서명한 인증서로 증명되며, 이 인증서의 검증에 필요한 루트 CA의 공개키는 운영체제에 이미 내장되어 있어 별도의 외부 요청 없이 즉시 검증이 가능합니다.