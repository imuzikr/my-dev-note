---
date: 2026-01-29
tags:
  - vibe_coding
  - API서버
  - CORS
  - 요청
  - 라우터
  - Vercel
  - Serverless
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---

# 개발자가 작성한 "텍스트(코드)"가 어떻게 "실행 가능한 단위"로 변하는가?

---

### 서버리스 변환의 3단계 디테일

#### 1. 번들링 (The Packaging)

개발자가 작성한 `route.ts` 파일만으로는 서버에서 실행될 수 없습니다. `db` 객체가 정의된 파일, 설치한 외부 라이브러리(`mongodb`, `firebase-admin` 등)가 모두 필요하죠.

- **과정:** 빌드 도구(Next.js의 경우 Webpack이나 Turbopack)가 이 함수를 실행하는 데 필요한 **모든 의존성을 하나로 뭉쳐서** 약 수백 KB ~ 수 MB의 작은 패키지로 만듭니다.
- **결과:** "이 파일 하나만 있으면 어디서든 실행 가능해!" 상태가 됩니다.
    

#### 2. 엔드포인트 분리 (Isolation)

전통적인 서버는 하나의 큰 덩어리(Monolith)가 모든 요청을 처리하지만, 서버리스는 다릅니다.

- **과정:** `/api/books`와 `/api/users`가 있다면, 이 둘을 완전히 별개의 **독립된 개체**로 쪼갭니다.
- **장점:** 만약 `books` API에 요청이 폭주해도 `users` API는 아무런 영향을 받지 않고 평온하게 작동합니다.
    

#### 3. 콜드 스타트 준비 (Dormancy & Activation)

이 부분이 서버리스의 가장 큰 특징입니다.

- **과정:** 클라우드 서비스 제공자(Vercel, AWS 등)는 이 번들링된 파일을 서버 메모리에 늘 올려두지 않고, 저렴한 저장소에 보관만 합니다.
- **실행:** 누군가 접속하면 그제야 파일을 메모리로 로드하고 함수를 실행합니다. 이것을 **"깨어난다"** 고 표현하며, 처음 깨어날 때 발생하는 약간의 지연 시간을 **콜드 스타트(Cold Start)** 라고 부릅니다.
    
