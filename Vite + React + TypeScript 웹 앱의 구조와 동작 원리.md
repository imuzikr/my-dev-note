---
date: 2026-04-02
tags:
  - 웹개발
  - CSR
  - 리액트
  - React
  - TypeScript 
  - vite
  - 프론트엔드
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---
## Vite + React + TypeScript 웹 앱의 구조와 동작 원리

> 파일 구조 · 실행 흐름 · 컴포넌트 · CSR 방식을 하나의 흐름으로 이해하기 대상: 비개발자 · 초보자

---

## 1. 이 프로젝트는 무엇으로 만들어졌는가

이 프로젝트는 세 가지 기술이 조합된 웹 애플리케이션입니다. **React**는 화면을 구성하는 핵심 라이브러리이고, **TypeScript**는 JavaScript에 타입 시스템을 추가하여 코드의 안정성을 높여주는 언어이며, **Vite**는 이 코드들을 브라우저가 이해할 수 있도록 변환하고 개발 환경을 제공하는 빌드 도구입니다. 프로젝트의 본질적인 기술은 React + TypeScript이고, Vite는 이를 효율적으로 개발하기 위한 도구라고 이해하면 됩니다.

#### 파일 확장자와 설정 파일로 기술 스택 판별하기

프로젝트 폴더를 열었을 때, 어떤 기술이 사용되었는지는 파일 이름만으로도 판단할 수 있습니다.

|파일 / 확장자|의미|
|---|---|
|`vite.config.ts`|Vite를 빌드 도구로 사용|
|`tsconfig.json`, `.ts` 파일들|TypeScript 사용|
|`.tsx` 파일들 (`App.tsx`, `main.tsx`)|TypeScript + React(JSX 문법) 사용|
|`package.json`|npm 기반 의존성 관리|
|`firebase.ts`|Firebase 서비스 연동|

특히 `.tsx`는 TypeScript 안에서 **JSX**(JavaScript XML, JavaScript 안에 HTML 유사 마크업을 작성하는 React 고유의 문법)를 사용한다는 의미이므로, 이 확장자만 보아도 React 프로젝트임을 판단할 수 있습니다. 다만 엄밀히 말하면 Preact나 SolidJS 같은 라이브러리도 `.tsx`를 쓸 수 있으므로, 100% 확정하려면 `package.json`의 의존성 목록에서 `react`와 `react-dom` 패키지를 확인하는 것이 가장 확실합니다.

> [!tip] 핵심 정리 파일 확장자(`.tsx`, `.ts`)와 설정 파일(`vite.config.ts`, `tsconfig.json`)만 보아도 어떤 기술이 사용되었는지 판단할 수 있습니다.

---

## 2. 앱이 실행되는 순서

웹 앱이 동작하는 흐름은 하나의 연쇄적인 과정입니다.

```
index.html → main.tsx → App.tsx → 각 페이지 컴포넌트 (Dashboard, BoardView 등)
```

사용자가 웹사이트에 접속하면 브라우저는 가장 먼저 `index.html`을 받아옵니다. 이 파일 안에는 `<div id="root"></div>`라는 빈 껍데기와 `<script type="module" src="/src/main.tsx"></script>`라는 스크립트 태그만 존재합니다. 브라우저가 이 스크립트 태그를 만나면 `main.tsx` 파일을 불러와 실행하고, `main.tsx`는 `App.tsx` 컴포넌트를 그 빈 `div` 안에 **렌더링**(화면에 그려내는 것)합니다. 그리고 `App.tsx`는 URL 주소에 따라 적절한 페이지 컴포넌트를 선택하여 화면에 표시합니다.

여기서 `type="module"` 속성은 브라우저에게 해당 스크립트를 **ES 모듈**(최신 JavaScript의 모듈 시스템) 방식으로 처리하라고 알려주는 역할을 하며, Vite는 개발 중에 이 모듈 시스템을 활용하여 별도의 번들링 없이 빠른 개발 환경을 제공합니다.

> [!info] index.html이 프로젝트 루트에 있는 이유 Vite 프로젝트의 전형적인 구조에서 `index.html`은 프로젝트 최상위(루트)에 위치합니다. 이것이 Vite의 진입점 역할을 하며, 다른 빌드 도구와 구분되는 특징 중 하나입니다.

---

## 3. 각 파일의 역할

이 프로젝트의 파일들은 각각 고유한 역할을 가지고 있으며, 서로 연결되어 하나의 앱을 구성합니다.

#### index.html — 앱의 출발점

앱의 최초 진입점으로, 실질적인 내용은 거의 없고 JavaScript를 불러오는 출발점 역할만 합니다. `<div id="root"></div>`라는 빈 컨테이너와 `main.tsx`를 불러오는 스크립트 태그가 핵심입니다.

#### main.tsx — React 앱의 시작점

`App` 컴포넌트를 HTML의 `root` 요소에 연결하는 다리 역할을 합니다. React의 `createRoot` 같은 함수를 실행하여 빈 `div` 안에 앱을 심어주는 것입니다.

#### App.tsx — 뼈대이자 교통 정리

앱의 전체 뼈대이자 교통 정리 역할을 담당하며, 구체적으로 세 가지 일을 합니다.

```
① Firebase를 통해 로그인 상태 확인 (13~19번 줄)
    ↓
② 확인하는 동안 로딩 화면 표시 (21~27번 줄)
    ↓
③ URL 주소에 따라 적절한 페이지 연결 (29~37번 줄)
    ├── "/" (홈)         → Dashboard 화면
    ├── "/board/:boardId" → BoardView 화면
    └── 그 외 주소       → 자동으로 홈으로 되돌림
```

각 화면에 `user` 정보를 함께 전달하므로, Dashboard와 BoardView 모두 현재 로그인한 사용자가 누구인지 알 수 있습니다.

#### pages 폴더 — 실제 각 페이지의 화면

`pages` 폴더 안의 파일들(`Dashboard.tsx`, `BoardView.tsx`)은 사용자가 실제로 보게 되는 각 페이지의 화면과 기능을 담당합니다. `App.tsx`가 건물의 복도와 안내 표지판이라면, `pages` 폴더의 파일들은 실제 각 방의 내부 구성에 해당합니다.

`BoardView.tsx`를 예로 들면 이 파일은 게시판의 상세 화면을 구성하는데, **Board** 인터페이스에서 게시판 하나의 정보(제목, 설명, 익명 허용 여부 등)를 정의하고, **Post** 인터페이스에서 게시글 하나의 구조(작성자, 내용, 이미지 등)를 정의합니다. 그리고 URL에서 게시판 ID를 추출하여 해당 게시판의 데이터를 불러오고, 새 글 작성에 필요한 입력값들을 관리합니다.

#### 기타 파일들

`firebase.ts`는 Firebase 서비스와의 연결을 설정하는 파일이고, `index.css`는 앱의 시각적 스타일을 정의하며, `lib/utils.ts`는 여러 곳에서 공통으로 사용하는 유틸리티 함수들을 모아둔 파일입니다.

---

## 4. 컴포넌트 — React의 핵심 개념

React에서 가장 핵심적인 개념은 **컴포넌트(Component)**입니다. 컴포넌트란 화면을 구성하는 독립적인 조각으로, 각 `.tsx` 파일 자체가 하나의 컴포넌트이면서, 동시에 그 안에서 더 작은 컴포넌트들을 가져다 쓸 수 있습니다.

```
App.tsx (최상위 컴포넌트)
  ├── Dashboard.tsx (페이지 컴포넌트)
  └── BoardView.tsx (페이지 컴포넌트)
        ├── Plus (아이콘 컴포넌트)
        ├── ArrowLeft (아이콘 컴포넌트)
        ├── Trash2 (아이콘 컴포넌트)
        └── Loader2 (로딩 아이콘 컴포넌트)
```

`BoardView.tsx`는 그 자체로 하나의 컴포넌트이면서, 내부에서 `lucide-react`라는 아이콘 라이브러리의 컴포넌트들을 사용합니다. 이런 식으로 큰 상자 안에 중간 상자가 있고, 그 안에 작은 상자들이 들어가는 **계층 구조**로 화면이 구성됩니다. 이렇게 화면을 작은 단위로 쪼개서 조합하는 방식 덕분에 코드를 재사용하고 관리하기가 훨씬 수월해집니다.

> [!tip] 핵심 정리 React에서 컴포넌트는 화면의 독립적인 조각입니다. 큰 컴포넌트가 작은 컴포넌트를 포함하는 계층 구조로 전체 화면이 구성됩니다.

---

## 5. CSR 방식 — 브라우저가 화면을 그린다

이 프로젝트는 **CSR(Client-Side Rendering, 클라이언트 측 렌더링)** 방식으로 동작합니다. 서버가 완성된 HTML을 만들어 보내주는 것이 아니라, 서버는 빈 HTML 껍데기와 JavaScript 파일만 전달하고, 실제 화면 구성은 전부 브라우저에서 이루어집니다.

```
[서버가 보내는 것]
  빈 index.html + JavaScript 파일들

[브라우저에서 일어나는 일]
  JavaScript 실행 → 컴포넌트를 HTML 요소로 변환 → 빈 div 안에 채워 넣음
```

Vite + React 조합은 기본적으로 이 CSR 방식에 최적화되어 있으며, 별도의 설정 없이 프로젝트를 생성하면 자연스럽게 CSR로 동작합니다.

#### CSR과 SSR의 차이

반면 서버에서 컴포넌트를 미리 HTML로 변환해서 보내주는 방식을 **SSR(Server-Side Rendering)** 이라고 합니다. Vite에서 SSR을 구현하는 것이 기술적으로 불가능하지는 않지만, 라우팅 처리, 데이터 fetching, 서버와 클라이언트 간의 상태 동기화 등을 직접 구현해야 하므로 상당히 복잡해집니다.

|구분|CSR (이 프로젝트)|SSR|
|---|---|---|
|화면을 그리는 곳|브라우저(클라이언트)|서버|
|서버가 보내는 것|빈 HTML + JavaScript|완성된 HTML|
|대표적 도구|Vite + React|Next.js, Remix|
|첫 화면 로딩|상대적으로 느림 (JS 다운로드 후 렌더링)|빠름 (서버에서 완성된 HTML 전송)|
|이후 페이지 전환|빠름 (브라우저 내에서 처리)|서버 요청이 필요할 수 있음|

그래서 SSR이 필요한 경우에는 Vite + React를 직접 설정하기보다 처음부터 SSR을 위해 설계된 **Next.js**나 **Remix** 같은 프레임워크를 선택하는 것이 실무에서의 일반적인 접근입니다.

> [!info] 참고 Next.js는 SSR, SSG(Static Site Generation), CSR을 페이지 단위로 자유롭게 선택할 수 있어 가장 널리 쓰이는 React 기반 SSR 프레임워크입니다.

---

## 핵심 개념 정리

|개념|한마디 설명|비유|
|---|---|---|
|**Vite**|코드를 브라우저가 이해할 수 있도록 변환하는 빌드 도구|건물을 짓는 공구|
|**React**|화면을 컴포넌트 단위로 구성하는 라이브러리|건물의 설계 방식|
|**TypeScript**|JavaScript에 타입 시스템을 추가한 언어|설계도에 치수를 정확히 적는 것|
|**`.tsx` 확장자**|TypeScript + JSX(React 문법) 파일|React 컴포넌트임을 나타내는 표식|
|**컴포넌트**|화면을 구성하는 독립적인 재사용 가능 조각|레고 블록|
|**CSR**|브라우저에서 JavaScript로 화면을 그리는 방식|재료를 받아서 현장에서 조립|
|**SSR**|서버에서 완성된 HTML을 보내주는 방식|공장에서 완제품을 배송|
|**`index.html`**|앱의 최초 진입점 (빈 껍데기)|건물의 정문|
|**`main.tsx`**|React 앱의 시작점|정문에서 엘리베이터까지의 통로|
|**`App.tsx`**|URL에 따라 페이지를 연결하는 뼈대|건물 안내 데스크|
|**`pages` 폴더**|각 페이지의 실제 화면과 기능|건물 안의 각 방|

---

> [!abstract] 전체 흐름 요약 이 프로젝트는 Vite라는 빌드 도구 위에서 React + TypeScript로 개발된 CSR 방식의 웹 앱입니다. 빈 `index.html`에서 출발하여 `main.tsx` → `App.tsx` → 각 페이지 컴포넌트 순서로 실행되며, 모든 화면 구성은 서버가 아닌 브라우저에서 이루어집니다. React의 컴포넌트 기반 구조 덕분에 화면을 작은 조각으로 나누어 체계적으로 관리할 수 있으며, SSR이 필요한 경우에는 Next.js 같은 전용 프레임워크를 선택하는 것이 일반적입니다.