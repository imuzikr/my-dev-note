---
date: 2026-01-07
tags:
  - 깃
  - Git
  - GitHub
  - revert
  - commit
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---

## 1. revert가 하는 일

`git revert`는 과거 커밋을 삭제하는 것이 아니다. 해당 커밋이 만든 변경의 **반대 행동**을 새 커밋으로 기록하는 것이다.

예를 들어 commit 4가 이렇게 바꿨다고 하자.

```
원래: 사과
변경: 사과, 바나나
```

`git revert commit4`는 이것의 반대를 수행한다.

```
사과, 바나나  →  사과
```

추가한 것은 지우고, 지운 것은 다시 살리고, 수정한 것은 이전 상태로 되돌린다. 그리고 이 되돌림 자체가 하나의 새로운 커밋으로 기록된다. 기존 히스토리는 건드리지 않는다.

---

## 2. revert의 내부 동작: 3-way merge

단순히 "글자 몇 개를 반대로 바꾸면 되지 않느냐"고 생각할 수 있지만, Git은 그렇게 작동하지 않는다. revert는 내부적으로 **3-way merge**를 수행한다. 이것은 `git merge`가 브랜치를 합칠 때 사용하는 것과 본질적으로 동일한 메커니즘이다.

`git revert commit4`를 실행하면 Git은 세 가지 상태를 비교한다.

- **base**: commit 4 자체 (되돌리려는 커밋)
- **ours**: 현재 HEAD (지금 작업 중인 상태)
- **theirs**: commit 3 (commit 4의 부모, 즉 commit 4가 적용되기 전 상태)

Git은 "commit 4 → commit 3 방향의 변경"과 "commit 4 → 현재 HEAD 방향의 변경"을 동시에 합치려고 시도한다. 만약 현재 HEAD가 commit 4 그 자체라면 "commit 4 → HEAD" 방향에는 아무 변경이 없으므로, commit 3 방향의 변경만 깔끔하게 적용된다. 하지만 commit 4 이후에 다른 커밋들이 쌓여 있다면, 두 방향의 변경이 같은 영역에서 겹칠 수 있고, 이때 충돌이 발생한다.

이 구조를 이해하면 중요한 통찰을 하나 얻게 된다. merge conflict와 revert conflict는 별개의 현상이 아니라 동일한 3-way merge 메커니즘에서 비롯된다는 것이다.

---

## 3. 충돌이 없는 경우: 단순한 시나리오

공책에 문장이 하나 있다고 하자.

**commit 3** (초기 상태):

```
오늘은 날씨가 좋다.
```

**commit 4**에서 이렇게 수정했다:

```
오늘은 날씨가 매우 좋다.
```

현재 HEAD가 commit 4라면, 파일 내용은 그대로 `오늘은 날씨가 매우 좋다.`이다. 이 상태에서 `git revert commit4`를 실행하면 Git은 3-way merge를 수행한다.

- base(commit 4): `오늘은 날씨가 매우 좋다.`
- ours(HEAD = commit 4): `오늘은 날씨가 매우 좋다.`
- theirs(commit 3): `오늘은 날씨가 좋다.`

base와 ours가 동일하므로 theirs의 변경만 적용하면 된다. 결과는 `오늘은 날씨가 좋다.`가 되고, 충돌 없이 완료된다.

---

## 4. 충돌이 나는 경우: 후속 커밋이 같은 영역을 수정했을 때

이제 commit 4 이후에 commit 5가 추가된 상황을 보자.

### 사례 A: 같은 줄을 부분적으로 수정한 경우

먼저 상황을 정리하면 이렇다.

- **commit 3**: `오늘은 날씨가 좋다.`
- **commit 4**: `오늘은 날씨가 매우 좋다.` (commit 3에서 `매우`를 추가)
- **commit 5**: `오늘은 날씨가 엄청 매우 좋다.` (commit 4에서 `엄청`을 추가)

이 상태에서 `git revert commit4`를 실행하면, Git은 3-way merge의 세 축을 이렇게 설정한다.

- base(commit 4): `오늘은 날씨가 매우 좋다.`
- ours(HEAD = commit 5): `오늘은 날씨가 엄청 매우 좋다.`
- theirs(commit 3): `오늘은 날씨가 좋다.`

3-way merge의 원리는 "base를 기준으로 양쪽이 각각 무엇을 바꿨는지 확인하고, 한쪽만 바꿨으면 그 변경을 채택하고, 양쪽 다 바꿨으면 충돌로 처리한다"는 것이다. 여기서 중요한 점은 Git이 보는 단위가 **줄(line)**이라는 것이다.

base의 해당 줄은 `오늘은 날씨가 매우 좋다.`인데, ours에서는 이 줄이 `오늘은 날씨가 엄청 매우 좋다.`로 바뀌었고, theirs에서는 `오늘은 날씨가 좋다.`로 바뀌었다. Git은 줄 안에서 "어느 단어가 추가되고 어느 단어가 삭제되었는지"를 세밀하게 분석하지 않는다. 그저 **줄 전체가 base와 다른지 같은지**만 본다. 양쪽 모두 base와 다르므로, Git은 "같은 줄을 양쪽에서 서로 다르게 수정했다"고 판단하고 충돌을 선언한다.

사람의 눈에는 ours가 `엄청`을 추가한 것이고 theirs가 `매우`를 삭제한 것이니, 둘을 합치면 `오늘은 날씨가 엄청 좋다.`가 되는 것이 자연스러워 보인다. 하지만 Git은 줄 내부의 의미 단위를 파악하지 않기 때문에 이런 "합산"을 자동으로 수행할 수 없다. Git이 아는 것은 오직 "이 줄이 base와 같은가, 다른가"뿐이고, 양쪽 다 다르면 무조건 충돌이다.

실제로 충돌이 발생하면 파일에는 이런 형태의 충돌 마커가 남는다.

```
<<<<<<< HEAD
오늘은 날씨가 엄청 매우 좋다.
=======
오늘은 날씨가 좋다.
>>>>>>> revert of commit4
```

`<<<<<<< HEAD`와 `=======` 사이가 현재 HEAD(ours)의 내용이고, `=======`와 `>>>>>>>` 사이가 revert가 만들려는 결과(theirs)의 내용이다. 개발자는 이 두 버전을 보고 최종 결과를 직접 결정해야 한다. `오늘은 날씨가 엄청 좋다.`로 만들 수도 있고, `오늘은 날씨가 좋다.`로 만들 수도 있고, 아예 다른 문장으로 바꿀 수도 있다. 이 판단은 코드의 맥락과 의도를 아는 사람만 할 수 있기 때문에, Git은 섣불리 결정하지 않고 사람에게 넘기는 것이다.

### 사례 B: 같은 줄을 완전히 다른 내용으로 교체한 경우

**commit 5**에서 해당 줄을 아예 다른 문장으로 바꿨다고 하자:

```
내일은 비가 온다.
```

이 경우 3-way merge는 이렇게 된다:

- base(commit 4): `오늘은 날씨가 매우 좋다.`
- ours(HEAD = commit 5): `내일은 비가 온다.`
- theirs(commit 3): `오늘은 날씨가 좋다.`

base → ours도 변경되었고, base → theirs도 변경되었다. 같은 줄이 양쪽에서 완전히 다르게 바뀌었으므로 충돌이 난다. 이 경우는 revert가 되돌리려는 원래 내용 자체가 이미 존재하지 않으므로, 더 직관적으로 "당연히 자동 처리가 불가능하다"는 것을 이해할 수 있다.

---

## 5. Git은 "줄 단위 + 문맥"으로 변경 위치를 찾는다

Git이 diff를 생성할 때는 변경된 줄 자체만 보는 것이 아니라, 변경 줄 위아래의 **context lines**(기본 3줄)를 함께 기록한다. 이 문맥 줄들이 일종의 좌표 역할을 해서, Git이 "이 변경을 파일의 어느 위치에 적용해야 하는지"를 판단하는 근거가 된다.

따라서 직접 수정한 줄이 아니더라도, 그 **바로 근처의 줄**이 후속 커밋에서 바뀌었다면 문맥이 달라져서 Git이 변경을 적용할 정확한 위치를 특정하지 못하게 된다. "나는 그 줄을 안 건드렸는데 왜 충돌이 나지?"라는 의문이 드는 상황은 대부분 이 문맥 불일치 때문이다.

---

## 6. 비유로 정리: 원본 문제지와 낙서가 된 종이

전체 과정을 하나의 비유로 정리하면 이렇다.

commit 4를 되돌리는 것은 "문제지에 표시된 정답을 지워서 원본 상태로 복원하는 작업"이라고 할 수 있다.

- **commit 4(base)**: 정답이 표시된 문제지
- **commit 3(theirs)**: 정답 표시가 없는 원본 문제지
- **현재 HEAD(ours)**: 정답 표시 위에 다른 사람이 메모와 밑줄까지 추가해놓은 현재 종이

정답만 지우면 되는 상황이라면 간단하다. 하지만 정답이 적힌 바로 그 자리 위에 다른 메모가 덧씌워져 있다면, 정답만 정확히 지우면서 메모는 보존하는 작업을 자동으로 수행하기 어렵다. 이것이 Git이 충돌을 선언하고 사람의 판단을 요청하는 이유다.

---

## 7. 요약

`git revert`는 내부적으로 3-way merge를 수행한다. 되돌리려는 커밋을 base로 놓고, 현재 HEAD와 해당 커밋의 부모를 양쪽으로 비교한다. 현재 HEAD가 되돌리려는 커밋 바로 그 시점이라면 한쪽에 변경이 없으므로 충돌 없이 처리된다. 하지만 그 커밋 이후에 후속 커밋들이 같은 파일의 같은 영역(또는 그 근처의 문맥 범위)을 수정한 상태라면, Git은 두 방향의 변경을 자동으로 합칠 수 없어 충돌이 발생한다.

한 문장으로 줄이면 이렇다:

> **revert는 과거 변경의 반대를 현재에 적용하는 것인데, 그 사이에 같은 영역이 또 바뀌어 있으면 Git이 어느 쪽이 맞는지 혼자 결정하지 못해서 충돌이 난다.**