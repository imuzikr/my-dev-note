---
date: 2026-03-14
tags:
  - 네트워크
  - 게이트웨이
  - SSL
  - HTTPS
  - DNS
  - 로드밸런싱
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---
# 웹 서비스의 네트워크 구조 이해하기

> 게이트웨이 · SSL · 서버 분리를 하나의 흐름으로 이해하기
> 대상: 비개발자 · 초보자

---

## 1. 웹 서비스의 가장 단순한 모습

모든 웹 서비스는 결국 **"요청과 응답"** 이라는 단순한 구조에서 출발합니다. 브라우저에 주소를 입력하면 서버가 그에 맞는 결과를 돌려주는 것, 이것이 웹의 본질입니다.

```
사용자(브라우저)  ── 요청(Request) ──▶  서버
사용자(브라우저)  ◀── 응답(Response) ──  서버
```

이때 브라우저와 서버는 **HTTP(HyperText Transfer Protocol)** 라는 약속된 언어로 대화합니다. 브라우저가 "이 페이지를 보여줘(GET)"라고 요청하면 서버가 해당 페이지를 응답으로 보내주고, "이 데이터를 저장해줘(POST)"라고 요청하면 서버가 데이터를 저장한 뒤 결과를 알려주는 식입니다.

개인 블로그나 간단한 포트폴리오 사이트처럼 작은 프로젝트라면 서버 한 대로 충분합니다. 요청이 들어오면 서버가 처리하고 결과를 보내주면 끝입니다.

---

## 2. 서비스가 커지면 무엇이 달라지는가

서비스가 성장하면 하나의 서버가 모든 일을 처리하기 어려워집니다. 예를 들어 AI 기반 서비스를 운영한다고 가정해 봅시다. 이 서비스에는 채팅 기능, 파일 업로드 기능, AI 모델 연산 기능이 있습니다. 이 세 가지는 성격이 완전히 다릅니다. 채팅은 빠른 응답이 중요하고, 파일 업로드는 저장 공간이 핵심이며, AI 연산은 강력한 처리 능력이 필요합니다.

이 문제를 해결하는 방법은 두 가지입니다.

- **스케일 업(Scale Up):** 서버 한 대의 성능을 높이는 방법 (CPU, 메모리 추가). 한계가 명확합니다.
- **스케일 아웃(Scale Out):** 서버를 여러 대로 늘리는 방법. 실무에서 더 널리 사용됩니다.

그래서 실무에서는 기능별로 서버를 분리합니다. 이를 **마이크로서비스(Microservice)** 아키텍처라고 부릅니다.

```
채팅 서버      ← 실시간 메시지 처리  (예: WebSocket 기반)
파일 서버      ← 업로드·다운로드 담당 (예: S3 스토리지 연동)
모델 서버      ← AI 연산 담당       (예: GPU 서버)
```

이렇게 나누면 세 가지 이점이 생깁니다.

| 이점 | 설명 |
|---|---|
| **독립적 유지보수** | 각 기능을 별도의 팀이 독립적으로 개발·배포할 수 있습니다 |
| **장애 격리** | 모델 서버에 장애가 나도 채팅·파일 서버는 정상 작동합니다 |
| **효율적 확장** | AI 연산에만 부하가 몰리면 모델 서버만 추가로 늘리면 됩니다 |

---

## 3. 외부에서는 내부 구조를 알 수 없다

서버를 여러 대로 나누었다 해도 사용자 입장에서는 아무것도 달라지지 않습니다. 사용자가 아는 것은 오직 하나의 주소뿐입니다.

```
https://example.com
```

사용자는 내부에 채팅 서버가 몇 대인지, 모델 서버가 어디에 위치하는지, 각 서버의 포트 번호가 무엇인지 알 필요도 없고 알 수도 없습니다. 이것은 의도된 설계이며, 소프트웨어 공학에서는 이를 **추상화(Abstraction)** 라고 부릅니다. 복잡한 내부 구현을 숨기고 단순한 인터페이스만 제공하는 원칙입니다.

내부 구조를 외부에 그대로 노출하면 두 가지 문제가 생깁니다.
- **보안 위험**: 공격자가 내부 서버의 주소와 포트를 알면 직접 공격이 가능합니다
- **결합도 증가**: 내부 구조가 바뀔 때마다 사용자(클라이언트) 측에서도 코드를 수정해야 합니다

그렇다면 사용자의 요청을 받아서 적절한 내부 서버로 전달해 주는 **"정문"** 역할을 하는 무언가가 필요합니다.

---

## 4. 그 정문이 바로 게이트웨이(Gateway)

게이트웨이는 영어로 "출입구"라는 뜻입니다. 실제 건물에 비유하면 이해가 쉽습니다. 큰 건물에 들어갈 때 방문자는 정문 안내데스크에서 행선지를 말하고, 안내데스크가 적절한 층과 방을 안내해 주는 것과 같습니다.

```
브라우저
  ↓
게이트웨이 (정문 안내데스크)
  ↓           ↓           ↓
채팅 서버   파일 서버   모델 서버
```

게이트웨이가 하는 일은 크게 세 가지입니다. 외부로부터 들어오는 모든 요청을 가장 먼저 받고, 요청의 내용을 보고 어느 서버로 보낼지 판단한 뒤, 해당 서버의 응답을 다시 사용자에게 돌려줍니다.

조금 더 구체적으로 살펴보면, 게이트웨이는 요청 URL의 **경로(Path)** 를 기준으로 판단합니다.

| 요청 URL | 전달 대상 |
|---|---|
| `https://example.com/chat` | → 채팅 서버 |
| `https://example.com/upload` | → 파일 서버 |
| `https://example.com/ai/generate` | → 모델 서버 |

사용자가 보기에는 하나의 사이트이지만, 실제로는 게이트웨이가 뒤에서 요청을 적절히 나눠주고 있는 것입니다. 이 역할을 기술적으로는 **리버스 프록시(Reverse Proxy)** 라고도 부릅니다. 실무에서 이 역할을 수행하는 대표적인 소프트웨어로는 **Nginx**, **Traefik**, **Caddy**, 클라우드 환경에서는 **AWS ALB**, **Cloudflare** 등이 있습니다.

> [!tip] 핵심 정리
> 게이트웨이는 내부 서버들을 대신해서 외부와 소통하는 단일 창구입니다. 사용자는 게이트웨이만 바라보면 되고, 내부의 복잡한 구조는 알 필요가 없습니다.

---

## 5. HTTPS와 SSL — 통신을 암호화하는 구조

인터넷을 통해 데이터가 오갈 때, 아무런 보호 장치가 없으면 중간에서 누군가 그 내용을 엿볼 수 있습니다. 마치 엽서를 보내는 것처럼, 배달 과정에서 누구든 내용을 읽을 수 있는 상태입니다.

이 문제를 해결하는 방식이 **HTTPS**입니다. HTTPS는 HTTP에 **보안 계층(TLS/SSL)** 을 추가한 것으로, 브라우저와 서버 사이의 통신 내용을 암호화합니다.

```
HTTP:   브라우저 ──── "안녕하세요" ────▶ 서버    ← 누구나 읽을 수 있음 (엽서)
HTTPS:  브라우저 ──── "x#k@!2&z" ────▶ 서버    ← 암호화되어 읽을 수 없음 (봉인된 편지)
```

이 암호화를 가능하게 하는 핵심 요소가 **SSL/TLS 인증서**입니다.

> [!info] SSL과 TLS
> SSL(Secure Sockets Layer)은 오래된 이름이고, 현재 실제로 사용되는 기술은 **TLS(Transport Layer Security)** 입니다. 하지만 관례적으로 "SSL 인증서"라는 표현이 여전히 널리 쓰입니다.

SSL 인증서는 두 가지 역할을 합니다.

1. **신원 증명**: "이 서버가 진짜 `example.com`이 맞다"는 것을 제3자(인증기관, CA)가 보증
2. **암호화 키 제공**: 브라우저와 서버가 안전한 통신을 시작할 수 있도록 공개키를 제공

이 과정을 **TLS 핸드셰이크(Handshake)** 라고 하며, 간략히 표현하면 다음과 같습니다.

```
① 브라우저 → 서버:  "안전하게 통신하고 싶어요"
② 서버 → 브라우저:  "여기 제 인증서(공개키)입니다"
③ 브라우저:          인증서 검증 → 신뢰할 수 있는 서버인지 확인
④ 양쪽:              공유 암호키 생성 → 이후 모든 통신을 이 키로 암호화
```

---

## 6. SSL 인증서는 왜 게이트웨이에 두는가

그렇다면 이 SSL 인증서를 어디에 설치해야 할까요? 내부 서버가 여러 대라면 각각에 설치해야 할 것 같지만, 실제로는 **게이트웨이 한 곳에만 설치**하는 것이 일반적입니다.

```
브라우저 ⇄ [HTTPS 암호화 구간] ⇄ 게이트웨이 → (내부 통신) → 내부 서버
```

이 구조를 **SSL 터미네이션(SSL Termination)** 이라고 부릅니다. "터미네이션"은 암호화를 게이트웨이에서 "종료"시킨다는 뜻입니다.

이유는 세 가지입니다.

| 이유 | 설명 |
|---|---|
| **암호화 경계** | 외부 인터넷과 직접 만나는 지점이 게이트웨이이므로, 암호화가 필요한 구간은 "브라우저 ↔ 게이트웨이" 사이입니다 |
| **관리 효율** | 인증서를 한 곳에서만 관리하면 갱신·교체가 간편합니다. 무료 인증서 발급 서비스인 **Let's Encrypt**를 사용하면 자동 갱신도 가능합니다 |
| **내부 안전** | 게이트웨이 뒤쪽의 내부 네트워크(VPC)는 외부에서 직접 접근할 수 없는 격리된 환경이므로 상대적으로 안전합니다 |

건물에 비유하면 정문에 보안 게이트를 한 번만 설치하면, 건물 내부에서 부서 간에 이동할 때는 별도의 보안 검색을 거치지 않아도 되는 것과 같습니다.

> [!tip] 핵심 정리
> SSL 인증서를 게이트웨이에 집중 배치하면 보안과 관리 효율을 동시에 확보할 수 있습니다. 이를 SSL 터미네이션이라 합니다.

---

## 7. 전체 흐름을 하나로 연결하면

지금까지의 내용을 순서대로 정리하면 다음과 같은 자연스러운 흐름이 완성됩니다.

웹은 기본적으로 **HTTP 프로토콜 위의 요청-응답** 구조입니다. 서비스가 커져서 기능이 많아지면 하나의 서버로는 한계가 있으므로 **기능별로 서버를 분리(마이크로서비스)** 합니다. 그런데 외부 사용자에게 여러 서버를 직접 노출할 수는 없으므로 하나의 출입구인 **게이트웨이(리버스 프록시)** 를 둡니다. 인터넷 통신의 보안을 위한 **SSL 인증서는 그 게이트웨이에 설치(SSL 터미네이션)** 하여, 외부와의 모든 통신이 암호화된 상태로 이루어지도록 합니다.

```
 ┌───────────────────────────────────────────────────────┐
 │                    인터넷 (외부)                         │
 │                                                       │
 │   사용자가 https://example.com/chat 을 입력             │
 │        ↓                                              │
 │   ① DNS가 example.com → 203.0.113.10 으로 변환         │
 │        ↓                                              │
 │   ② TLS 핸드셰이크 (SSL 인증서 검증 + 암호화 시작)       │
 └───────────────────────┬───────────────────────────────┘
                         │ HTTPS (암호화된 요청)
              ┌──────────▼──────────┐
              │      게이트웨이       │
              │  (Nginx / Traefik)  │
              │                     │
              │  ③ SSL 터미네이션    │  ← 여기서 암호화 해제
              │  ④ 경로 분석: /chat  │  ← 요청을 어디로 보낼지 판단
              │  ⑤ 내부 서버로 전달   │
              └──────────┬──────────┘
         ┌───────────────┼───────────────┐
         ▼               ▼               ▼
  ┌──────────┐    ┌──────────┐    ┌──────────┐
  │ 채팅 서버  │    │ 모델 서버  │    │ 파일 서버  │
  │ :3000     │    │ :5000     │    │ :4000     │
  └──────────┘    └──────────┘    └──────────┘

  ─────────── 내부 네트워크 VPC (외부에서 직접 접근 불가) ──────────
```

---

## 8. DNS — 도메인이 서버 주소로 변환되는 과정

사용자가 `https://example.com`을 입력하면, 그 텍스트가 어떻게 실제 게이트웨이에 도달할까요? 컴퓨터끼리는 `example.com` 같은 이름이 아니라 **IP 주소**(예: `203.0.113.10`)라는 숫자 주소로 통신합니다.

이 변환을 담당하는 것이 **DNS(Domain Name System)** 입니다. 인터넷의 "전화번호부"라고 생각하면 됩니다.

```
사용자: "example.com에 접속할래"
  ↓
브라우저 → DNS 서버: "example.com의 IP 주소가 뭐야?"
  ↓
DNS 서버 → 브라우저: "203.0.113.10이야"
  ↓
브라우저 → 203.0.113.10 (게이트웨이): "연결할게!"
```

DNS가 중요한 이유는 **유연성** 때문입니다. 서버의 IP 주소가 바뀌어도 DNS 레코드만 업데이트하면 사용자는 여전히 같은 도메인으로 접근할 수 있습니다. 서버를 다른 데이터센터로 이전하거나, 클라우드 서비스를 교체하더라도 사용자에게는 아무 영향이 없습니다.

> [!info] 도메인은 어디서 구매하나요?
> 도메인은 **도메인 등록기관(Registrar)** 에서 구매합니다. 대표적으로 Namecheap, GoDaddy, 가비아(한국) 등이 있으며, 클라우드 서비스(AWS Route 53, Cloudflare)에서도 구매·관리할 수 있습니다.

---

## 9. 포트(Port) — 하나의 서버에서 여러 프로그램을 구분하는 방법

게이트웨이가 내부 서버로 요청을 전달할 때, 같은 컴퓨터에 여러 서버 프로그램이 동시에 실행 중이라면 어떻게 구분할까요?

답은 **포트(Port)** 번호입니다. IP 주소가 "건물의 주소"라면, 포트 번호는 "건물 안의 호수(방 번호)"에 해당합니다.

```
IP 주소 203.0.113.10 (건물 주소)
  ├── :80   → HTTP 기본 포트      (1층 로비)
  ├── :443  → HTTPS 기본 포트     (보안 출입구)
  ├── :3000 → 채팅 서버           (301호)
  ├── :4000 → 파일 서버           (401호)
  └── :5000 → 모델 서버           (501호)
```

사용자가 `https://example.com`에 접속하면 브라우저는 자동으로 **443번 포트**(HTTPS의 기본 포트)로 연결합니다. 게이트웨이가 443번에서 요청을 받아, 경로를 분석한 뒤 적절한 내부 포트로 전달하는 것입니다.

```
외부 요청: https://example.com/chat   (→ 443 포트로 도착)
게이트웨이: /chat 경로 확인 → 내부 localhost:3000으로 전달
```

> [!tip] 핵심 정리
> 사용자는 포트 번호를 알 필요가 없습니다. 게이트웨이가 숨겨주기 때문입니다. 이것이 바로 앞서 설명한 **추상화**의 또 다른 예입니다.

---

## 10. 로드 밸런싱 — 요청을 여러 서버에 분산하기

서비스가 더 성장하면 어떻게 될까요? 채팅 요청이 폭증하면 채팅 서버 한 대로는 감당이 안 됩니다. 이때 같은 기능의 서버를 여러 대 두고 요청을 **고르게 나누어 주는 것**을 **로드 밸런싱(Load Balancing)** 이라고 합니다.

```
게이트웨이 (로드 밸런서 역할 겸임)
  ├── /chat 요청 → 채팅서버 1  (한가함 ✓)
  ├── /chat 요청 → 채팅서버 2  (한가함 ✓)
  ├── /chat 요청 → 채팅서버 3  (바쁨 ✗ → 건너뜀)
  └── /chat 요청 → 채팅서버 1  (다시 돌아옴)
```

게이트웨이는 요청을 분산할 때 다양한 전략을 사용합니다.

| 전략 | 설명 | 비유 |
|---|---|---|
| **라운드 로빈** | 순서대로 돌아가며 배분 | 카드 돌리기 |
| **최소 연결** | 현재 연결이 가장 적은 서버로 | 가장 짧은 줄에 서기 |
| **가중치 기반** | 성능 좋은 서버에 더 많이 배분 | 힘센 사람에게 더 무거운 짐 |
| **IP 해시** | 같은 사용자는 항상 같은 서버로 | 단골 손님은 같은 직원이 응대 |

로드 밸런싱의 핵심 이점:
- **고가용성(High Availability)**: 한 서버가 다운되어도 나머지가 처리하므로 서비스가 중단되지 않음
- **수평 확장**: 트래픽이 늘면 서버를 추가하기만 하면 됨
- **성능 향상**: 요청이 고르게 분산되어 응답 속도가 빨라짐

---

## 11. 캐싱 — 반복되는 요청을 빠르게 처리하기

게이트웨이는 단순한 "중간 전달자"를 넘어서 **자주 요청되는 응답을 저장(캐시)** 해 두었다가, 같은 요청이 다시 들어오면 내부 서버까지 가지 않고 바로 돌려줄 수 있습니다.

```
첫 번째 요청:
  브라우저 → 게이트웨이 → 내부 서버 → 게이트웨이(캐시에 저장) → 브라우저

두 번째 동일 요청:
  브라우저 → 게이트웨이(캐시에서 바로 응답) → 브라우저   ← 훨씬 빠름!
```

캐싱은 특히 다음과 같은 데이터에 효과적입니다.
- 자주 조회되지만 변경이 적은 데이터 (인기 게시물, 상품 목록)
- 정적 파일 (이미지, CSS, JavaScript)
- API 응답 중 동일 결과가 반복되는 것

더 나아가 **CDN(Content Delivery Network)** 을 활용하면 전 세계 여러 지역에 캐시 서버를 두어, 사용자와 물리적으로 가까운 서버에서 응답을 보낼 수 있습니다. 한국 사용자가 미국 서버의 콘텐츠를 요청해도, 한국에 있는 CDN 서버가 대신 응답해주는 식입니다.

---

## 핵심 개념 정리

|개념|한마디 설명|비유|
|---|---|---|
|**HTTP**|브라우저와 서버가 대화하는 약속된 규칙(프로토콜)|대화의 언어|
|**서버 분리**|기능별로 서버를 나누어 독립적으로 관리·확장하는 구조 (마이크로서비스)|건물 내 부서별 사무실|
|**추상화**|복잡한 내부를 숨기고 단순한 인터페이스만 외부에 노출하는 원칙|건물 내부를 모르는 방문객|
|**게이트웨이**|외부 요청을 가장 먼저 받아 적절한 내부 서버로 전달하는 단일 출입구 (리버스 프록시)|건물 정문의 안내데스크|
|**HTTPS / TLS**|브라우저와 서버 사이의 통신 내용을 암호화하는 프로토콜|편지를 봉투에 넣어 봉인하는 것|
|**SSL 인증서**|서버의 신원을 증명하고 암호화 통신을 가능하게 하는 전자 인증서|정문의 보안 게이트 + 신분증|
|**SSL 터미네이션**|게이트웨이에서 암호화를 해제하여 내부 서버 부담을 줄이는 방식|정문에서만 보안검색|
|**DNS**|도메인 이름을 IP 주소로 변환하는 시스템|전화번호부|
|**포트**|같은 IP에서 여러 프로그램을 구분하는 번호|건물 안의 호수(방 번호)|
|**로드 밸런싱**|동일 기능의 여러 서버에 요청을 고르게 분산하는 기법|마트 계산대 줄 분산|
|**캐싱**|자주 요청되는 응답을 저장해 두었다가 빠르게 반환하는 기법|자주 쓰는 서류를 책상 위에 꺼내두기|
|**CDN**|전 세계에 분산된 캐시 서버로 사용자와 가까운 곳에서 콘텐츠를 제공|전국 각지의 편의점 물류창고|

---

> [!abstract] 다음 단계
> 이 네트워크 구조를 이해한 뒤, 자연스럽게 이어지는 주제들입니다.
> - **Docker와 컨테이너**: 여러 서버를 한 컴퓨터 안에서 충돌 없이 실행하는 방법
> - **CI/CD**: 코드 변경이 자동으로 서버에 배포되는 파이프라인
> - **클라우드 서비스**: AWS, GCP, Azure 등이 제공하는 매니지드 서비스
> - **모니터링**: 서버의 건강 상태를 실시간으로 추적하는 방법
