---
date: 2026-05-17
tags:
  - Next_js
  - 백엔드
  - 서버리스
  - Firebase
  - Supabase
  - BaaS
  - API
  - 프론트엔드
  - Serverless
contexts:
  - Knowledge
  - Lecture
index:
  - Vibe Coding
summary: Next.js 프로젝트가 전통적인 백엔드 서버를 포함하는지 확인하려면 먼저 src/app/api 또는 pages/api 폴더가 있는지 살펴보는 것이 가장 확실하다. api 폴더가 없다면 서버 코드를 직접 만들지 않았을 가능성이 높고, 대신 firebase.ts나 supabase.ts 같은 설정 파일을 통해 Firebase나 Supabase 같은 외부 서비스에 인증, 데이터베이스, 서버 역할을 맡겼는지 확인할 수 있다. 더 깊게는 데이터 처리 코드가 fetch로 자체 API 주소를 호출하는지, addDoc이나 getDocs처럼 서비스 전용 함수를 브라우저에서 직접 사용하는지를 보면 백엔드 중심 구조인지 서버리스 프론트엔드 중심 구조인지 구분할 수 있다.
author:
people:
YouTube_url:
scr_url:
---
## 🔍 초보자를 위한 Next.js 백엔드 코드 구별법

Next.js 프로젝트 폴더를 열었을 때, 이 앱이 "전통적인 백엔드 서버"를 가지고 있는지 아니면 "프론트엔드 중심(서버리스)"으로 만들어졌는지 한눈에 알아보는 3가지 탐정 수사법입니다.

![백엔드 코드 구별법.png](file:///G:/%EB%82%B4%20%EB%93%9C%EB%9D%BC%EC%9D%B4%EB%B8%8C/Obsidian.library/images/MP9RHX9V2N42V.info/%EB%B0%B1%EC%97%94%EB%93%9C%20%EC%BD%94%EB%93%9C%20%EA%B5%AC%EB%B3%84%EB%B2%95.png)


#### 📂 1단계: `api` 폴더 찾아보기 (가장 확실한 방법!)

- **Next.js의 규칙**: Next.js 프레임워크에서 직접 백엔드(서버) 코드를 작성하려면, 반드시 `src/app/api/` (또는 과거 버전의 경우 `pages/api/`) 라는 이름의 폴더 안에 코드를 넣어야 한다는 약속이 있습니다.
- **수사 방법**: 프로젝트의 `src/app/` 폴더 안을 확인하세요.
    - `api` 폴더가 **있다** ➔ "아, Next.js 안에 자체적인 백엔드 서버 코드가 있구나!"
    - `api` 폴더가 **없다** ➔ "서버 코드는 안 만들었군!" (90% 이상 확신 가능)

#### 🧰 2단계: 외부 서비스(BaaS) 설정 파일 확인하기

- 백엔드가 없다면 누군가는 그 역할을 대신해 주어야 합니다.
- **수사 방법**: 프로젝트 최상위 폴더나 `src/lib/` 폴더 안에 다음과 같은 파일이 있는지 확인하세요.
    - `firebase.ts` (구글 파이어베이스)
    - `supabase.ts` (수파베이스)
- **결론**: 이런 파일이 보인다면, "서버와 데이터베이스를 직접 구축하지 않고 외부 전문 서비스에 통째로 역할을 맡겼구나!" 라고 알 수 있습니다.

#### 🔍 3단계: 데이터 저장/불러오기 코드 살펴보기 (심화)

코드를 살짝 읽을 수 있다면 데이터를 주고받는 '방식'을 보면 확실히 알 수 있습니다.

- **백엔드가 있는 경우 (API 통신)**
    - `fetch("https://우리서버주소.com/api/posts")` 처럼 **인터넷 주소(URL)**를 부르면서 서버에 데이터를 요청합니다.
- **프론트엔드 중심인 경우 (Firebase 등 사용)**
    - 인터넷 주소를 부르는 대신, `addDoc()`, `getDocs()` 와 같이 파이어베이스가 제공하는 **전용 마법 주문(함수)**을 사용하여 브라우저에서 직접 데이터베이스와 통신합니다.

---

> 💡 **최종 요약** 남의 코드를 볼 때, **"`api` 폴더가 전혀 없고, 파이어베이스(`firebase.ts`) 설정 파일이 보인다"**면? 그 프로젝트는 백엔드 개발자 없이 프론트엔드 중심으로 구성된 **서버리스(Serverless)** 앱이라고 100% 확신할 수 있습니다! 😎
