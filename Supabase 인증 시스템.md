---
date: 2026-01-18
tags:
  - JWT
  - vibe_coding
  - supabase
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---



# Supabase 인증 시스템 완벽 가이드

웹 애플리케이션을 만들 때 사용자 로그인과 데이터베이스 접근을 어떻게 안전하게 관리할까요? Supabase는 이러한 복잡한 작업을 자동으로 처리해주는 현대적인 백엔드 서비스입니다. 이 문서에서는 Supabase의 인증 시스템이 어떻게 작동하는지, 개발자는 무엇을 해야 하는지 처음부터 차근차근 알아보겠습니다.

## 1. 기본 개념: 토큰이란 무엇인가?

### 현실 세계의 비유

학교 출입증을 생각해봅시다. 선생님이 아침에 교문에서 출입증을 받으면, 그날 하루 동안 교무실, 교실, 도서관 등 학교 내 여러 곳을 자유롭게 드나들 수 있습니다. 매번 교문에 가서 신분을 확인받을 필요가 없습니다. 출입증이 곧 "나는 이 학교의 선생님입니다"라는 증명이기 때문입니다.

웹 애플리케이션도 똑같습니다. 사용자가 로그인하면 "토큰"이라는 디지털 출입증을 받습니다. 이후 데이터베이스에서 정보를 가져오거나, 성적을 수정하거나, 파일을 업로드할 때마다 이 토큰을 보여주면 됩니다. 서버는 토큰을 확인하고 "아, 이 사람은 인증된 사용자구나" 하고 요청을 처리해줍니다.

### 웹에서의 인증 흐름

전통적인 방식에서는 매우 복잡했습니다. 개발자가 직접:

- 비밀번호를 안전하게 암호화해서 저장하고
- 로그인할 때마다 데이터베이스에서 사용자 정보를 확인하고
- 세션을 관리하고
- 보안 취약점을 막고
- 비밀번호 찾기 기능을 구현하고
- 이메일 인증을 처리하고...

수백 줄의 복잡한 코드가 필요했습니다. 하지만 Supabase 같은 현대적인 서비스는 이 모든 것을 자동으로 처리해줍니다. 개발자는 "로그인" 버튼을 눌렀을 때 Supabase에게 "이 사용자 로그인시켜줘"라고 요청만 하면 됩니다.

## 2. 두 가지 토큰: 액세스 토큰과 리프레시 토큰

Supabase는 보안을 위해 두 가지 종류의 토큰을 사용합니다. 이는 마치 호텔의 룸키와 프런트 데스크 카드를 함께 받는 것과 비슷합니다.

### 액세스 토큰 (Access Token) - 짧은 출입증

**역할**: 실제 작업을 수행할 때 사용하는 토큰입니다. **수명**: 보통 1시간

학생 관리 시스템에서 선생님이 학생 목록을 조회하거나, 성적을 입력하거나, 출석을 체크할 때마다 이 액세스 토큰이 요청과 함께 전송됩니다. Supabase 서버는 이 토큰을 확인하고 "이 사람은 선생님이 맞네, 요청을 처리해주자"라고 판단합니다.

**왜 1시간만 유효할까요?**

만약 누군가 이 토큰을 훔쳐간다면 어떻게 될까요? 토큰을 가진 사람은 선생님인 척 행동할 수 있습니다. 이것이 토큰 탈취의 위험입니다. 그래서 짧은 수명을 줌으로써, 설령 토큰이 탈취되더라도 피해 기간을 최대 1시간으로 제한하는 것입니다.

### 리프레시 토큰 (Refresh Token) - 긴 출입증

**역할**: 액세스 토큰이 만료되었을 때 새로운 액세스 토큰을 받기 위한 토큰입니다. **수명**: 보통 30일

1시간마다 사용자에게 다시 로그인하라고 하면 너무 불편할 것입니다. 그래서 리프레시 토큰이 있습니다. 액세스 토큰이 만료되면, Supabase 클라이언트가 자동으로 리프레시 토큰을 사용하여 새로운 액세스 토큰을 발급받습니다. 사용자는 이 과정을 전혀 느끼지 못합니다.

### 두 토큰의 협력

오전 9시에 선생님이 로그인했다고 가정해봅시다:

- 9:00 - 로그인 성공, 액세스 토큰(1시간)과 리프레시 토큰(30일) 받음
- 9:30 - 학생 목록 조회, 액세스 토큰 사용
- 10:00 - 액세스 토큰 만료! 하지만 문제없음
- 10:00:01 - Supabase 클라이언트가 자동으로 리프레시 토큰으로 새 액세스 토큰 받음
- 10:30 - 성적 입력, 새로운 액세스 토큰 사용

선생님은 한 번 로그인한 후 중단 없이 계속 작업할 수 있습니다. 하지만 보안은 유지됩니다. 액세스 토큰은 자주 갱신되어 탈취 위험이 줄어들고, 리프레시 토큰은 실제 데이터 접근에는 사용되지 않아 노출 빈도가 낮습니다.

## 3. JWT의 마법: 위조 불가능한 디지털 서명

### JWT란 무엇인가?

JWT(JSON Web Token)는 Supabase가 사용하는 토큰 형식입니다. 이것의 핵심은 **디지털 서명**입니다.

일반 문서에 도장을 찍으면 그 문서가 진짜임을 증명할 수 있습니다. 누군가 문서 내용을 몰래 바꾸면 도장이 일치하지 않아서 위조임을 알 수 있습니다. JWT도 똑같은 원리입니다.

### 서명 과정

Supabase 서버는 "시크릿 키"라는 비밀 암호를 가지고 있습니다. 이 키는 절대로 외부에 공개되지 않으며, 오직 Supabase 서버만 알고 있습니다.

토큰을 만들 때:

1. 사용자 정보를 준비합니다 (이메일, 사용자 ID, 권한 등)
2. 만료 시간을 추가합니다 (예: 1시간 후)
3. 이 모든 정보를 시크릿 키로 "서명"합니다
4. 완성된 토큰을 사용자에게 전달합니다

토큰을 검증할 때:

1. 사용자가 토큰과 함께 요청을 보냅니다
2. Supabase 서버가 토큰에서 정보를 추출합니다
3. 시크릿 키로 서명을 다시 계산합니다
4. 계산한 서명과 토큰의 서명을 비교합니다
5. 일치하면 진짜, 일치하지 않으면 위조

### 왜 위조가 불가능한가?

해커가 토큰을 가로채서 "일반 사용자"를 "관리자"로 바꾸려고 시도한다고 가정해봅시다. 토큰의 내용을 수정하는 순간, 서명이 맞지 않게 됩니다. 올바른 서명을 만들려면 시크릿 키가 필요한데, 해커는 이 키를 모릅니다. 따라서 Supabase 서버는 즉시 위조된 토큰임을 알아챕니다.

### 그렇다면 JWT는 완벽하게 안전한가?

여기서 중요한 깨달음이 있습니다: **JWT는 위조와 변조는 막을 수 있지만, 탈취는 막을 수 없습니다.**

정상적으로 발급된 진짜 토큰을 누군가 그대로 복사해간다면? 그 토큰은 완벽하게 유효합니다. 서명도 맞고, Supabase가 발급한 것도 맞습니다. 서버 입장에서는 이것이 정당한 사용자의 요청인지, 토큰을 훔친 사람의 요청인지 구분할 수 없습니다.

이것이 바로 액세스 토큰의 수명이 짧은 이유입니다. 토큰이 탈취되더라도 1시간 후에는 자동으로 무효화되어 피해를 최소화합니다.

## 4. 리프레시 토큰 Rotation: 탈취 감지 메커니즘

Supabase는 한 단계 더 나아가 "Rotation"이라는 보안 기법을 사용합니다.

### Rotation의 원리

액세스 토큰을 갱신할 때마다 리프레시 토큰도 함께 새로 발급됩니다. 이전 리프레시 토큰은 즉시 무효화됩니다.

**정상적인 상황:**

- 10시: refresh_token_A 사용 → 새로운 access_token_1과 refresh_token_B 발급
- 11시: refresh_token_B 사용 → 새로운 access_token_2과 refresh_token_C 발급
- 계속 갱신...

**토큰 탈취 시나리오:**

- 10시: 해커가 refresh_token_A를 탈취
- 11시: 정상 사용자가 먼저 refresh_token_A 사용 → refresh_token_B 발급, A는 무효화
- 11시 5분: 해커가 refresh_token_A 사용 시도
- Supabase: "이 토큰은 이미 사용되었는데 또 사용하려고 함? 의심스럽다!"
- Supabase: 해당 사용자의 모든 세션 강제 종료

이미 사용된 리프레시 토큰이 다시 사용되는 것은 명백히 비정상적인 상황입니다. 정상적인 사용자라면 이미 새로운 토큰을 받았을 테니까요. 이를 통해 Supabase는 토큰 탈취를 감지하고 대응할 수 있습니다.

## 5. 브라우저에서의 토큰 저장

### localStorage: 토큰의 집

사용자가 로그인하면 토큰이 어디에 저장될까요? 기본적으로 브라우저의 **localStorage**에 저장됩니다.

localStorage는 웹사이트가 사용자의 브라우저에 데이터를 영구적으로 저장할 수 있는 공간입니다. 마치 컴퓨터의 하드 드라이브처럼, 브라우저를 닫았다가 다시 열어도 데이터가 남아있습니다.

**저장되는 내용:**

- 액세스 토큰
- 리프레시 토큰
- 토큰 만료 시간
- 사용자 정보 (이메일, ID 등)

**왜 localStorage를 사용할까?**

사용자가 아침에 로그인했다가 점심시간에 브라우저를 닫고, 오후에 다시 브라우저를 열어도 로그인 상태가 유지되어야 합니다. localStorage에 토큰이 저장되어 있기 때문에 가능한 일입니다.

### 다른 저장 방식들

상황에 따라 다른 저장 방식을 선택할 수 있습니다:

**sessionStorage (탭을 닫으면 삭제)** 공용 컴퓨터를 사용하는 환경에 적합합니다. 교무실 공용 PC에서 작업하고 브라우저 탭을 닫으면 자동으로 로그아웃됩니다.

**메모리 전용 (저장 안 함)** 가장 안전하지만 가장 불편합니다. 페이지를 새로고침하면 로그아웃됩니다. 매우 민감한 관리자 페이지에서 사용할 수 있습니다.

### 보안 고려사항

localStorage에 토큰을 저장하는 것은 편리하지만 위험도 있습니다. 악의적인 스크립트가 웹사이트에 주입되면 (XSS 공격) localStorage의 토큰을 훔쳐갈 수 있습니다.

**방어 방법:**

- 사용자 입력을 철저히 검증하여 스크립트 주입 방지
- HTTPS만 사용하여 네트워크 전송 중 탈취 방지
- 중요한 작업(성적 수정, 권한 변경 등) 전에는 비밀번호 재확인 요구
- Content Security Policy 설정으로 허용되지 않은 스크립트 실행 차단

## 6. Supabase 클라이언트: 모든 것을 연결하는 다리

### 클라이언트가 필요한 이유

우리의 앱은 브라우저에서 실행되고, Supabase 데이터베이스는 클라우드 서버에 있습니다. 이 둘을 연결하려면 "통신 채널"이 필요합니다. 그것이 바로 **Supabase 클라이언트**입니다.

택배 서비스에 비유하면:

- Supabase DB = 창고 (데이터가 저장된 곳)
- 우리 앱 = 집 (데이터를 사용하는 곳)
- Supabase 클라이언트 = 택배 기사 (데이터를 전달하는 역할)

창고에서 직접 물건을 가져올 수 없듯이, 클라이언트 없이는 데이터베이스에 접근할 수 없습니다.

### 클라이언트가 자동으로 처리하는 것들

Supabase 클라이언트를 생성하면, 매우 많은 일들이 자동으로 설정됩니다:

**인증 관련:**

- 로그인 시 토큰 자동 저장
- localStorage에서 토큰 자동 로드
- 액세스 토큰 만료 감지
- 리프레시 토큰으로 자동 갱신
- 로그아웃 시 토큰 자동 삭제

**API 요청 관련:**

- 모든 요청에 액세스 토큰 자동 포함
- 네트워크 오류 시 자동 재시도
- 적절한 HTTP 헤더 자동 설정
- JSON 형식 자동 변환

**보안 관련:**

- 토큰 유효성 자동 검증
- 만료된 토큰 자동 갱신
- 동시 요청 시 토큰 갱신 한 번만 수행

만약 클라이언트가 없다면 개발자가 이 모든 것을 직접 구현해야 합니다. 수백 줄의 복잡한 코드가 필요할 것입니다.

### 개발자가 실제로 해야 하는 일

다행히도 개발자가 해야 할 일은 매우 간단합니다:

**1단계: 라이브러리 설치** 프로젝트에 Supabase JavaScript 라이브러리를 추가합니다. 터미널에서 한 줄의 명령어만 실행하면 됩니다.

**2단계: 클라이언트 생성 파일 작성** 프로젝트에 새 파일을 하나 만들고, 3~5줄의 코드를 작성합니다. Supabase 프로젝트의 URL과 API 키를 입력하여 클라이언트를 생성하는 코드입니다.

**3단계: 필요한 곳에서 사용** 로그인 페이지, 학생 목록 페이지, 성적 입력 페이지 등 어디서든 클라이언트를 불러와 사용하면 됩니다.

**이게 전부입니다.** 나머지 복잡한 토큰 관리, 자동 갱신, 보안 처리는 Supabase 클라이언트가 알아서 합니다.

### 클라이언트 생성은 언제 일어날까?

중요한 점은 클라이언트 생성이 로그인보다 먼저 일어난다는 것입니다:

**정확한 순서:**

1. 개발자가 클라이언트 생성 코드를 작성 (개발 시점)
2. 사용자가 앱에 접속 (런타임)
3. 클라이언트가 브라우저에서 초기화됨
4. 사용자가 로그인 시도
5. 클라이언트를 통해 로그인 요청 전송
6. 액세스 토큰과 리프레시 토큰 발급
7. 클라이언트가 자동으로 localStorage에 저장

로그인을 하려면 먼저 클라이언트가 있어야 하기 때문입니다. 클라이언트 생성 코드는 앱의 "기본 설정"이라고 생각하면 됩니다.

### 한 번만 만들면 끝

클라이언트는 프로젝트당 한 번만 생성하면 됩니다. 그 후로는 앱의 모든 곳에서 같은 클라이언트를 재사용합니다.

마치 회사에 우체부가 한 명 있으면, 그 우체부가 모든 부서의 우편물을 배달하는 것과 같습니다. 부서마다 별도의 우체부를 고용할 필요가 없습니다.

## 7. 전체 흐름 정리

이제 모든 것을 하나의 흐름으로 정리해봅시다.

### 개발 단계 (개발자가 한 번만 하는 일)

**1. Supabase 프로젝트 생성** Supabase 웹사이트에서 새 프로젝트를 만들고, 데이터베이스 테이블을 설계합니다.

**2. 개발 환경 설정** 컴퓨터에서 프로젝트 폴더를 만들고, Supabase 라이브러리를 설치합니다.

**3. 클라이언트 생성 코드 작성** 한 개의 파일에 3~5줄의 코드를 작성하여 Supabase 클라이언트를 생성합니다.

**4. 로그인 UI 만들기** 사용자가 이메일과 비밀번호를 입력할 수 있는 로그인 화면을 만듭니다.

**5. 인증 상태 관리** 로그인 상태에 따라 다른 화면을 보여주도록 앱을 구성합니다.

### 런타임 단계 (앱이 실행될 때 자동으로 일어나는 일)

**사용자의 첫 방문:**

1. 사용자가 브라우저에서 앱 주소 입력
2. JavaScript 파일들이 로드되면서 Supabase 클라이언트 생성
3. 클라이언트가 localStorage 확인 → 토큰 없음
4. 로그인 화면 표시

**로그인:**

1. 사용자가 이메일과 비밀번호 입력 후 로그인 버튼 클릭
2. 클라이언트가 Supabase 서버에 로그인 요청 전송
3. 서버가 비밀번호 확인 후 액세스 토큰과 리프레시 토큰 발급
4. 클라이언트가 자동으로 localStorage에 토큰 저장
5. 메인 화면 표시

**데이터 사용:**

1. 사용자가 학생 목록 조회 요청
2. 클라이언트가 자동으로 액세스 토큰을 요청에 포함
3. Supabase 서버가 토큰 검증 후 데이터 반환
4. 화면에 학생 목록 표시

**1시간 후 (자동 토큰 갱신):**

1. 사용자가 성적 입력 시도
2. 클라이언트가 액세스 토큰 만료 감지
3. 자동으로 리프레시 토큰으로 새 액세스 토큰 요청
4. 새 토큰 쌍 발급 및 localStorage 업데이트
5. 원래 요청했던 성적 입력 자동 재시도
6. 사용자는 중단 없이 작업 완료

**다음 날 재방문:**

1. 사용자가 브라우저에서 앱 주소 입력
2. 클라이언트가 localStorage 확인 → 토큰 발견
3. 토큰 유효성 검사
4. 로그인 화면 건너뛰고 바로 메인 화면 표시

## 8. 핵심 요약

### Supabase 인증 시스템의 장점

**개발자 관점:**

- 복잡한 인증 로직을 직접 구현할 필요 없음
- 3~5줄의 간단한 코드로 전체 시스템 구축
- 토큰 관리, 갱신, 보안이 모두 자동화됨
- 버그와 보안 취약점 위험 최소화

**사용자 관점:**

- 한 번 로그인하면 오래 지속됨 (30일)
- 중간에 끊김 없는 부드러운 경험
- 빠른 응답 속도
- 안전한 데이터 보호

### 꼭 기억해야 할 것들

**1. 두 가지 토큰의 역할**

- 액세스 토큰: 실제 작업용, 짧은 수명 (1시간)
- 리프레시 토큰: 갱신용, 긴 수명 (30일)

**2. JWT의 보안 특성**

- 위조/변조는 불가능 (시크릿 키로 서명)
- 하지만 탈취에는 취약
- 짧은 수명으로 탈취 피해 최소화

**3. Rotation의 중요성**

- 토큰 갱신 시 리프레시 토큰도 갱신
- 이미 사용된 토큰 재사용 시도를 감지
- 자동으로 보안 위협 대응

**4. 클라이언트의 역할**

- 앱과 Supabase 서버를 연결하는 다리
- 모든 복잡한 로직을 자동 처리
- 개발자는 간단한 생성 코드만 작성

**5. 필수 개발 작업**

- Supabase 클라이언트 생성 코드 작성 (필수)
- 로그인 UI 만들기
- 인증 상태에 따른 화면 분기

### 현대 백엔드 서비스(BaaS)의 가치

Supabase 같은 서비스는 "개발자 경험 최적화"를 목표로 합니다. 과거에는 며칠, 몇 주가 걸리던 인증 시스템 구축을 이제는 몇 분 만에 완성할 수 있습니다. 개발자는 복잡한 인프라와 보안 문제에 시간을 쓰는 대신, 사용자에게 가치를 제공하는 기능 개발에 집중할 수 있습니다.

토큰 관리, 자동 갱신, 보안 처리 같은 "복잡하지만 모든 앱에 필요한" 작업들을 Supabase가 대신 처리해주므로, 초보 개발자도 전문가 수준의 안전한 애플리케이션을 만들 수 있습니다. 이것이 바로 현대 웹 개발의 힘입니다.

