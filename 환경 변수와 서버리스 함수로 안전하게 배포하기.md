---
date: 2026-06-20
tags:
  - vibe_coding
  - 환경변수
  - 서버리스
  - API키
  - Vercel
  - Netlify
  - 웹보안
  - 개인정보보호
category: 바이브코딩
summary: 바이브코딩으로 만든 교실 도구를 배포하기 직전, "그 환경 변수가 사용자 브라우저에서 읽히는가, 서버 안에서만 읽히는가"를 기준으로 비밀 키 노출을 점검하는 법. .env·.env.local·.env.example의 차이, 로컬과 배포 환경 변수의 분리, NEXT_PUBLIC·VITE 접두어의 의미를 짚고, 배포 시에는 API 키를 환경 변수에 두고 외부 호출을 서버리스 함수에 맡기라는 방향을 교사 개발자의 운영 책임 관점에서 제시한다.
---
## 환경 변수와 서버리스 함수로 안전하게 배포하기

> 바이브코딩으로 만든 교실 도구를 배포하기 전에 멈춰 서야 할 지점 · 대상: 처음 배포에 나서는 교사 개발자

> [!quote] 핵심 메시지
> 환경 변수를 쓴다는 사실만으로 안전해지지 않는다. 진짜 질문은 ==그 값이 사용자 브라우저에서 읽히는가, 아니면 서버 안에서만 읽히는가== 이다.

바이브코딩으로 만든 도구는 생각보다 빨리 그럴듯해진다. 화면이 생기고, 입력창이 생기고, 버튼을 누르면 AI가 답을 해 준다. 로컬 화면에서 잘 작동하면 이제 배포만 하면 될 것처럼 느껴진다. 링크만 생기면 다른 사용자도, 동료 교사도 바로 쓸 수 있을 것 같다.

문제는 배포 직전에 숨어 있다. 화면에서는 보이지 않지만, 앱 안에는 외부 AI를 부르는 키가 들어 있을 수 있다. 사용자가 입력한 문장이 외부 서비스로 전송될 수 있다. 내 컴퓨터에서는 잘 보이지 않던 설정 파일이 GitHub에 올라갈 수도 있고, 배포 플랫폼에서는 로컬에서 쓰던 키를 찾지 못해 앱이 멈출 수도 있다.

이때 등장하는 말이 환경 변수다. AI 도구는 종종 "환경 변수를 설정하세요", "`.env.local`에 키를 넣으세요", "Vercel에 Environment Variables를 등록하세요"라고 말한다. 개발 경험이 없는 교사에게는 이 말이 막연하게 느껴질 수 있다. 하지만 핵심은 단순하다. 환경 변수는 **코드 안에 직접 쓰면 안 되는 값을 코드 밖에 따로 두는 방법**이다.

다만 환경 변수를 쓴다고 해서 자동으로 안전해지는 것은 아니다. 더 중요한 질문은 이것이다.

**그 환경 변수는 사용자 브라우저에서 읽히는가, 아니면 서버 안에서만 읽히는가?**

이 질문에 답하지 못한 채 배포하면, 겉으로는 정상 작동하지만 API 키가 사용자에게 노출되는 구조가 될 수 있다. 그래서 환경 변수는 [[서버리스 함수의 정체와 API 키 보호 없음|서버리스 함수]]와 함께 이해해야 한다.

## 환경 변수는 왜 필요한가

웹앱이 외부 서비스를 사용하려면 대개 키가 필요하다. Gemini, OpenAI, Stripe, SendGrid, 지도 API, 날씨 API처럼 외부 서비스에 요청을 보내려면 "내가 허가받은 사용자입니다"라고 증명하는 값이 필요하다. 이것이 API 키다.

API 키는 단순한 글자가 아니다. 비용, 사용량, 권한과 연결된 열쇠다. 이 키가 공개되면 누군가 내 계정으로 외부 API를 호출할 수 있다. AI API라면 비용이 발생할 수 있고, 데이터베이스 관리자 키라면 데이터 접근 권한이 열릴 수도 있다. 그래서 API 키는 코드에 직접 적으면 안 된다.

환경 변수는 이 키를 코드 밖에 두기 위한 방법이다. 코드에는 "어떤 이름의 키를 읽어야 하는지"만 남겨 두고, 실제 값은 따로 보관한다. 내 컴퓨터에서는 `.env`나 `.env.local` 같은 파일에 두고, 배포할 때는 Netlify나 Vercel 같은 플랫폼의 환경 변수 설정 화면에 둔다.

여기서 교사가 이해해야 할 핵심은 "키를 어디에 적느냐"보다 "그 키가 누구에게 보이느냐"이다. 내 컴퓨터 안에서만 쓰는 키와, 사용자 브라우저로 전달되는 키는 책임의 무게가 다르다.

## `.env`, `.env.local`, `.env.example`은 무엇이 다른가

환경 변수 파일은 이름이 비슷해서 헷갈리기 쉽다. 하지만 역할을 나누면 분명하다. 처음에는 다음 세 가지만 구분해도 충분하다.

| 이름 | 역할 | 실제 비밀 값 포함 여부 | GitHub 업로드 |
|---|---|---|---|
| `.env` | 로컬 개발에서 쓰는 기본 환경 변수 파일 | 포함될 수 있음 | 보통 제외 |
| `.env.local` | 내 컴퓨터에서만 쓰는 개인 환경 변수 파일 | 포함됨 | 제외 |
| `.env.example` | 어떤 환경 변수가 필요한지 알려 주는 예시 파일 | 포함하지 않음 | 가능 |

`.env`와 `.env.local`은 실제 키가 들어갈 수 있는 파일이다. 따라서 공개 저장소에 올리면 안 된다. 반대로 `.env.example`은 실제 키가 아니라 변수 이름만 알려 주는 안내서다. 예를 들어 "이 앱을 실행하려면 GEMINI_API_KEY라는 값이 필요합니다"라고 알려 주는 역할이다. (같은 역할의 파일을 `.env.sample`이라고 부르기도 하지만, 더 널리 쓰이는 이름은 `.env.example`이다.)

이 구분은 협업할 때도 중요하다. 실제 키가 담긴 파일은 각자 자기 컴퓨터에만 두고, 저장소에는 예시 파일만 올린다. 그래야 다른 사람이 프로젝트를 열었을 때 어떤 값을 준비해야 하는지 알 수 있으면서도, 비밀 값은 공개되지 않는다.

> [!info] 초보 교사 개발자를 위한 비유
> - `.env.local`은 내 책상 서랍에 넣어 둔 실제 열쇠다.
> - `.env.example`은 "이 서랍에는 이런 이름의 열쇠가 필요합니다"라고 적어 둔 목록이다.
> - GitHub에는 실제 열쇠가 아니라 목록만 올라가야 한다.

## 로컬 환경 변수와 배포 환경 변수는 다르다

많은 오류가 여기서 생긴다. 내 컴퓨터에서 `.env.local`을 만들고 앱을 실행하면 잘 작동한다. 그래서 GitHub에 올리고 Netlify나 Vercel에 배포한다. 그런데 배포된 앱에서는 갑자기 AI 호출이 실패하거나, 키가 없다는 오류가 난다.

이유는 단순하다. `.env.local`은 내 컴퓨터 안의 파일이다. 보안을 위해 GitHub에는 올리지 않는다. 배포 플랫폼은 GitHub에 올라간 코드만 받아서 빌드하므로, 내 컴퓨터의 `.env.local`을 자동으로 알 수 없다.

따라서 배포할 때는 같은 이름의 환경 변수를 Netlify나 Vercel 설정 화면에 다시 등록해야 한다. 로컬에서 `GEMINI_API_KEY`라는 이름으로 썼다면, 배포 플랫폼에서도 정확히 같은 이름으로 등록해야 한다. 대소문자나 밑줄 하나가 달라도 앱은 그 값을 찾지 못한다.

이 과정은 귀찮은 중복 작업처럼 보일 수 있지만, 사실은 안전장치다. 비밀 값을 GitHub에 올리지 않기 때문에, 배포 플랫폼에 별도로 안전하게 넣는 것이다.

## 브라우저로 가는 환경 변수는 비밀이 아니다

Next.js나 Vite 같은 프레임워크에는 브라우저로 보내도 되는 환경 변수를 표시하는 접두어가 있다. Next.js에서는 `NEXT_PUBLIC_`, Vite에서는 `VITE_`가 대표적이다.

이 접두어가 붙은 변수는 브라우저 코드에 포함될 수 있다. 즉, 사용자가 개발자 도구를 열면 확인할 수 있는 값이라고 생각해야 한다. Firebase의 공개 설정값처럼 브라우저에서 사용해야 하는 값은 이런 접두어를 붙일 수 있다. 하지만 Gemini, OpenAI, Stripe, 데이터베이스 관리자 키처럼 비용이나 권한이 연결된 비밀 키에는 붙이면 안 된다.

> [!warning] 접두어를 붙이는 순간 "공개 선언"이 된다
> "환경 변수니까 안전하겠지"라고 생각하면 안 된다. `NEXT_PUBLIC_`이나 `VITE_`가 붙은 환경 변수는 이름부터 "브라우저로 보내도 된다"는 표시다. 비밀 키에 이 접두어를 붙이는 순간, 비밀을 숨긴 것이 아니라 ==공개하겠다고 선언한 것==에 가까워진다.

따라서 외부 AI API 키처럼 숨겨야 하는 값은 서버에서만 읽혀야 한다. 그리고 사용자 브라우저가 그 키를 직접 사용하지 않도록 중간에 서버리스 함수를 두어야 한다.

## 서버리스 함수가 필요한 순간

화면만 있는 도구라면 서버리스 함수가 필요하지 않을 수 있다. 사용자가 입력한 값이 브라우저 안에서만 잠깐 쓰이고, 저장도 외부 전송도 하지 않는다면 확인할 것이 비교적 적다.

하지만 도구가 AI나 외부 API에 무언가를 물어본다면 상황이 달라진다. 사용자가 쓴 문장을 Gemini나 OpenAI로 보내고 답을 받는다면, 앱은 외부 서비스와 통신하고 있다. 이때 브라우저가 외부 AI를 직접 호출하게 만들면 API 키가 노출될 수 있다.

서버리스 함수는 이때 필요한 중개인이다. 사용자 브라우저는 서버리스 함수에 요청을 보낸다. 서버리스 함수는 클라우드 안에서 환경 변수를 읽어 API 키를 꺼낸다. 그런 다음 외부 AI나 API를 대신 호출하고, 필요한 결과만 브라우저에 돌려준다. 이 과정에서 사용자 브라우저는 API 키를 알지 못한다. ([[서버리스 함수와 Gemini API]]에서 이 흐름을 코드로 따라갈 수 있다.)

서버리스 함수가 필요한 이유는 키를 숨기는 것만이 아니다. 함수 안에서는 사용자 입력을 한 번 더 확인할 수 있다. 빈 입력을 막거나, 너무 긴 입력을 제한하거나, 이름 같은 개인정보를 제거한 뒤 외부 AI로 보낼 수 있다. 오류가 났을 때 사용자에게 내부 에러 내용을 그대로 보여 주지 않고, "잠시 후 다시 시도해 주세요"처럼 안전한 안내로 바꿀 수도 있다.

그래서 AI나 외부 서비스에 물어보는 도구라면 서버리스 함수는 단순한 기술 선택이 아니라 안전장치에 가깝다.

## Netlify·Vercel에 배포할 때

Netlify와 Vercel은 모두 GitHub 저장소를 연결하면 코드가 바뀔 때 자동으로 빌드·배포해 주고, 서버리스 함수 기능(Netlify Functions, Vercel의 API Route)도 함께 제공한다. 세부 설정 화면은 플랫폼마다 조금씩 다르지만, 교사 개발자가 기억할 방향은 두 가지로 충분하다.

첫째, **API 키는 코드가 아니라 플랫폼의 환경 변수에 저장한다.** Netlify는 사이트 설정의 Environment variables에, Vercel은 프로젝트 설정의 Environment Variables에 로컬에서 쓰던 것과 같은 이름으로 값을 넣는다. 이때 비밀 키에는 `NEXT_PUBLIC_`이나 `VITE_` 접두어를 붙이지 않는다.

둘째, **외부 AI나 API 호출은 브라우저가 아니라 서버리스 함수를 거치게 한다.** 그래야 키가 서버 안에 머물고, 사용자 브라우저로 내려가지 않는다.

이 두 가지만 지켜도 "키가 노출되는" 가장 흔한 배포 사고는 막을 수 있다. 환경을 나누어 관리하는 방식(예: Vercel의 Production·Preview)이나 재배포 절차처럼 세부적인 부분은 각 플랫폼 문서를 따르면 된다. 로컬은 잘 되는데 배포 후 실패한다면, 가장 먼저 환경 변수가 같은 이름으로 등록되었는지, 그리고 값을 넣은 뒤 다시 배포했는지를 확인한다.

## 배포 전 교사가 멈춰 볼 질문

환경 변수와 서버리스 함수는 개발자의 세부 기술처럼 보이지만, 교사에게는 운영 책임의 문제다. 사용자가 직접 쓰는 도구라면 배포 직전에 한 번 멈추어 확인해야 한다.

먼저, 이 도구가 외부 AI나 API를 사용하는지 확인한다. 사용한다면 그 키가 어디에 있는지 묻는다. 코드 안에 직접 들어 있는지, `.env.local`에 있는지, Netlify나 Vercel 환경 변수에 등록되어 있는지 구분한다.

다음으로, 그 키가 브라우저로 가는지 확인한다. `NEXT_PUBLIC_`이나 `VITE_` 접두어가 붙은 값이라면 브라우저에 공개될 수 있다. 비밀 키라면 이 접두어가 없어야 하고, 서버리스 함수나 서버 코드 안에서만 사용되어야 한다.

그다음, 사용자 입력이 어디로 가는지 확인한다. 사용자가 입력한 이름, 답변, 작성한 글처럼 개인을 식별할 수 있는 내용이 외부 AI로 전송된다면, 정말 필요한 정보만 보내고 있는지 살펴야 한다. 이름 같은 식별 정보 없이도 작동한다면 보내지 않는 편이 낫다.

마지막으로, 배포 플랫폼 설정을 확인한다. 로컬의 `.env.local`과 Netlify/Vercel의 환경 변수는 별개다. 로컬에서 잘 된다고 배포에서도 자동으로 되는 것은 아니다. 배포 플랫폼에 값을 등록했고, 다시 배포했고, 실제 링크에서 작동하는지 확인해야 한다.

## AI에게 맡겨도 되는 점검, 교사가 직접 판단해야 하는 점검

교사가 모든 코드를 직접 읽을 필요는 없다. AI에게 "이 프로젝트에서 비밀 키가 브라우저에 노출되는지 찾아 달라"고 요청할 수 있다. `.env.local`이 GitHub에 올라가는 구조인지, `NEXT_PUBLIC_`이 붙은 변수 중 비밀 키로 보이는 것이 있는지, 외부 API 호출이 클라이언트 코드에 직접 들어 있는지 점검하게 할 수 있다.

하지만 AI가 대신 판단할 수 없는 것도 있다. 사용자 입력을 외부 AI로 보내도 되는지, 그 정보가 수업 목적에 꼭 필요한지, 안내와 동의가 필요한지, 학교 안의 절차를 거쳐야 하는지는 교사가 판단해야 한다. 기술적 위험은 AI가 찾아 줄 수 있지만, 교육적 책임은 교사에게 남는다.

그래서 좋은 질문은 "이 코드 괜찮아?"가 아니다. 다음처럼 구체적으로 물어야 한다.

- API 키나 비밀 값이 브라우저로 노출되는 구조인가요?
- `.env`, `.env.local` 파일이 GitHub에 올라가지 않도록 되어 있나요?
- `.env.example`에 필요한 변수 이름만 정리되어 있나요?
- Netlify나 Vercel에 등록해야 할 환경 변수 목록은 무엇인가요?
- 사용자 입력이 외부 AI로 전송되기 전에 개인정보를 줄일 수 있나요?
- 외부 API 호출을 서버리스 함수로 옮겨야 하나요?

이 질문들은 코드를 직접 고치기 위한 질문이 아니라, 내가 만든 도구가 어디까지 책임져야 하는지 보기 위한 질문이다.

## 정리

환경 변수는 비밀 값을 코드 밖으로 분리하는 방법이다. 그러나 환경 변수를 쓴다는 사실만으로 안전이 보장되지는 않는다. 그 값이 브라우저로 가는지, 서버 안에 남는지가 더 중요하다.

`.env.local`은 내 컴퓨터에서만 쓰는 실제 비밀 값이고, `.env.example`은 필요한 변수 이름을 알려 주는 안내문이다. 실제 키가 들어 있는 파일은 GitHub에 올리지 않는다. 배포할 때는 Netlify나 Vercel의 환경 변수 설정에 같은 이름으로 값을 따로 등록한다.

AI나 외부 API를 사용하는 도구라면 서버리스 함수를 고려해야 한다. 서버리스 함수는 사용자 브라우저와 외부 서비스 사이에 서서 API 키를 숨기고, 입력을 확인하고, 필요한 결과만 돌려준다. 교사가 서버를 직접 운영하지 않아도 이런 중간 계층을 둘 수 있다는 점이 Netlify와 Vercel 같은 배포 플랫폼의 장점이다.

결국 배포 전 질문은 하나로 모인다.

**이 도구에서 비밀 값과 사용자 입력은 어디를 지나가는가?**

그 흐름을 설명할 수 있다면, 도구는 더 안전해진다. 설명할 수 없다면 배포 전에 한 번 더 멈추어야 한다. 책임 있게 만드는 교사 개발자는 모든 코드를 외우는 사람이 아니라, 보이지 않는 흐름을 확인하고 필요한 순간 멈출 줄 아는 사람이다.

---

> [!info] 관련 노트
> - [[서버리스 함수의 정체와 API 키 보호 없음]] — 한 폴더 안에서 브라우저·서버리스·엣지 코드를 구분하는 법
> - [[Vercel과 route.ts - 서버리스 함수 만들기]] — `route.ts`가 서버리스 함수로 변환·배포되는 과정
> - [[서버리스 함수와 Gemini API]] — 서버리스 함수가 AI API 키를 숨기고 호출을 중개하는 실제 흐름
> - [[route.ts를 서버리스 함수로 변환하는 Vercel과 Netlify의 배포 방식 비교]] — 두 플랫폼의 배포 방식 차이
> - [[프론트엔드와 보안]] — 브라우저로 가는 값이 왜 비밀이 될 수 없는가
> - [[웹 개발과 배포]] — 배포 전체 흐름 속에서의 위치
