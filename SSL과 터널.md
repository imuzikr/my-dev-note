---
date: 2026-02-28
tags:
  - 보안
  - 터미널
  - SSL
  - HTTP
  - 프록시
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---
# 🌐 인터넷 비밀 편지 배달의 모든 것

우리가 웹사이트에 접속해서 정보를 주고받는 과정은 아주 정교한 **비밀 배달 시스템**과 같습니다.

---

## 1. HTTP 메시지: 우리가 주고받는 '편지'

모든 소통의 기본은 메시지입니다.

- **요청(Request):** 클라이언트(나)가 서버(상대방)에게 "이 페이지 좀 보여줘!"라고 보내는 편지입니다.
- **응답(Response):** 서버가 나에게 "알았어, 여기 내용이야!"라고 답장하는 편지입니다.

---

## 2. TCP 포트와 커넥션: '출입문'과 '전화 연결'

편지를 보내려면 먼저 통로가 있어야 합니다.

- **포트(Port):** 한 컴퓨터 안의 여러 프로그램 중 웹 서비스 전용 **'방 번호(예: 80번, 443번)'**입니다.
- **커넥션(Connection):** 데이터를 주고받기 위해 내 컴퓨터와 상대방 서버 사이에 연결된 **'전용 전화선'**입니다.

---

## 3. 프록시 서버: 중간에 서 있는 '대리인'

가끔 내 컴퓨터가 서버와 직접 통화하지 않고, 중간에 **대리인(프록시)**을 거칠 때가 있습니다.

- **포워드 프록시:** 우리(클라이언트) 팀의 보호자. 유해 사이트를 막거나 내 정체를 숨겨줍니다.
- **리버스 프록시:** 서버 팀의 안내데스크. 서버가 너무 바쁘지 않게 업무를 나눠주거나 서버를 보호합니다.
- **중요:** 프록시 서버는 거창한 기계가 아니라, 컴퓨터 안에서 돌아가는 **'중계용 프로그램(코드)'**입니다.

---

## 4. TLS (HTTPS): '강철 금고와 암호화'

편지 내용을 누군가 훔쳐볼까 봐 걱정될 때 **TLS**라는 시스템을 사용합니다.

> 💡 **SSL과 TLS의 관계:** 흔히 'SSL'이라는 이름을 사용하지만, SSL은 보안 취약점으로 인해 이미 사용이 중단된 구식 프로토콜입니다. 현재 실제로 사용되는 것은 **TLS(1.2 또는 1.3 버전)**입니다. 'SSL 인증서', 'SSL 적용' 같은 표현이 여전히 관습적으로 남아 있을 뿐, 실제 동작은 모두 TLS가 담당합니다.

TLS는 단순히 암호화만 하는 것이 아니라, **서버 인증**과 **데이터 암호화** 두 가지 역할을 함께 수행합니다.

### 작동 순서: TLS 핸드셰이크

데이터를 암호화하기 전에, 브라우저와 서버는 먼저 **핸드셰이크(Handshake)**라는 준비 과정을 거칩니다. 이 과정은 실제 데이터를 주고받기 전에 다음 세 가지를 확인하고 합의하는 절차입니다.

1. **서버 신원 확인:** 서버가 공인된 인증 기관(CA)이 서명한 **인증서(Certificate)**를 브라우저에 제출합니다. 브라우저는 이 인증서를 검증하여 "정말 믿을 수 있는 서버인가?"를 확인합니다. 마치 택배 기사가 신분증을 보여주는 것과 같습니다.
2. **암호화 방식 합의:** 브라우저와 서버가 서로 지원하는 암호화 알고리즘 중 가장 안전한 것을 협의해서 선택합니다.
3. **대칭키 교환:** 이후 실제 데이터를 암호화·복호화할 때 사용할 **공통 비밀 키(대칭키)**를 안전하게 교환합니다.

이 핸드셰이크가 완료되면, 비로소 HTTP 편지를 암호화하여 주고받을 수 있게 됩니다. 이렇게 TLS 위에서 동작하는 HTTP를 **HTTPS**라고 부릅니다.

### 암호화란 무엇인가?

암호화는 평문(원래 텍스트)을 **제3자가 읽을 수 없는 암호문(ciphertext)**으로 변환하는 과정입니다. 암호문은 무작위로 생성되는 난수와는 다르게, 정해진 수학적 규칙에 따라 변환된 데이터이며, 올바른 키를 가진 상대방만이 원래 내용으로 복원할 수 있습니다.

---

## 5. HTTP 터널링: '검문소 프리패스'

TLS로 암호화된 데이터(금고)를 들고 프록시(검문소)를 지나가려니, 프록시 입장에서는 금고 안의 내용을 알 수 없어 그냥 통과시켜도 되는지 판단하기 어렵습니다. 이 문제를 해결하는 것이 바로 **HTTP 터널링** 기법입니다.

### 작동 순서

1. **터널 요청 (CONNECT 메서드):** 클라이언트가 프록시에게 HTTP의 `CONNECT` 메서드를 사용하여 "지금부터 특정 서버로 가는 금고가 지나갈 테니, 내용물은 검사하지 말고 길만 열어줘!"라고 요청합니다. `CONNECT`는 HTTP 메서드 중 하나로, 프록시에게 TCP 터널 개설을 명시적으로 요청하는 명령입니다.
    
2. **터널 생성:** 프록시가 이 요청을 수락하면, 프록시 내부에 데이터가 내용 검사 없이 그대로 통과하는 **'비밀 지하차도(TCP 터널)'**가 뚫립니다. 이 시점에서 프록시는 더 이상 데이터의 내용을 해석하지 않고 단순히 양쪽을 연결해주는 중계자 역할만 합니다.
    
3. **TLS 핸드셰이크 시작:** 터널이 뚫린 후에야, 그 터널 안에서 브라우저와 서버 사이의 **TLS 핸드셰이크**가 진행됩니다. 즉, 순서는 "터널 개설 → 그 안에서 TLS 연결 수립 → 암호화된 데이터 전송"입니다.
    
4. **안전한 배달 완료:** 이제 TLS로 암호화된 편지는 프록시의 방해 없이 터널을 통과하여 목적지까지 안전하게 도착합니다.
    

---

## 💡 한눈에 보는 요약표

|개념|비유|핵심 역할|
|---|---|---|
|**HTTP 메시지**|편지|주고받는 실제 데이터 내용|
|**TCP 포트**|방 번호|컴퓨터 내 서비스의 정확한 도착지|
|**TCP 커넥션**|전화 연결|데이터를 주고받기 위한 통신 채널|
|**프록시 서버**|대리인|중간에서 요청을 대신 전달하는 프로그램|
|**TLS (구 명칭: SSL)**|금고 시스템|서버 인증과 데이터 암호화를 담당하는 보안 프로토콜|
|**HTTP 터널**|지하 전용도로|프록시가 암호문을 검사 없이 통과시키는 TCP 통로|

---

## 🎯 결론: 우리가 웹을 서핑할 때 일어나는 일

1. 브라우저와 서버가 **포트**를 통해 **TCP 커넥션**을 맺습니다.
2. 중간에 프록시가 있다면, 브라우저가 `CONNECT` 명령으로 **TCP 터널**을 개설합니다.
3. 터널 안에서 브라우저와 서버가 **TLS 핸드셰이크**를 수행합니다. 이 과정에서 서버 신원을 확인하고, 암호화에 사용할 대칭키를 교환합니다.
4. 핸드셰이크가 완료되면, **HTTP 편지**가 TLS에 의해 **암호문**으로 변환됩니다.
5. 암호화된 편지가 **터널**을 타고 프록시를 통과하여 목적지 서버까지 안전하게 배달됩니다.