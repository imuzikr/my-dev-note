---
date: 2025-10-26
tags:
  - "#RESTfulAPI"
  - "#API설계"
  - "#웹개발"
  - vibe_coding
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---


 
> [!summary]
> RESTful API(Representational State Transfer API)는 웹에서 **자원(데이터)**을 효율적이고 일관된 방식으로 주고받기 위한 약속된 규칙을 따르는 웹 API로, 오늘날 웹 서비스 API의 사실상 표준입니다. 이를 도서관 사서에게 책을 요청하는 과정에 비유하여 핵심 원리를 이해할 수 있습니다.
> 
> **1. 자원 (Resource): '무엇을' 다룰까?**
> RESTful API에서 다루는 모든 데이터나 객체, 서비스를 '자원'이라고 부릅니다. 이는 서버가 관리하는 대상으로, 항상 '명사'로 표현됩니다 (예: `users`, `posts`). 도서관에서 관리하는 '책'이나 '사용자 명부'와 같습니다.
> 
> **2. URI (Uniform Resource Identifier): '어디에' 있는가?**
> 모든 자원은 인터넷상의 고유한 주소인 URI를 가집니다. 클라이언트는 이 URI를 통해 원하는 자원을 명확하게 가리킵니다. 좋은 RESTful API는 URI에 행위(동사)를 포함하지 않고, 자원을 나타내는 '명사 복수형'을 사용하는 것이 모범 사례입니다 (예: `/books/123`은 특정 책 ID를, `/books?category=novel`은 특정 카테고리의 책을 의미).
> 
> **3. HTTP 메서드 (행위): '무엇을 할 것인가?'**
> 동일한 자원(URI)에 대해 수행할 작업(행위)은 HTTP 메서드라는 동사로 정의됩니다. 이는 도서관 사서에게 '책 목록을 보여달라' 또는 '새 책을 등록해달라'와 같이 어떤 요청을 할지 명확히 말하는 것과 같습니다.
> *   **GET**: 자원의 정보를 **조회**합니다 (예: "책 목록 보여주세요").
> *   **POST**: 서버에 새로운 자원을 **생성**합니다 (예: "새 책을 등록해주세요").
> *   **PUT**: 자원의 **전체 정보**를 대체하여 수정합니다 (예: "책 ID 123번 정보를 수정해주세요").
> *   **DELETE**: 특정 자원을 **삭제**합니다 (예: "책 ID 123번을 삭제해주세요").
> *   **PATCH**: 자원의 **일부 정보**만 수정합니다.
> 
> **4. 무상태성 (Stateless): 추가 핵심 원칙**
> RESTful API의 중요한 원칙 중 하나는 '무상태성'입니다. 서버는 클라이언트의 상태(이전에 로그인했는지 여부 등)를 저장하지 않습니다. 각 요청은 독립적이므로, 매 요청마다 인증 토큰 등 필요한 모든 정보를 함께 보내야 합니다. 이는 요청마다 '신분증'을 제시하는 것과 같으며, 서버의 부담을 줄이고 확장성을 높이는 장점이 있습니다.
> 
> **작동 흐름 및 응답**
> RESTful API는 HTTP 프로토콜을 기반으로 합니다. 클라이언트가 URI로 자원을 명확하게 정의하고 HTTP 메서드를 통해 해당 자원에 대한 행위를 요청하면, 서버는 적절한 HTTP 상태 코드를 반환하여 결과를 알려줍니다. 주요 성공 응답 코드로는 `200 OK`(요청 성공), `201 Created`(자원 생성 성공), `204 No Content`(성공했지만 반환할 내용 없음) 등이 있습니다.


## 도서관 사서에게 요청하듯이, RESTful API의 핵심 원리 이해하기

RESTful API(Representational State Transfer API)는 웹에서 **자원(데이터)**을 효율적이고 일관된 방식으로 주고받기 위한 **약속된 규칙**을 따르는 웹 API입니다. 오늘날 웹 서비스 API의 사실상 표준으로 사용되고 있습니다.

RESTful API의 작동 방식을 이해하기 가장 쉬운 방법은 **도서관 사서에게 책을 요청하는 과정**에 빗대어 보는 것입니다.

여러분이 도서관에서 원하는 자료를 얻기 위해 사서에게 요청을 보내는 것처럼, 클라이언트(사용자)는 RESTful API를 통해 서버(사서)에 데이터를 요청하고 응답받습니다.

---

## 1단계: '무엇을' 다룰까? — 자원 (Resource)

RESTful API에서 다루는 모든 데이터나 객체, 서비스를 **자원**이라고 부릅니다. 자원은 서버가 관리하는 대상이며, 항상 **명사**로 표현됩니다 (예: `users`, `posts`).

|기술 용어|도서관 비유|설명|
|:--|:--|:--|
|**자원 (Resource)**|**책, 사용자 명부, 댓글**|서버가 관리하는 데이터 덩어리입니다. 도서관에서 관리하는 모든 실물 자료나 목록이라고 생각할 수 있습니다.|

## 2단계: '어디에' 있는가? — URI (Uniform Resource Identifier)

모든 자원은 인터넷상의 고유한 **주소** 또는 **이름**인 URI를 가집니다. 이 URI를 통해 클라이언트는 정확히 어떤 자원을 원하는지 명확하게 가리킵니다.

|기술 용어|도서관 비유|예시 (API)|예시 (도서관 요청)|
|:--|:--|:--|:--|
|**URI**|**책의 분류 번호 또는 ID**|`/books/123`|"책 ID **123번**을 보여주세요"|
|**URI**|**특정 카테고리 선반**|`/books?category=novel`|"소설 카테고리의 책을 보여주세요"|

좋은 RESTful API는 URI에 행위(동사)를 포함하지 않고, 자원을 나타내는 **명사 복수형**을 사용하는 것이 모범 사례입니다 (예: `/users`는 좋지만, `/getUsers`는 나쁜 예입니다).

## 3단계: '무엇을 할 것인가?' — HTTP 메서드 (행위)

동일한 자원(URI)이라도, 그 자원에 대해 수행할 **작업(행위)**은 **HTTP 메서드**라는 동사로 정의됩니다.

도서관 사서에게 어떤 요청을 할지 명확히 말하는 것과 같습니다.

|HTTP 메서드|주된 목적 (CRUD)|도서관 요청|기술적 의미|
|:--|:--|:--|:--|
|**GET**|조회 (Read)|"책 목록 보여주세요"|자원의 정보를 **가져오거나 조회**합니다.|
|**POST**|생성 (Create)|"새 책을 등록해주세요"|서버에 새로운 자원을 **생성**합니다.|
|**PUT**|전체 수정 (Update)|"책 ID 123번 정보를 수정해주세요"|자원의 **전체 정보**를 대체하여 수정합니다.|
|**DELETE**|삭제 (Delete)|"책 ID 123번을 삭제해주세요"|특정 자원을 **삭제**합니다.|
|**PATCH**|부분 수정 (Update)|(부분 수정)|자원의 **일부 정보**만 수정합니다.|

이처럼 RESTful API는 **URI(명사)와 HTTP 메서드(동사)**를 명확하게 구분하여 요청합니다. 이를 통해 **명확하고 일관된 방식**으로 서버에 요청을 보낼 수 있습니다.

---

## 4단계: 추가 핵심 원칙 — 무상태성 (Stateless)

RESTful API의 중요한 원칙 중 하나는 **무상태성**입니다.

|기술 용어|도서관 비유|설명|
|:--|:--|:--|
|**무상태성 (Stateless)**|**요청마다 신분증 제시**|서버는 클라이언트의 상태(이전에 로그인했는지 여부 등)를 저장하지 않습니다. 각 요청은 독립적이므로, 매 요청마다 인증 토큰 등 필요한 모든 정보를 함께 보내야 합니다.|

이는 서버의 부담을 줄이고, 어떤 서버로 요청이 가도 동일하게 처리할 수 있어 확장성(Scalability)을 높이는 장점이 있습니다.

---

## 요약: RESTful API의 작동 흐름

RESTful API는 HTTP 프로토콜을 기반으로 합니다. 클라이언트가 URI로 자원을 명확하게 정의하고, HTTP 메서드를 통해 해당 자원에 대한 행위를 요청하면, 서버는 적절한 HTTP 상태 코드를 반환하여 결과를 알려줍니다.

|주요 성공 응답 (HTTP 상태 코드)|의미|
|:--|:--|
|`200 OK`|요청 성공|
|`201 Created`|자원 생성 성공|
|`204 No Content`|성공했지만 반환할 내용 없음 (주로 `DELETE` 요청 시)|