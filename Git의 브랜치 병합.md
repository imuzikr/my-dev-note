---
date: 2026-05-09
tags:
  - Git
  - GitHub
  - 브랜치
  - vba/병합
  - merge
  - rebase
  - squash
contexts:
  - Knowledge
index:
  - Vibe Coding
summary: Git 브랜치 병합이란 서로 다른 브랜치의 작업 내용을 하나의 흐름으로 통합하는 과정이며, Merge, Rebase, Squash는 히스토리 보존 방식과 커밋 정리 방식에 따라 구분되는 대표적인 병합 방법이다.
author:
people:
YouTube_url:
scr_url:
---

## 1. Merge (병합)

가장 기본적인 방법이다. 두 브랜치의 작업 내용을 합치면서 **"병합했다"는 기록이 남는** 커밋이 하나 생긴다.

```
main:     A --- B --------- M (병합 커밋)
                \           /
feature:         C --- D ---
```

feature 브랜치에서 작업한 C, D가 그대로 보존되고, M이라는 병합 커밋이 새로 만들어진다. 히스토리가 있는 그대로 남기 때문에 “언제 어디서 갈라져서 언제 합쳤는지” 전체 흐름이 보인다.

---

## 2. Rebase (리베이스)

feature 브랜치의 커밋들을 **main 브랜치 끝에 옮겨 붙이는** 방식이다.

```
변경 전:
main:     A --- B
                \
feature:         C --- D

변경 후:
main:     A --- B --- C' --- D'
```

마치 처음부터 main에서 순서대로 작업한 것처럼 **히스토리가 한 줄로 깔끔**해진다. 대신 "갈라져서 합쳤다"는 기록은 사라진진다.

---

## 3. Squash (스쿼시)

feature 브랜치의 여러 커밋을 **하나로 합쳐서** main에 붙이는 방식이다.

```
변경 전:
main:     A --- B
                \
feature:         C --- D --- E

변경 후:
main:     A --- B --- F (C+D+E를 하나로 합친 커밋)
```

feature에서 시행착오를 거치며 커밋을 여러 번 했더라도, main에는 **깔끔하게 하나의 커밋**으로만 남는다.

---

## 언제 뭘 쓰나요?

**Merge**는 히스토리를 있는 그대로 보존하고 싶을 때, **Rebase**는 히스토리를 한 줄로 깔끔하게 정리하고 싶을 때, **Squash**는 자잘한 커밋들을 하나로 묶어서 간결하게 만들고 싶을 때 사용한다.

팀마다 선호하는 방식이 다르고, 정해진 정답은 없다. 다만 혼자 작업하는 브랜치에서는 Rebase나 Squash로 정리하고, 여러 명이 공유하는 브랜치에서는 Merge를 쓰는 경우가 일반적이다.
