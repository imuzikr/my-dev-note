---
date: 2025-11-29
tags:
  - vite
  - React
  - 번들러
  - next_js
  - 빌드도구
  - vibe_coding
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---


# 📘 웹 개발 초보자를 위한 React 생태계 완벽 해설서

> [!INFO] 문서 개요
> 이 문서는 React를 사용하여 웹 애플리케이션을 만들 때 마주치는 핵심 도구들(Next.js, Vite, Babel, Vercel 등)의 개념과 관계를 비유를 통해 쉽게 설명합니다.

---

## 1. React와 Next.js: 무엇을 선택할까?

가장 먼저 결정해야 할 것은 "무엇으로 만들 것인가"입니다. 이 둘의 관계는 **자동차 엔진**과 **완성차**의 관계와 같습니다.

### 1.1 핵심 비유
| 구분 | React (Library) | Next.js (Framework) |
| :--- | :--- | :--- |
| **비유** | ⚙️ **자동차 엔진** | 🚘 **완성된 자동차** |
| **특징** | 엔진만 따로 팝니다. 바퀴, 핸들(라우팅 등)은 직접 조립해야 합니다. | 엔진에 바퀴, 핸들, 네비게이션까지 풀옵션으로 장착되어 있습니다. |
| **자유도** | 높음 (내 마음대로 튜닝 가능) | 낮음 (정해진 규칙을 따라야 함) |

### 1.2 선택 가이드

> [!QUESTION] 어떤 도구를 써야 할까요?
> **"검색 엔진(네이버, 구글)에 노출되어야 합니까?"** 가 가장 중요한 기준입니다.

* **React (Vite 사용)** 🟢
    * **추천:** 관리자 페이지, 내부 도구, **게임(지뢰찾기)**, **자리 배치 앱**, 로그인해야 쓰는 앱.
    * **이유:** 검색 노출이 필요 없고, 화면 전환이 부드러우며 가볍습니다.
* **Next.js** 🔵
    * **추천:** 블로그, 뉴스, 쇼핑몰, **입시 홍보 사이트**, **역사 탐방 가이드**.
    * **이유:** 검색 로봇이 내용을 잘 읽어갑니다(SEO 유리).



## 2. Vite (비트): 로켓 배송급 작업 도구

Next.js를 쓰지 않고 **React**만 쓰기로 했다면, **Vite**는 선택이 아닌 **필수**입니다.

### 2.1 Vite가 필요한 이유
브라우저(크롬 등)는 생각보다 똑똑하지 않습니다. 우리가 작성하는 최신 React 코드(JSX, TypeScript)를 이해하지 못합니다.
* **Vite의 역할:** 우리가 짠 코드를 브라우저가 이해할 수 있는 언어(표준 JS)로 실시간으로 **번역**하고 **가공**해서 화면에 보여줍니다.

### 2.2 Vite vs Babel vs Webpack
이들은 모두 개발을 도와주는 도구들이지만 역할이 다릅니다. '출판사'에 비유해 봅시다.

* **Babel (전문 번역가):** 최신 언어를 옛날 언어로 한 줄 한 줄 번역만 합니다. (책을 제본하거나 배달하진 않음)
* **Webpack (옛날 배달 트럭):** 짐(코드)을 싣고 배달하는데, 짐이 하나만 바뀌어도 전체를 다시 포장해서 출발합니다. (느림)
* **Vite (최신 로켓 배송):**
    * **번역:** Babel 대신 더 빠른 번역가(esbuild)를 씁니다.
    * **배달:** 전체 포장을 다시 하지 않고, **바뀐 물건 하나만 쏙** 배달합니다. (엄청나게 빠름)

> [!TIP] 결론
> 복잡하게 생각할 필요 없이, React 개발을 할 때는 **무조건 Vite**를 쓴다고 생각하면 됩니다.



## 3. 번들링(Bundling)과 배포

코드를 다 짰으면 사용자에게 보여줘야 합니다. 이 과정을 이사(Moving)에 비유해 보겠습니다.

### 3.1 번들링이란? (이사짐 포장)
* **개발 중 (Dev):** "집 구경만 시켜줄게."
    * 짐을 싸지 않습니다. 그냥 있는 그대로 보여줍니다. (Vite의 No-Bundle 방식)
* **배포 시 (Build):** "이제 진짜 이사 간다."
    * 수백 개의 파일(짐)을 낱개로 들고 가면 너무 힘듭니다.
    * `npm run build` 명령어를 치면, Vite가 **Rollup**이라는 포장 전문가를 불러 짐을 몇 개의 큰 박스로 **압축(Bundling)**합니다.

### 3.2 배포 서비스: Vercel / Netlify (식당 홀)
* **Vite (주방):** 요리를 만드는 곳. (내 컴퓨터)
* **Vercel (식당 홀):** 손님(사용자)에게 요리를 내놓는 곳. (인터넷 서버)
* **과정:** Vite로 만든 압축 파일(`dist` 폴더)을 Vercel에 올리면, 전 세계 사람들이 접속할 수 있는 URL(`https://...`)이 생성됩니다.



## 4. 실전: 프로젝트 시작과 작동 원리

마법은 `npm`을 통해 일어납니다.

### 4.1 사무실 개업 시뮬레이션

1.  **계약 (`npm create vite`)**
    * 빈 폴더에 "Vite 사무실 설계도(`package.json`)"를 받아옵니다.
2.  **인테리어 공사 (`npm install`)** ⭐️ **중요**
    * 설계도를 보고 필요한 자재와 일꾼(React, Vite 프로그램 등)을 인터넷에서 다운로드합니다.
    * 이때 **`node_modules`**라는 거대한 창고 방이 생기고, 여기에 Vite가 실제로 들어옵니다.
3.  **영업 시작 (`npm run dev`)**
    * 창고에 있는 Vite 반장님을 깨워서 일을 시킵니다.
    * 이때부터 브라우저에서 화면이 보이기 시작합니다.


```
# 단계 1: "Vite 세트 주세요" (주문)

빈 폴더에서 다음 명령어를 칩니다.

> `npm create vite@latest`

이 명령어를 치면, `npm` 기사님이 인터넷 저장소에서 **Vite 프로젝트의 설계도(뼈대)**를 선생님 폴더로 다운로드해 줍니다. 이때 `index.html`, `package.json` 같은 파일들이 생깁니다.

#### 단계 2: "부품 배달" (설치) - **여기가 핵심입니다!**

설계도만 있다고 작동하지 않죠? 다음 명령어를 칩니다.

> `npm install`

이 순간, 선생님 폴더 안에 **`node_modules`**라는 거대한 폴더가 새로 생깁니다.

- 이 안에 **Vite의 실제 실행 프로그램**, React, 그리고 수많은 도구가 인터넷에서 다운로드되어 저장됩니다.
    
- 즉, **Vite는 이 `node_modules` 폴더 안에 물리적으로 들어와 있습니다.**
    

#### 단계 3: "작동 시작" (실행)

이제 도구가 다 왔으니 실행합니다.

> `npm run dev`

이 명령어를 치면, 컴퓨터는 `node_modules` 폴더 안에 숨어 있는 Vite 실행 파일을 찾아서 작동시킵니다. 
그때부터 브라우저 화면이 열리는 것입니다.

---

### 2. 비유: 텅 빈 사무실 개업하기

이 과정을 **사무실(폴더) 개업**에 비유하면 이해가 아주 쉽습니다.

1. **빈 폴더 생성:** 임대료를 내고 **텅 빈 사무실**을 하나 구했습니다. 당연히 아무 일도 안 일어납니다.
    
2. **`package.json` 생성:** "우리는 인테리어 업체를 부를 거고(Vite), 책상은 5개 필요해(React)"라고 적힌 **작업 지시서**를 책상 위에 올려둡니다.
    
3. **`npm install` (중요):** 인력소에 전화를 겁니다. 그러면 **실제 일꾼들(Vite, React 등)**이 와서 사무실 뒤쪽 창고방(`node_modules`)에 대기합니다.
    
4. **`npm run dev`:** 사장님(선생님)이 "일 시작해!"라고 소리칩니다. 그러면 창고방에 있던 **Vite라는 반장님**이 나와서 일을 지휘하기 시작합니다.
    
    
---
  

#### "주문서 없이 장을 보러 가면?"

빈 폴더에서 다짜고짜 `npm install`을 치는 것은, **메뉴도 안정하고 빈손으로 마트에 가서 "재료 주세요!"라고 외치는 것**과 같습니다.

마트 직원(`npm`)은 이렇게 대답할 겁니다.

"손님, 도대체 뭘 드실 건가요? 주문서(`package.json`)가 없어서 드릴 게 없네요." -> **결과:** 아무것도 설치되지 않습니다.



### 2. 기술적 이유: `package.json`의 부재

`npm create vite` 명령어가 중요한 이유는, 이 명령어가 **`package.json`**이라는 파일을 만들어주기 때문입니다.

이 파일에는 다음과 같은 내용이 적혀 있습니다.

**[package.json 내용]** "이 프로젝트는 React 18버전과 Vite 5버전이 꼭 필요해. 그리고 TypeScript도 필요해."

`npm install` 명령어는 **오직 이 파일만 읽고 작동**합니다. 이 파일이 없으면 `npm install`은 작동하지 않습니다.

---


### 3. 요약

그래서 우리는 항상 프로젝트를 시작할 때:

1. **`npm create vite`**로 Vite를 쓰겠다는 **선언**을 하고,
    
2. **`npm install`**로 Vite라는 프로그램을 폴더(node_modules) 안에 **다운로드** 받은 뒤,
    
3. **`npm run dev`**로 다운로드된 Vite를 **실행**하는 것입니다.
```


### 4.2 파일 구조의 의미 (React+Vite)

> [!example] 파일 탐색기 구조
> * 📁 **node_modules**: (창고) 실제 도구들이 들어있는 곳. 건드리지 않음.
> * 📄 **package.json**: (주문서) 이 프로젝트에 무엇이 필요한지 적혀있는 명세서.
> * 📄 **index.html**: (대문) 브라우저가 가장 먼저 들어오는 입구. (Next.js에는 없음)
> * 📄 **src/App.tsx**: (메인 홀) 실제 화면을 그리는 React 코드가 있는 곳.

---

## 5. 요약: 한눈에 보기

| 상황               | 필요한 도구          | 비유           |
| :--------------- | :-------------- | :----------- |
| **앱을 만들고 싶다**    | **React**       | 밀가루 반죽 (재료)  |
| **쉽게 굽고 싶다**     | **Vite**        | 최신형 오븐 (도구)  |
| **검색이 잘 돼야 한다**  | **Next.js**     | 완성된 빵집 프랜차이즈 |
| **도구를 가져오고 싶다**  | **npm install** | 쿠팡 배송 기사님    |
| **남들에게 보여주고 싶다** | **Vercel**      | 배달의 민족 (플랫폼) |


> [!SUCCESS] 한 줄 요약
> **교육용 도구, 시뮬레이션, 게임** 등은 **[React + Vite]** 조합이 가장 배우기 쉽고, 가볍고, 빠릅니다!
>

