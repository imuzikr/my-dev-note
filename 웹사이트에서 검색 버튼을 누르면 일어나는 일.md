---
date: 2026-01-29
tags:
  - vibe_coding
  - API서버
  - CORS
  - 요청
  - 라우터
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---

## 🎯 이 글을 읽고 나면 알게 되는 것

- 웹사이트에서 버튼을 클릭하면 뒤에서 어떤 일이 일어나는지
- 프론트엔드와 백엔드가 어떻게 협력하는지
- API가 정확히 무엇이고 어떻게 작동하는지

---

## 📚 시작하기 전에: 기본 용어 설명

### 프론트엔드 (Front-end)

**"사용자가 보고 만지는 부분"**

예시:

- 검색창에 글자를 입력하는 화면
- 버튼을 클릭하는 것
- 예쁘게 꾸며진 웹사이트 화면
- 사진, 글씨, 색깔 등 눈에 보이는 모든 것

### 백엔드 (Back-end)

**"사용자 눈에 보이지 않지만 실제로 일을 처리하는 부분"**

예시:

- 검색어를 받아서 데이터베이스에서 찾는 일
- 사용자가 입력한 정보를 저장하는 일
- 비밀번호가 맞는지 확인하는 일
- 복잡한 계산을 하는 일

### API (Application Programming Interface)

**"프론트엔드와 백엔드가 대화하는 통로"**

비유: 식당에서 손님(프론트엔드)이 종업원(API)에게 주문하면, 종업원이 주방(백엔드)에 전달하는 것과 같습니다.

### URL (Uniform Resource Locator)

**"인터넷 주소"**

예시:

- `https://www.naver.com` - 네이버 홈페이지 주소
- `https://myapp.com/api/books` - 책 정보를 가져오는 API 주소

---

## 🍕 쉬운 비유로 이해하기

### 피자 주문으로 이해하는 웹사이트 작동 원리

```
┌─────────────────────────────────────────┐
│  손님 (사용자)                          │
│  "페퍼로니 피자 주세요"                 │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│  홀 (프론트엔드)                        │
│  - 메뉴판 보여주기                      │
│  - 주문서 작성                          │
│  - 종업원 부르기                        │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│  종업원 (API)                           │
│  "페퍼로니 피자 한 판 주세요!"          │
│  주문을 주방에 전달                     │
└─────────────────┬───────────────────────┘
                  │
                  ↓
┌─────────────────────────────────────────┐
│  주방 (백엔드)                          │
│  - 재료 확인 (데이터베이스 조회)       │
│  - 피자 만들기 (데이터 처리)           │
│  - 완성된 피자 내보내기 (응답)         │
└─────────────────┬───────────────────────┘
                  │
                  ↓
        손님에게 피자 도착!
```

---

## 🔍 실제 예시: 책 검색 기능

### 상황 설정

도서관 웹사이트에서 "해리포터"를 검색하는 경우를 단계별로 살펴보겠습니다.

---

## 📖 전체 과정 - 10단계

### 1단계: 사용자가 행동한다

**사용자가 하는 일:**

```
1. 검색창에 "해리포터" 입력
2. [검색] 버튼 클릭 👆
```

**화면에서 보이는 것:**

```
┌──────────────────────────────────┐
│  도서 검색                       │
│                                  │
│  [해리포터        ] [🔍 검색]   │
│                                  │
└──────────────────────────────────┘
```

---

### 2단계: 프론트엔드 코드가 반응한다

**무슨 일이 일어나나요?**

버튼을 클릭하면 미리 작성된 프로그램(이벤트 핸들러)이 자동으로 실행됩니다.

**코드 예시 (초보자용 설명 포함):**

```javascript
// 사용자가 검색 버튼을 클릭하면 실행되는 함수
function handleSearch() {
  // 1. 사용자가 입력한 검색어 가져오기
  const searchWord = "해리포터";
  
  // 2. 로딩 화면 보여주기
  showLoadingSpinner(); // "검색 중입니다..." 표시
  
  // 3. 백엔드에 도움 요청하기 (다음 단계로 이어짐)
  askBackendForBooks(searchWord);
}
```

**비유:**

- 손님: "페퍼로니 피자 주세요" (버튼 클릭)
- 종업원: "알겠습니다!" (이벤트 핸들러 실행)

---

### 3단계: fetch()로 백엔드에 요청 보내기

**프론트엔드가 백엔드에게 부탁합니다:**

```javascript
// 백엔드에게 "해리포터 검색해줘!" 라고 요청
fetch('/api/books/search?query=해리포터')
```

**이것은 무슨 뜻인가요?**

- `fetch()` = "백엔드야, 이 일 좀 해줘!" 하고 부탁하는 명령어
- `/api/books/search` = 백엔드의 "책 검색 담당 부서" 주소
- `?query=해리포터` = 검색어를 함께 전달

**전체 주소:**

```
https://library.com/api/books/search?query=해리포터
│                 │                  │
│                 │                  └── 검색어
│                 └─────────────────── 백엔드 주소
└───────────────────────────────────── 도메인
```

**비유:**

- 종업원이 주방에 가서: "페퍼로니 피자 한 판 만들어주세요!" 하고 주문서를 전달하는 것

---

### 4단계: 인터넷을 통해 요청이 서버로 전송

**무슨 일이 일어나나요?**

컴퓨터가 인터넷을 통해 서버(백엔드 컴퓨터)에 메시지를 보냅니다.

```
사용자 컴퓨터              인터넷              서버 컴퓨터
    [💻]    ─────────→    [🌐]    ─────────→    [🖥️]
    
"해리포터 검색해줘!" 라는 요청이 날아감
```

**보이지 않지만 실제로 전송되는 내용:**

```
GET /api/books/search?query=해리포터 HTTP/1.1
Host: library.com
User-Agent: Chrome Browser
Accept: application/json

(이런 정보들이 전송됨)
```

**비유:**

- 종업원이 주방으로 걸어가는 것 (인터넷 = 복도)

---

### 5단계: 서버의 라우터가 담당 부서 찾기

**라우터가 하는 일:**

서버에 요청이 도착하면, "라우터"라는 교통정리 시스템이 "어느 부서가 이 일을 처리해야 하지?"라고 판단합니다.

```
라우터: "음... /api/books/search 요청이 왔네?"
라우터: "이건 책 검색 부서가 담당이야!"
라우터: "app/api/books/search/route.ts 파일을 실행해!"
```

**파일 구조:**

```
app/
  ├── api/
  │   ├── books/
  │   │   └── search/
  │   │       └── route.ts  ← 이 파일을 찾아냄!
  │   └── users/
  │       └── route.ts
```

**비유:**

- 주방장: "페퍼로니 피자? 그럼 피자 담당 요리사를 불러!"

---

### 6단계: route.ts 파일의 코드가 실행됨

**이제 실제로 일을 처리합니다!**

`route.ts` 파일 안에는 "이 요청이 오면 이렇게 처리해라"라는 지시서가 적혀있습니다.

**코드 예시 (상세 설명 포함):**

```javascript
// app/api/books/search/route.ts

// GET 요청(데이터 가져오기)이 오면 이 함수 실행
export async function GET(request) {
  console.log('✅ 검색 요청 받음!');
  
  // 1단계: 검색어 추출
  // URL에서 "query=해리포터" 부분을 찾아냄
  const url = new URL(request.url);
  const searchWord = url.searchParams.get('query');
  // searchWord = "해리포터"
  
  console.log('🔍 검색어:', searchWord);
  
  // 2단계: 데이터베이스에서 검색
  // 데이터베이스 = 모든 책 정보가 저장된 거대한 창고
  console.log('📚 데이터베이스 검색 중...');
  
  const books = await database.collection('books')
    .where('title', 'contains', searchWord)
    .get();
  
  console.log('✨ 찾은 책:', books.length, '권');
  
  // 3단계: 결과 정리
  // 필요한 정보만 골라서 예쁘게 정리
  const results = books.map(book => {
    return {
      id: book.id,
      title: book.title,
      author: book.author,
      year: book.publishedYear
    };
  });
  
  // 4단계: 결과 돌려보내기
  console.log('📦 결과 전송!');
  return Response.json({
    success: true,
    count: results.length,
    books: results
  });
}
```

**단계별로 무슨 일이 일어나는지:**

```
1. 검색어 확인: "해리포터"를 찾으라는구나!
   ↓
2. 데이터베이스 접속: 거대한 책 창고에 들어감
   ↓
3. 검색 실행: "해리포터"가 제목에 포함된 책 찾기
   ↓
4. 결과 발견: 3권의 책을 찾음
   ↓
5. 정보 정리: 필요한 정보만 골라냄
   ↓
6. 포장: JSON 형태로 예쁘게 포장
```

**비유:**

- 주방에서 실제로 피자를 만드는 과정
    1. 재료 확인 (도우, 치즈, 페퍼로니)
    2. 도우 펴기
    3. 토핑 올리기
    4. 오븐에 굽기
    5. 접시에 담기

---

### 7단계: 처리 결과 데이터 생성

**무엇이 만들어지나요?**

검색 결과가 "JSON"이라는 형식으로 만들어집니다.

**JSON이란?**

- 컴퓨터들이 서로 데이터를 주고받을 때 사용하는 표준 형식
- 사람도 읽을 수 있게 만들어진 텍스트 형태

**실제 생성된 데이터:**

```json
{
  "success": true,
  "count": 3,
  "books": [
    {
      "id": "1",
      "title": "해리포터와 마법사의 돌",
      "author": "J.K. 롤링",
      "year": 1997
    },
    {
      "id": "2",
      "title": "해리포터와 비밀의 방",
      "author": "J.K. 롤링",
      "year": 1998
    },
    {
      "id": "3",
      "title": "해리포터와 아즈카반의 죄수",
      "author": "J.K. 롤링",
      "year": 1999
    }
  ]
}
```

**데이터 설명:**

- `success: true` = 검색이 성공했어요!
- `count: 3` = 총 3권의 책을 찾았어요!
- `books: [...]` = 찾은 책들의 목록이에요!

**비유:**

- 완성된 피자를 접시에 담아서 준비 완료!

---

### 8단계: HTTP 응답 생성 및 전송

**서버가 결과를 포장해서 보냅니다:**

```javascript
return Response.json({
  success: true,
  count: 3,
  books: [...]
});
```

**이것은 무슨 뜻인가요?**

`return` = "이 데이터를 돌려보내!" `Response.json()` = "JSON 형태로 포장해서!"

**실제로 전송되는 HTTP 응답:**

```
HTTP/1.1 200 OK
Content-Type: application/json
Content-Length: 245

{
  "success": true,
  "count": 3,
  "books": [...]
}
```

**숫자의 의미:**

- `200` = 성공! (OK)
- `404` = 못 찾았어요 (Not Found)
- `500` = 서버 오류 (Server Error)

**비유:**

- 종업원이 완성된 피자를 들고 손님 테이블로 가는 것

---

### 9단계: 인터넷을 통해 브라우저로 전송

**데이터가 다시 사용자 컴퓨터로 돌아옵니다:**

```
서버 컴퓨터              인터넷              사용자 컴퓨터
    [🖥️]    ─────────→    [🌐]    ─────────→    [💻]
    
"3권의 책을 찾았어요!" 라는 데이터가 날아옴
```

**전송 시간:**

- 빠르면: 0.1초 (100ms)
- 보통: 0.5초 (500ms)
- 느리면: 2초 이상

**브라우저 개발자 도구에서 확인할 수 있는 정보:**

```
Request URL: /api/books/search?query=해리포터
Request Method: GET
Status Code: 200 OK
Response Time: 234ms
```

**비유:**

- 종업원이 피자를 들고 손님 테이블에 도착!

---

### 10단계: 브라우저가 화면 업데이트

**마지막 단계입니다!**

프론트엔드 코드가 받은 데이터로 화면을 업데이트합니다.

**코드 예시:**

```javascript
// 3단계에서 시작한 fetch()가 드디어 끝남
fetch('/api/books/search?query=해리포터')
  .then(response => response.json())  // 받은 데이터를 JSON으로 변환
  .then(data => {
    // 여기서 data = 서버가 보낸 검색 결과
    
    console.log('✅ 데이터 받음!');
    console.log('찾은 책:', data.count, '권');
    
    // 화면 업데이트
    showSearchResults(data.books);
    
    // 로딩 화면 숨기기
    hideLoadingSpinner();
  });

// 화면에 책 목록 표시하는 함수
function showSearchResults(books) {
  books.forEach(book => {
    // 각 책을 화면에 카드 형태로 표시
    displayBookCard(book);
  });
}
```

**사용자가 보는 최종 화면:**

```
┌──────────────────────────────────────────┐
│  도서 검색                               │
│                                          │
│  [해리포터        ] [🔍 검색]           │
│                                          │
│  검색 결과: 3권                          │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ 📕 해리포터와 마법사의 돌          │ │
│  │ 저자: J.K. 롤링                    │ │
│  │ 출판: 1997년                       │ │
│  │ [상세보기]                         │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ 📗 해리포터와 비밀의 방            │ │
│  │ 저자: J.K. 롤링                    │ │
│  │ 출판: 1998년                       │ │
│  │ [상세보기]                         │ │
│  └────────────────────────────────────┘ │
│                                          │
│  ┌────────────────────────────────────┐ │
│  │ 📘 해리포터와 아즈카반의 죄수      │ │
│  │ 저자: J.K. 롤링                    │ │
│  │ 출판: 1999년                       │ │
│  │ [상세보기]                         │ │
│  └────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

**비유:**

- 손님이 맛있게 피자를 먹는 것!

---

## 🎬 전체 과정 요약

### 한 눈에 보는 흐름도

```
👤 사용자
  │
  │ 1. 검색 버튼 클릭
  ↓
💻 프론트엔드 (브라우저)
  │
  │ 2. 이벤트 핸들러 실행
  │ 3. fetch() 호출
  ↓
🌐 인터넷
  │
  │ 4. HTTP 요청 전송
  ↓
🖥️ 백엔드 서버
  │
  │ 5. 라우터가 route.ts 찾기
  │ 6. route.ts 코드 실행
  │    ├─ 검색어 추출
  │    ├─ 데이터베이스 검색
  │    └─ 결과 정리
  │ 7. 결과 데이터 생성 (JSON)
  │ 8. HTTP 응답 생성
  ↓
🌐 인터넷
  │
  │ 9. HTTP 응답 전송
  ↓
💻 프론트엔드 (브라우저)
  │
  │ 10. 화면 업데이트
  ↓
👤 사용자
  │
  │ 검색 결과 확인! ✨
```

---

## 🔑 핵심 개념 정리

### 1. 자동으로 전달되지 않습니다!

**❌ 잘못된 생각:**

```
버튼 클릭 → (자동) → 백엔드 실행
```

**✅ 실제 과정:**

```
버튼 클릭 → 이벤트 핸들러 → fetch() 호출 → 백엔드 실행
```

**왜 이렇게 복잡하게 만들었나요?**

개발자가 정확히 제어할 수 있어야 하기 때문입니다!

**예시:**

```javascript
function handleSearch() {
  // 검색어가 너무 짧으면 검색 안 함
  if (searchWord.length < 2) {
    alert('2글자 이상 입력하세요!');
    return; // 백엔드 호출 안 함
  }
  
  // 검색어가 충분히 길면 백엔드 호출
  fetch('/api/books/search?query=' + searchWord);
}
```

### 2. 라우터의 역할

**라우터는 "교통정리"만 합니다:**

```
요청: "GET /api/books/search"
  ↓
라우터: "이 주소는 app/api/books/search/route.ts야!"
  ↓
해당 파일 실행
```

**라우터가 하지 않는 것:**

- ❌ 버튼 클릭 감지
- ❌ 자동으로 API 호출
- ❌ 데이터 처리

**라우터가 하는 것:**

- ✅ URL과 파일 매칭
- ✅ 올바른 파일 찾기
- ✅ 해당 파일 실행

### 3. API URL에서 보이는 것

**브라우저에서 `/api/books` 접속 시:**

```json
{
  "books": [
    { "title": "해리포터" },
    { "title": "반지의 제왕" }
  ]
}
```

**이것은:**

- ✅ route.ts가 실행된 **결과**
- ✅ 데이터베이스에서 가져온 **데이터**
- ❌ route.ts **코드** (절대 안 보임!)

**비유:**

```
주방(route.ts) = 손님은 못 봄
완성된 요리(JSON) = 손님이 받는 것
```

---

## 🎨 다른 예시들

### 예시 1: 회원가입

```
1. 사용자: 회원가입 양식 작성 후 [가입] 버튼 클릭
   ↓
2. 프론트엔드: handleSignup() 함수 실행
   ↓
3. fetch('/api/users', {
     method: 'POST',
     body: {
       email: 'user@example.com',
       password: '********'
     }
   })
   ↓
4. 백엔드: app/api/users/route.ts의 POST 함수 실행
   - 이메일 중복 확인
   - 비밀번호 암호화
   - 데이터베이스에 저장
   ↓
5. 응답: { "success": true, "userId": "abc123" }
   ↓
6. 프론트엔드: "가입 완료!" 메시지 표시
```

### 예시 2: 좋아요 버튼

```
1. 사용자: ❤️ 버튼 클릭
   ↓
2. 프론트엔드: handleLike() 함수 실행
   ↓
3. fetch('/api/posts/123/like', { method: 'POST' })
   ↓
4. 백엔드: app/api/posts/[id]/like/route.ts 실행
   - 이미 좋아요 했는지 확인
   - 좋아요 카운트 +1
   - 데이터베이스 업데이트
   ↓
5. 응답: { "likes": 42 }
   ↓
6. 프론트엔드: 하트 빨간색으로 변경, "42" 표시
```

### 예시 3: 실시간 검색

```
1. 사용자: 검색창에 "ㅎ" 입력
   ↓
2. 프론트엔드: onChange 이벤트 발생
   ↓
3. 0.3초 대기 (너무 자주 검색하지 않도록)
   ↓
4. 사용자: "해" 까지 입력
   ↓
5. fetch('/api/search?query=해')
   ↓
6. 백엔드: 검색 실행
   ↓
7. 응답: ["해리포터", "해변의 카프카", ...]
   ↓
8. 프론트엔드: 드롭다운으로 추천 검색어 표시
```

---

## ❓ 자주 묻는 질문 (FAQ)

### Q1: 왜 이렇게 복잡하게 만들었나요?

**A:** 보안과 성능 때문입니다!

**나쁜 방법 (프론트엔드에서 직접 처리):**

```javascript
// 브라우저에서 직접 데이터베이스 접속 (위험!)
const password = "database_password_123"; // 모든 사용자가 볼 수 있음!
const books = database.connect(password).getBooks();
```

**문제점:**

- 🚨 데이터베이스 비밀번호가 모든 사용자에게 노출
- 🚨 악의적인 사용자가 데이터를 마음대로 수정/삭제 가능
- 🚨 보안 사고 발생!

**좋은 방법 (백엔드를 통해 처리):**

```javascript
// 프론트엔드: 백엔드에게 부탁만 함
fetch('/api/books');

// 백엔드: 비밀번호는 여기에만 있음 (안전!)
const password = process.env.DATABASE_PASSWORD; // 사용자는 못 봄
const books = database.connect(password).getBooks();
```

### Q2: route.ts 코드는 왜 안 보이나요?

**A:** 서버에서만 실행되기 때문입니다!

**비유:**

```
식당 주방 레시피 = route.ts 코드
  → 손님(사용자)은 절대 못 봄
  → 주방(서버)에만 있음

완성된 요리 = JSON 데이터
  → 손님이 받아서 먹음
```

**실제로 전송되는 것:**

```
서버 → 사용자
{ "books": [...] } ✅

route.ts 코드 ❌ (절대 전송 안 됨)
```

### Q3: API와 URL의 차이가 뭔가요?

**A:** API는 기능, URL은 그 기능의 주소입니다!

```
API = "책 검색 기능"
URL = "그 기능에 접근하는 주소"

예시:
API: 네이버 검색 API
URL: https://api.naver.com/search
```

**비유:**

```
API = 피자 주문 서비스
URL = 피자 가게 전화번호
```

### Q4: JSON이 뭔가요?

**A:** 데이터를 주고받을 때 사용하는 표준 형식입니다!

**JSON 예시:**

```json
{
  "name": "홍길동",
  "age": 15,
  "hobbies": ["독서", "운동", "게임"]
}
```

**특징:**

- 사람도 읽을 수 있음
- 컴퓨터도 쉽게 처리할 수 있음
- 거의 모든 프로그래밍 언어에서 사용 가능

### Q5: 데이터베이스는 뭔가요?

**A:** 데이터를 체계적으로 저장하는 거대한 창고입니다!

**비유:**

```
도서관 = 데이터베이스
책 = 데이터
서가 = 테이블/컬렉션
청구기호 = ID
```

**예시:**

```
books 테이블:
┌────┬────────────────────┬────────────┬──────┐
│ ID │ 제목               │ 저자       │ 연도 │
├────┼────────────────────┼────────────┼──────┤
│ 1  │ 해리포터           │ J.K. 롤링  │ 1997 │
│ 2  │ 반지의 제왕        │ 톨킨       │ 1954 │
│ 3  │ 어린왕자           │ 생텍쥐페리 │ 1943 │
└────┴────────────────────┴────────────┴──────┘
```

---

## 🎓 핵심 요약 (시험에 나올 만한 것!)

### 전체 흐름 (외우기!)

```
1. 사용자 이벤트 (클릭, 입력 등)
   ↓
2. 이벤트 핸들러 실행
   ↓
3. fetch()로 백엔드 API 호출 ⭐
   ↓
4. HTTP 요청이 서버로 전송
   ↓
5. 라우터가 route.ts 파일 찾기
   ↓
6. route.ts 코드 실행 (데이터 처리)
   ↓
7. 처리 결과를 JSON으로 생성
   ↓
8. HTTP 응답으로 전송
   ↓
9. 브라우저가 데이터 수신
   ↓
10. 화면 업데이트
```

### 꼭 기억할 것!

1. **자동으로 전달되지 않습니다**
    
    - 개발자가 fetch()를 명시적으로 호출해야 함
2. **라우터는 교통정리만 합니다**
    
    - URL과 파일을 매칭해주는 역할
3. **route.ts 코드는 절대 안 보입니다**
    
    - 서버에서만 실행됨
    - 오직 결과(JSON)만 전송됨
4. **프론트엔드 ≠ 백엔드**
    
    - 프론트엔드: 화면, 사용자 상호작용
    - 백엔드: 데이터 처리, 보안, 비즈니스 로직

---

## 🚀 더 공부하고 싶다면?

### 다음 단계로 배울 것들:

1. **HTTP 메서드**
    
    - GET: 데이터 가져오기
    - POST: 데이터 생성하기
    - PUT: 데이터 수정하기
    - DELETE: 데이터 삭제하기
2. **HTTP 상태 코드**
    
    - 200: 성공
    - 400: 잘못된 요청
    - 401: 인증 필요
    - 404: 못 찾음
    - 500: 서버 오류
3. **인증과 권한**
    
    - 로그인 시스템
    - 토큰 (JWT)
    - 권한 검사
4. **데이터베이스**
    
    - SQL vs NoSQL
    - CRUD 작업
    - 쿼리 작성

---

## 🎯 마무리

이제 웹사이트에서 버튼을 클릭하면 뒤에서 어떤 일이 일어나는지 이해하셨나요?

**핵심만 기억하세요:**

```
사용자 동작 
  → 프론트엔드(fetch 호출) 
  → 백엔드(route.ts 실행) 
  → 결과 반환 
  → 화면 업데이트
```

**그리고 가장 중요한 것:**

> 라우터가 자동으로 전달하는 게 아니라,  
> 개발자가 fetch()를 명시적으로 호출해야 합니다! ⭐

이 글이 도움이 되셨기를 바랍니다! 😊

