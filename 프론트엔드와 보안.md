---
date: 2026-05-17
tags:
contexts:
  - Knowledge
  - Lecture
index:
  - Vibe Coding
summary: >-
  프론트엔드 중심 아키텍처에서는 Next.js와 Firebase, Supabase 같은 BaaS를 결합해 빠르게 개발할 수 있지만, 백엔드 서버가 없기 때문에 브라우저가 데이터베이스와 직접 통신하는 구조가 된다. 이 때문에 API 키를 코드에 하드코딩하거나 공개 저장소에 올리면 쉽게 노출될 수 있으며, 로컬에서는 `.env.local`과 `.gitignore`로 관리하고 배포 환경에서는 Vercel 같은 플랫폼의 환경 변수 설정에 동일한 키를 등록해야 한다. 특히 클라이언트에서 필요한 값은 Next.js 규칙에 따라 `NEXT_PUBLIC_` 접두어를 붙여 브라우저 노출을 명시해야 하며, 이는 편의성과 보안 사이의 구조적 타협을 의미한다. 결국 프론트엔드 중심 앱의 실질적인 방어는 환경 변수 관리에 그치지 않고, 클라우드 콘솔에서 HTTP 리퍼러 제한을 걸어 허용된 도메인에서 온 요청만 통과시키는 방식으로 완성된다.
author:
people:
YouTube_url:
scr_url:
---
## 🛡️ 프론트엔드 중심 앱(Next.js + BaaS) 보안 핵심 정리

이 문서는 프론트엔드 중심 아키텍처(서버리스)로 웹 애플리케이션을 개발할 때 반드시 알아야 할 환경 변수 보안과 배포 시 주의사항을 정리한 일반적인 가이드 문서입니다.

![보안 핵심.png](file:///G:/%EB%82%B4%20%EB%93%9C%EB%9D%BC%EC%9D%B4%EB%B8%8C/Obsidian.library/images/MP9RV0FCCX3VM.info/%EB%B3%B4%EC%95%88%20%ED%95%B5%EC%8B%AC.png)


#### 1. 아키텍처의 특징: 프론트엔드 중심 (Serverless)

- **특징**: 전통적인 백엔드 서버를 직접 구축하지 않고, **Next.js(화면 렌더링)**와 **BaaS(Firebase, Supabase 등)** 외부 서비스를 결합하여 개발하는 방식입니다.
- **핵심 인과관계**: 백엔드 서버가 없기 때문에, 데이터베이스와 통신하는 역할이 **사용자의 웹 브라우저(클라이언트)**에게 맡겨집니다.

#### 2. 자주 발생하는 보안 및 배포 이슈

##### 🚨 이슈 1: 공개 저장소(GitHub) API 키 노출

- **원인**: 외부 서비스에 접속하기 위한 출입증(API 키)을 소스 코드에 그대로 적어둔 채(하드코딩) 깃허브 같은 공개 저장소에 올렸기 때문에 발생합니다.
- **해결**: `.env.local` 같은 로컬 환경 변수 파일을 만들어 키를 숨기고, 깃허브에는 이 파일이 올라가지 않도록(`.gitignore`) 설정해야 합니다.

##### 🚨 이슈 2: 배포 플랫폼(Vercel 등) 빌드 에러

- **원인**: 깃허브에는 보안을 위해 `.env` 파일이 올라가지 않았으므로, Vercel이 코드를 빌드할 때 API 키를 찾지 못해 발생합니다. (예: `invalid-api-key` 에러)
- **해결**: 배포 플랫폼(Vercel)의 설정 화면(Environment Variables)에 내 컴퓨터의 로컬 환경 변수와 동일한 키워드와 값을 직접 입력해 주어야 합니다. (주의: 키 이름의 대소문자가 정확히 일치해야 함)
- **보충**: 프론트엔드 중심 앱은 백엔드 서버 없이 클라이언트(브라우저)가 데이터베이스(BaaS)와 직접 통신하는 구조이므로, Next.js에게 **"이 키는 브라우저로 내보내도 괜찮아!"** 라고 허락하는 `NEXT_PUBLIC_` 접두어가 변수명 앞에 반드시 필요합니다. (백엔드 중심 앱이라면 반대로 이 접두어가 없어야 더 안전합니다.)

#### 3. 핵심 보안 개념 💡

##### 왜 변수명에 `NEXT_PUBLIC_`을 붙여야 할까?

이것은 파이어베이스의 규칙이 아닌 **Next.js 프레임워크의 엄격한 환경 변수 노출 규칙**입니다.

- **`NEXT_PUBLIC_`이 없는 변수 (예: `SECRET_API_KEY`)**
    - 의미: 서버 안에서만 안전하게 사용하고 클라이언트(브라우저)로는 절대 내보내지 말라는 마크입니다. (백엔드 중심 앱에서 정상적인 설정)
- **`NEXT_PUBLIC_`이 있는 변수 (예: `NEXT_PUBLIC_FIREBASE_API_KEY`)**
    - 의미: 앱 실행을 위해 브라우저(사용자)가 알아야 하므로 **"이 정보는 밖으로 내보내도 돼!"**라고 허락하는 스위치입니다. (프론트엔드 통신이 필수적인 현재 구조에서는 무조건 붙여야 함)

##### 우리의 최종 방어선: HTTP 리퍼러 제한 (도메인 제한) 🧱

프론트엔드 중심 앱이기 때문에, 구조상 API 키가 브라우저를 통해 누구나 볼 수 있게 노출될 수밖에 없습니다. 이를 악용한 '요금 폭탄' 공격을 막기 위한 필수 보안 조치입니다.

- **개념**: 외부 클라우드 플랫폼(Google Cloud 등) 설정에서 "우리 서비스의 정식 웹사이트 주소에서 온 요청만 통과시켜 줘!"라고 허용 목록(Whitelist)을 작성해 두는 것입니다.
- **효과**: 해커가 API 키를 훔쳐서 다른 불법 사이트나 외부 컴퓨터에 심어두더라도, 출발지 주소가 일치하지 않으므로 플랫폼 자체에서 요청을 원천 차단합니다. (안전 보장)

---

> **최종 요약** 백엔드를 생략하여 얻은 엄청난 개발의 편리함(속도) 대신, 클라이언트(브라우저)에 API 키가 노출되는 운명적인 약점이 생깁니다. 이를 1차적으로 환경 변수(`.env`)와 `NEXT_PUBLIC_`으로 통제하고, 최종적으로 클라우드 콘솔에서 **'도메인 리퍼러 제한'을 걸어 완벽하게 방어하는 것이 프론트엔드 중심 아키텍처 보안의 뼈대입니다.
