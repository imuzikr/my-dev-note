---
date: 2025-11-29
tags:
  - 웹앱
  - 빌드
  - dist
  - build
  - 번들러
  - HTML
  - Typescript
  - Javascript
  - React
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---

> [!summary]
> 웹 앱 빌드는 개발자가 작성한 소스 코드를 웹 브라우저가 이해하고 실행할 수 있는 형태로 변환하는 필수 과정입니다. 개발의 편의성과 생산성을 위해 TypeScript, JSX, SCSS 같은 확장된 언어와 여러 파일 및 모듈로 나눈 코드를 사용하지만, 브라우저는 HTML, CSS, JavaScript만 인식하므로 이러한 코드들을 브라우저 친화적인 형태로 바꿔야 합니다. 이 과정은 주로 터미널에서 `npm run build` 명령을 통해 시작되며, 이는 `package.json`에 정의된 스크립트를 실행합니다.
> 
> 빌드 과정은 여러 단계로 진행됩니다.
> 
> 1.  **TypeScript 컴파일:** 먼저 TypeScript 컴파일러(tsc)가 프로젝트 내의 모든 `.ts`, `.tsx` 파일을 읽어 `tsconfig.json` 설정에 따라 순수한 JavaScript로 변환합니다. 이 단계에서 타입 정보는 제거되고, 타입 오류가 검출되면 빌드가 중단됩니다.
> 2.  **모듈 번들링:** JavaScript로 변환된 여러 파일들은 웹팩(Webpack)이나 Vite 같은 번들러에 의해 하나 또는 몇 개의 파일로 합쳐집니다. 이는 브라우저가 개별 파일을 여러 번 요청하는 대신 효율적으로 자원을 로드하여 네트워크 부담을 줄이고 로딩 속도를 향상시키기 위함입니다. 번들러는 `import` 의존성 그래프를 따라 필요한 모든 코드를 수집합니다.
> 3.  **코드 최적화(Minification):** 번들링된 코드는 사용자가 읽기 쉬운 형태에서, 불필요한 공백, 줄바꿈, 주석을 제거하고 변수명과 함수명을 짧게 축약하여 파일 크기를 최소화하는 과정을 거칩니다. 또한 사용되지 않는 코드(dead code)도 제거됩니다.
> 4.  **CSS 처리:** SCSS나 LESS 같은 CSS 전처리기를 사용했다면, 이들은 먼저 일반 CSS로 컴파일됩니다. 이후 CSS 파일들도 병합 및 압축되며, PostCSS와 같은 도구를 통해 브라우저 호환성을 위한 벤더 프리픽스(예: `-webkit-`)가 자동으로 추가될 수 있습니다.
> 5.  **이미지 및 정적 파일 처리:** 프로젝트에 포함된 이미지, 폰트 등 정적 파일들도 압축되거나 WebP 같은 최신 포맷으로 변환됩니다. 작은 이미지나 SVG 파일은 Base64로 인코딩되어 CSS나 JavaScript 파일에 직접 포함되어 HTTP 요청 수를 줄이기도 합니다.
> 6.  **코드 분할(Code Splitting):** 초기 로딩 시간을 줄이기 위해 코드 전체를 하나의 거대한 파일로 만들지 않고, 필요한 경우에만 로드되도록 코드를 여러 개의 청크(chunk)로 분할합니다. `React.lazy()`와 같은 지연 로딩 기법이 여기에 해당합니다.
> 7.  **Tree Shaking:** 사용하지 않는 코드를 최종 번들에서 제거하는 최적화 기법으로, 예를 들어 라이브러리에서 특정 함수만 사용한다면 해당 함수와 그 의존성 코드만 포함하고 나머지는 제거하여 번들 크기를 더욱 줄입니다.
> 8.  **환경변수 처리:** 개발 및 프로덕션 환경에 따라 달라지는 설정 값(예: API_URL)은 `.env` 파일에 정의된 환경변수를 통해 빌드 시점에 실제 값으로 대체됩니다.
> 9.  **HTML 파일 생성:** 마지막으로 `index.html` 파일이 생성되며, 빌드된 JavaScript 및 CSS 파일들을 로드하는 `<script>`와 `<link>` 태그가 자동으로 삽입됩니다.
> 
> 이 모든 과정을 거치면 `dist` 또는 `build` 폴더에 최적화된 JavaScript, CSS 파일과 정적 자원, 그리고 이를 참조하는 `index.html` 파일이 생성됩니다. 이 빌드 결과물은 웹 서버에 배포되어 사용자에게 제공됩니다. 빌드 시간은 프로젝트 규모에 따라 다르며, Vite 같은 최신 빌드 도구는 개발 중 빠른 피드백과 프로덕션 빌드 최적화를 위해 다양한 기법을 사용합니다. 빌드 완료 후에는 `npm run preview` 명령을 통해 로컬 서버에서 실제 배포 환경과 동일하게 빌드 결과물을 테스트할 수 있습니다. 실무에서는 다국어 지원, 서버 사이드 렌더링, 자동 테스트 등 추가적인 복잡한 과정이 빌드 파이프라인에 포함될 수 있습니다.





웹 앱을 빌드한다는 것은 개발 과정에서 작성한 소스 코드를 실제 브라우저에서 실행 가능한 형태로 변환하는 과정입니다. 이 과정을 처음부터 끝까지 상세하게 살펴보겠습니다.

## 빌드가 필요한 이유

- 개발자는 편의성과 생산성을 위해 TypeScript, JSX, SCSS 같은 확장된 언어를 사용하고, 코드를 여러 파일과 모듈로 나누어 작성합니다. 하지만 브라우저는 기본적으로 HTML, CSS, JavaScript만 이해할 수 있습니다. 따라서 개발 환경에서 사용한 모든 기술들을 브라우저가 이해할 수 있는 형태로 변환해야 하는데, 이것이 바로 빌드 과정입니다.

- 예를 들어 TypeScript로 다음과 같은 코드를 작성했다고 가정해보겠습니다.

```typescript
interface Student {
	name: string;
	grade: number;
}

const students: Student[] = [
	{ name: "김철수", grade: 85 },
	{ name: "이영희", grade: 92 }
];

const averageGrade = students.reduce((sum, student) => sum + student.grade, 0) / students.length;
```

- 이 코드는 개발자에게는 명확하고 안전하지만, 브라우저는 `interface`나 타입 선언인 `: Student[]` 같은 TypeScript 문법을 이해하지 못합니다. 빌드 과정을 거치면 이것이 순수한 JavaScript로 변환됩니다.

## 빌드 과정의 시작: 명령 실행

- 터미널에서 `npm run build` 명령을 입력하면 빌드 과정이 시작됩니다. 이 명령은 실제로는 `package.json` 파일에 정의된 스크립트를 실행하는 것입니다. `package.json` 파일을 열어보면 다음과 같은 부분이 있습니다.

```json
{
	"scripts": {
		"dev": "vite",
		"build": "tsc && vite build",
		"preview": "vite preview"
	}
}
```

- 여기서 `build` 스크립트를 보면 `tsc && vite build`라고 되어 있습니다. 이것은 두 가지 작업을 순차적으로 실행한다는 의미입니다. 먼저 TypeScript 컴파일러(tsc)가 실행되고, 그다음 Vite 빌더가 실행됩니다.

## 1단계: TypeScript 컴파일

- TypeScript 컴파일러는 프로젝트의 모든 `.ts`와 `.tsx` 파일을 찾아서 JavaScript로 변환합니다. 이 과정에서 `tsconfig.json` 파일에 설정된 규칙을 따릅니다.

```json
{
	"compilerOptions": {
		"target": "ES2020",
		"module": "ESNext",
		"jsx": "react-jsx",
		"strict": true,
		"outDir": "./dist"
	}
}
```

- 이 설정에서 `target: "ES2020"`은 컴파일된 JavaScript가 ES2020 문법을 사용한다는 의미입니다. 앞서 예시로 든 TypeScript 코드는 다음과 같은 JavaScript로 변환됩니다.

```javascript
const students = [
	{ name: "김철수", grade: 85 },
	{ name: "이영희", grade: 92 }
];

const averageGrade = students.reduce((sum, student) => sum + student.grade, 0) / students.length;
```

- 타입 정보는 모두 제거되고 순수한 JavaScript 로직만 남게 됩니다. 컴파일러는 이 과정에서 타입 오류도 검사합니다. 만약 `student.grade`를 `student.score`로 잘못 입력했다면 컴파일 단계에서 오류가 발생하고 빌드가 중단됩니다.

## 2단계: 모듈 번들링

- JavaScript로 변환된 코드는 여전히 여러 개의 파일로 나뉘어 있습니다. 예를 들어 다음과 같은 구조일 수 있습니다.

```
src/
	components/
		Header.tsx
		Footer.tsx
		StudentList.tsx
	utils/
		calculations.ts
		formatters.ts
	App.tsx
	main.tsx
```

- 브라우저가 이 모든 파일을 개별적으로 요청하면 네트워크 부담이 커지고 로딩 속도가 느려집니다. 번들러(Vite, Webpack 등)는 이 파일들을 분석해서 의존성 그래프를 만들고, 관련된 파일들을 하나 또는 몇 개의 파일로 합칩니다.

- Vite는 먼저 `main.tsx` 파일을 진입점(entry point)으로 시작합니다. 이 파일에서 `import`한 모듈들을 추적하고, 그 모듈들이 다시 `import`한 것들을 재귀적으로 따라가면서 필요한 모든 코드를 수집합니다.

- 예를 들어 `main.tsx`가 다음과 같다면:

```typescript
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
	<React.StrictMode>
		<App />
	</React.StrictMode>
);
```

- 번들러는 React 라이브러리, ReactDOM, App 컴포넌트, CSS 파일을 모두 찾아서 하나의 번들에 포함시킵니다. `App.tsx`에서 다시 다른 컴포넌트를 import하면 그것들도 포함됩니다.

## 3단계: 코드 최적화 (Minification)

- 번들링된 코드는 아직 개발자가 읽기 쉬운 형태입니다. 변수명이 의미있게 작성되어 있고, 들여쓰기와 줄바꿈이 있으며, 주석도 포함되어 있습니다. 하지만 사용자에게 전달될 파일은 크기가 작을수록 좋습니다.

- 최적화 과정에서는 다음과 같은 작업들이 진행됩니다. 불필요한 공백과 줄바꿈이 모두 제거되고, 변수명과 함수명이 짧게 축약됩니다. 주석은 삭제되고, 사용되지 않는 코드(dead code)는 제거됩니다.

- 예를 들어 다음과 같은 코드가:

```javascript
function calculateAverageGrade(studentList) {
	// 학생들의 평균 성적을 계산합니다
	const totalGrade = studentList.reduce((sum, student) => {
		return sum + student.grade;
	}, 0);
	
	const averageGrade = totalGrade / studentList.length;
	return averageGrade;
}
```

- 다음과 같이 압축됩니다:

```javascript
function a(b){const c=b.reduce((d,e)=>d+e.grade,0);return c/b.length}
```

- 이렇게 하면 파일 크기가 크게 줄어들어 네트워크 전송 시간이 단축됩니다.

## 4단계: CSS 처리

- JavaScript뿐만 아니라 CSS 파일들도 처리 과정을 거칩니다. SCSS나 LESS 같은 CSS 전처리기를 사용했다면 먼저 일반 CSS로 컴파일됩니다.

```scss
$primary-color: #3498db;

.button {
	background-color: $primary-color;
	
	&:hover {
		background-color: darken($primary-color, 10%);
	}
}
```

- 이런 SCSS 코드는 다음과 같은 CSS로 변환됩니다:

```css
.button {
	background-color: #3498db;
}

.button:hover {
	background-color: #2980b9;
}
```

- 그다음 CSS 파일들도 병합되고 압축됩니다. 또한 PostCSS 같은 도구가 브라우저 호환성을 위한 벤더 프리픽스를 자동으로 추가합니다.

```css
.flex-container {
	display: flex;
}
```

- 이것이 다음과 같이 변환됩니다:

```css
.flex-container {
	display: -webkit-box;
	display: -ms-flexbox;
	display: flex;
}
```

## 5단계: 이미지 및 정적 파일 처리

- 프로젝트에 포함된 이미지, 폰트, 아이콘 같은 정적 파일들도 최적화됩니다. 이미지는 압축되고, 필요한 경우 WebP 같은 최신 포맷으로 변환됩니다. 작은 이미지나 SVG 파일은 Base64로 인코딩되어 CSS나 JavaScript 파일에 직접 포함되기도 합니다.

- 예를 들어 작은 로고 이미지가 있다면, 별도의 이미지 파일로 남기는 대신 다음과 같이 CSS에 직접 포함될 수 있습니다:

```css
.logo {
	background-image: url(data:image/png;base64,iVBORw0KGgoAAAANSUhEUgAAAAUA...);
}
```

- 이렇게 하면 이미지를 가져오기 위한 별도의 HTTP 요청이 필요 없어서 로딩 속도가 빨라집니다.

## 6단계: 코드 분할 (Code Splitting)

- 모든 코드를 하나의 거대한 파일로 만들면 초기 로딩 시간이 길어집니다. 따라서 최신 빌드 도구들은 코드를 여러 청크(chunk)로 나누는 작업을 수행합니다.

- React에서 `React.lazy()`를 사용해서 컴포넌트를 지연 로딩하도록 설정했다면:

```typescript
const StudentDashboard = React.lazy(() => import('./components/StudentDashboard'));
```

- 빌드 과정에서 `StudentDashboard` 컴포넌트와 그와 관련된 코드들이 별도의 파일로 분리됩니다. 사용자가 실제로 대시보드 페이지에 접근할 때만 해당 파일이 로드됩니다.

- 빌드 결과물을 보면 다음과 같은 구조가 됩니다:

```
dist/
	assets/
		index-a1b2c3d4.js          # 메인 번들
		StudentDashboard-e5f6g7h8.js  # 지연 로딩되는 청크
		index-i9j0k1l2.css          # 스타일 파일
	index.html
```

- 파일명에 붙은 해시값(`a1b2c3d4` 같은 부분)은 파일 내용이 변경될 때마다 달라지므로, 브라우저 캐싱 문제를 해결할 수 있습니다.

## 7단계: Tree Shaking

- Tree shaking은 사용하지 않는 코드를 제거하는 최적화 기법입니다. 예를 들어 lodash 라이브러리를 설치하고 그중 하나의 함수만 사용한다면:

```javascript
import { debounce } from 'lodash';

const handleSearch = debounce((query) => {
	// 검색 로직
}, 300);
```

- 빌드 과정에서 lodash의 전체 코드가 아니라 `debounce` 함수와 그것이 의존하는 최소한의 코드만 번들에 포함됩니다. 나머지 수백 개의 함수들은 "흔들어서 떨어뜨려지는" 나뭇잎처럼 제거됩니다.

## 8단계: 환경변수 처리

- 개발 환경과 프로덕션 환경에서 다른 설정을 사용하는 경우가 많습니다. `.env` 파일에 정의한 환경변수들은 빌드 시점에 실제 값으로 대체됩니다.

```javascript
const API_URL = import.meta.env.VITE_API_URL;
```

- 이 코드는 빌드 과정에서 다음과 같이 변환됩니다:

```javascript
const API_URL = "https://api.example.com/v1";
```

- 이렇게 하면 런타임에 환경변수를 읽는 오버헤드가 없어집니다.

## 9단계: HTML 파일 생성

- 마지막으로 `index.html` 파일이 생성됩니다. 이 파일에는 빌드된 JavaScript와 CSS 파일들을 로드하는 태그가 자동으로 삽입됩니다.

- 개발 중에는 다음과 같은 간단한 HTML 템플릿을 사용했을 것입니다:

```html
<!DOCTYPE html>
<html lang="ko">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>학생 관리 시스템</title>
</head>
<body>
	<div id="root"></div>
	<script type="module" src="/src/main.tsx"></script>
</body>
</html>
```

- 빌드 후에는 다음과 같이 변환됩니다:

```html
<!DOCTYPE html>
<html lang="ko">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>학생 관리 시스템</title>
	<link rel="stylesheet" href="/assets/index-i9j0k1l2.css">
</head>
<body>
	<div id="root"></div>
	<script type="module" src="/assets/index-a1b2c3d4.js"></script>
</body>
</html>
```

## 빌드 결과물의 구조

- 모든 과정이 완료되면 `dist` 또는 `build` 폴더에 다음과 같은 구조의 파일들이 생성됩니다:

```
dist/
	assets/
		index-a1b2c3d4.js          # 메인 JavaScript 번들 (압축됨)
		index-i9j0k1l2.css          # 스타일시트 (압축됨)
		StudentDashboard-e5f6g7h8.js  # 지연 로딩 청크
		logo-m3n4o5p6.png           # 최적화된 이미지
	index.html                      # 진입점 HTML 파일
	favicon.ico                     # 파비콘
```

- 이 폴더의 내용을 웹 서버에 업로드하면 웹 앱이 실제로 동작하게 됩니다.

## 빌드 시간과 최적화

- 빌드 과정은 프로젝트 크기에 따라 몇 초에서 몇 분까지 걸릴 수 있습니다. Create React App으로 만든 간단한 프로젝트는 보통 10~30초 정도 소요되고, 대규모 프로젝트는 수 분이 걸리기도 합니다.

- Vite는 이 과정을 최적화하기 위해 여러 기법을 사용합니다. 개발 중에는 전체 번들링을 하지 않고 ES 모듈을 그대로 사용하며, 변경된 부분만 빠르게 다시 빌드하는 HMR(Hot Module Replacement) 기능을 제공합니다. 프로덕션 빌드에서는 Rollup을 사용해서 최적화된 번들을 생성합니다.

## 빌드 과정의 검증

- 빌드가 완료되면 실제로 잘 작동하는지 로컬에서 테스트해볼 수 있습니다. Vite의 경우 다음 명령을 실행하면 빌드된 결과물을 로컬 서버에서 미리 볼 수 있습니다:

```bash
npm run preview
```

- 이 명령은 `dist` 폴더의 내용을 로컬 웹 서버로 제공해서, 실제 배포 환경과 동일한 조건에서 테스트할 수 있게 해줍니다. 개발 서버와 달리 빌드된 파일을 직접 사용하므로, 빌드 과정에서 발생한 문제를 발견할 수 있습니다.

## 실무에서의 빌드 과정

- 실제 프로젝트에서는 빌드 과정이 더 복잡할 수 있습니다. 예를 들어 다국어 지원이 필요하다면 각 언어별로 별도의 번들을 생성할 수 있고, 서버 사이드 렌더링을 지원한다면 클라이언트용과 서버용 번들을 각각 만들어야 합니다.

- 또한 빌드 과정에서 자동으로 테스트를 실행하거나, 코드 커버리지를 측정하거나, 번들 크기를 분석하는 등의 추가 작업을 수행할 수도 있습니다. 이런 모든 과정이 앞서 말씀드린 CI/CD 파이프라인의 일부가 됩니다.