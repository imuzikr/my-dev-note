---
date: 2026-04-07
tags:
  - 서버리스
  - BaaS
  - Firebase
  - CSR
  - Vite
  - 환경변수
  - Next_js
  - 빌드
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---

---


## AI Studio 앱, 서버가 있는 걸까 없는 걸까

> 서버 없이 동작하는 앱의 비밀을 따라가는 탐구 대상: 비개발자 · 웹 개발 입문자

Google AI Studio에서 앱을 만들어 배포 버튼을 눌렀다. 프론트엔드 화면도 보이고, Firebase 데이터베이스도 연결되어 있다. 그런데 한 가지 이상한 점이 있다. 백엔드 서버 코드를 직접 작성한 적이 없는데, 앱은 정상적으로 동작한다. 데이터가 저장되고, Gemini API가 호출되고, 사용자가 접속하면 화면이 뜬다. 서버 없이 이 모든 것이 어떻게 가능한 걸까? 이 질문을 따라가다 보면, 현대 웹 개발의 핵심 구조가 자연스럽게 드러난다.

---

## 1. 서버의 역할을 다른 누군가가 대신하고 있다

전통적인 웹 서비스는 식당과 비슷하다. 손님(사용자)이 주문을 하면 웨이터(백엔드 서버)가 받아서 주방(데이터베이스)에 전달하고, 요리가 나오면 다시 손님에게 가져다준다. 웨이터는 단순한 전달자가 아니라 "이 손님은 알레르기가 있으니 재료를 바꿔야 해", "이 주문은 권한이 없으니 거절해야 해" 같은 판단과 검증도 함께 수행한다.

AI Studio로 만든 앱은 이 구조가 아니다. 웨이터 없이 손님이 직접 주방 창구에 가서 주문하는 셀프 서비스 식당에 가깝다. 다만 아무나 주방에 들어가서 음식을 만지작거리면 안 되니까, 창구에 "본인이 주문한 것만 가져갈 수 있습니다", "직원 전용 구역은 출입 금지입니다" 같은 규칙이 붙어 있다. 이 셀프 서비스 창구 시스템 전체를 구글이 운영해주는 것이 바로 **Firebase**이고, 창구에 붙은 규칙이 프로젝트 파일 중 `firestore.rules`에 정의된 **보안 규칙(Security Rules)**이다.

이처럼 개발자가 백엔드 서버를 직접 만들고 관리하지 않아도 되는 구조를 **서버리스(Serverless)**라고 부른다. 서버가 아예 없다는 뜻이 아니라, 서버의 존재를 개발자가 신경 쓸 필요가 없다는 의미다. 그리고 Firebase처럼 데이터베이스, 사용자 인증, 보안 규칙 같은 백엔드 기능을 통째로 제공해주는 서비스를 **BaaS(Backend as a Service, 서비스형 백엔드)**라고 부른다.

> [!info] 서버리스와 BaaS의 관계 서버리스는 큰 범주의 개념이고, BaaS는 그 안에 속하는 한 가지 형태다. 서버리스 = BaaS가 아니라, BaaS ⊂ 서버리스이다. AI Studio로 만든 앱에서는 Firebase(BaaS)와 Cloud Run이 각각 역할을 나눠 백엔드 기능을 대신 처리하고, 이 전체 구조를 통틀어 서버리스 아키텍처라고 부른다.

여기까지 이해하면 백엔드 쪽 의문은 풀린다. 그런데 곧 또 다른 질문이 생긴다. 백엔드를 Firebase가 대신해준다는 건 알겠는데, 그렇다면 사용자가 보는 화면은 대체 누가 만들어주는 걸까?

---

## 2. 서버가 없다면 화면은 누가 만들어주는가

답을 찾기 위해 프로젝트 파일 구조를 살펴볼 필요가 있다. 루트에 `vite.config.ts`가 있고, `src` 폴더 안에 `App.tsx`, `main.tsx`, `components`, `pages` 같은 구조가 보인다. 이것은 **Vite**라는 빌드 도구로 개발된 **React + TypeScript** 프론트엔드 프로젝트의 전형적인 모습이다. 백엔드 서버 코드에 해당하는 폴더(`server`, `api`, `routes` 등)는 어디에도 없으므로, 순수한 프론트엔드 프로젝트라는 것이 확인된다.

여기서 한 가지 짚고 넘어가야 할 것이 있다. 개발자가 작성하는 TypeScript와 React 코드는 사실 브라우저가 직접 이해할 수 없는 언어다. 그래서 배포 전에 **빌드(build)**라는 변환 과정을 거치게 된다. TypeScript는 일반 JavaScript로 변환되고, React의 컴포넌트 문법도 표준 JavaScript로 바뀌며, CSS는 하나로 합쳐지고, HTML 파일이 이 모든 것을 연결하는 뼈대 역할을 한다. 실제로는 성능 최적화를 위해 수십 개의 파일로 쪼개지지만, 본질은 "개발용 코드가 브라우저용 파일로 변환된다"는 것이다.

이렇게 빌드된 앱은 기본적으로 **CSR(Client Side Rendering, 클라이언트 측 렌더링)** 방식으로 동작한다. 사용자가 웹 주소로 접속하면 서버는 거의 비어 있는 HTML 파일 하나만 보내준다. 그 HTML 안에는 "이 JavaScript 파일을 다운로드해서 실행하세요"라는 지시만 담겨 있고, 브라우저가 그 JavaScript를 받아 실행한 뒤에야 화면의 모든 요소를 직접 그려낸다. 화면 그리는 작업(Rendering)이 서버가 아닌 클라이언트(브라우저)에서 일어나기 때문에 Client Side Rendering이라고 부르는 것이다.

> [!tip] 핵심 정리 Vite + CSR 조합은 서버리스 구조와 자연스럽게 맞아떨어진다. 서버는 파일만 전달하는 단순한 역할이고, 화면 구성과 데이터 통신은 모두 브라우저가 직접 처리한다. 다만 첫 접속 시 JavaScript 다운로드와 실행을 기다려야 해서 초기 화면이 늦게 뜨고, 검색 엔진이 페이지 내용을 읽기 어렵다는 단점이 있다.

여기까지 따라오면 또 한 가지 의문이 생긴다. 서버가 필요 없다고 했는데, 막상 Cloud Run으로 배포하면 서버가 하나 만들어진다고 한다. 이건 모순 아닌가?

---

## 3. 배포할 때 생기는 서버의 정체

다시 식당 비유로 돌아가 보자. 배포 시 만들어지는 이 서버는 요리를 하거나 주문을 처리하는 웨이터가 아니라, **식당 입구의 안내원**에 가깝다. 손님이 도착하면 "여기 메뉴판이요, 저쪽 창구에서 주문하세요"라고 빌드된 프론트엔드 파일을 건네주는 것이 전부다. 이런 서버를 **정적 웹 서버(Static Web Server)**라고 부른다. 비용도 부담이 적은데, 누군가 접속할 때만 서버가 깨어나고 아무도 접속하지 않으면 잠들어 있는 방식이라 소규모 앱이라면 무료 범위 안에서 유지되는 경우가 많다.

다만 이 서버가 한 가지 중요한 역할을 더 한다. 프로젝트의 `gemini.ts` 파일을 보면 `process.env.GEMINI_API_KEY`라는 코드가 있는데, 이것은 서버 측에서만 동작하는 Node.js 문법으로, 환경 변수에서 Gemini API 키를 가져오는 코드다. API 키는 민감한 정보이기 때문에 브라우저에 노출되면 안 되고, 반드시 서버 측에서 관리되어야 한다. AI Studio에서 배포 버튼을 누르면, 이 API 키를 Cloud Run의 환경 변수에 자동으로 저장해준다.

#### `.env` 파일과 환경 변수의 차이

여기서 중요한 점은 환경 변수가 저장되는 위치다. 프로젝트 폴더 안의 `.env` 파일에 저장되는 것이 아니라, **Google Cloud 인프라 내부**에 저장된다. 두 방식의 차이는 다음과 같다.

|구분|`.env` 파일|Cloud Run 환경 변수|
|---|---|---|
|저장 위치|프로젝트 폴더 안의 텍스트 파일|Google Cloud 인프라 내부|
|노출 위험|GitHub 실수 업로드, 빌드 과정 노출 가능|코드 어디에도 키 값이 남지 않음|
|접근 방법|파일 열기|Google Cloud 콘솔 로그인 필요|
|용도|로컬 개발 환경|프로덕션 배포 환경|

프로젝트에 보이는 `.env.example` 파일은 실제 키 값이 아닌 "이런 환경 변수가 필요합니다"라는 **안내서** 역할을 한다. 다른 사람이 이 프로젝트를 받아 실행할 때 이 파일을 참고해서 자신의 `.env`를 만들도록 돕는 것이다.

> [!tip] 핵심 정리 AI Studio가 배포 버튼 한 번으로 API 키 발급, 환경 변수 등록, 보안 관리까지 모두 자동 처리해준다. 사용자는 별도의 키 관리 작업 없이도 안전한 배포가 가능하다.

지금까지 본 것은 "Vite + Firebase + Cloud Run"이라는 특정 조합이다. 그렇다면 만약 같은 앱을 다른 도구로 만들었다면 어떻게 달라질까?

---

## 4. 다른 방식으로 만들었다면 어떻게 달라질까

같은 앱을 Vercel이나 Netlify로 배포한다면, Cloud Run이 자동 처리해주던 API 키 관리를 직접 해결해야 한다. 이 플랫폼들은 **서버리스 함수(Serverless Function)** 또는 **엣지 함수(Edge Function)**라는 기능을 제공하는데, Vercel의 경우 `/api` 폴더에 파일을 만들면 자동으로 서버리스 함수로 동작한다.

작동 방식은 이렇다. 브라우저가 Gemini API를 직접 호출하는 대신, 먼저 서버리스 함수에 요청을 보낸다. 서버리스 함수는 환경 변수에서 API 키를 꺼내 Gemini API를 대신 호출하고, 결과만 브라우저에 돌려준다. 이렇게 하면 API 키가 브라우저에 노출되지 않으면서도 상시 가동 서버를 운영할 필요가 없다. 원리는 Cloud Run과 동일하지만, 개발자가 이 중간 단계를 직접 설정해야 한다는 차이가 있다.

더 근본적인 차이는 **Next.js**로 개발했을 경우에 나타난다. Vite + React로 개발하면 순수 프론트엔드 코드만 만들어지고 서버 측 코드가 필요하면 외부 환경을 별도로 연결해야 하지만, Next.js는 프론트엔드와 서버 코드를 하나의 프로젝트 안에서 함께 작성할 수 있도록 설계된 프레임워크다. API Route(`app/api/.../route.ts`)나 Server Component 같은 기능을 통해 서버 측 로직을 프로젝트 내부에서 바로 처리할 수 있다.

또한 Next.js는 **SSR(Server Side Rendering)** 방식을 기본 지원한다. 서버에서 미리 완성된 HTML을 보내주기 때문에, CSR의 단점이었던 첫 화면 로딩 지연과 검색 엔진 노출 문제가 해결된다.

#### Vite와 Next.js 비교

|구분|Vite (현재 앱)|Next.js|
|---|---|---|
|도구 성격|빌드 도구|프레임워크|
|서버 코드 위치|프로젝트 외부에 별도 구성|프로젝트 내부에 통합|
|기본 렌더링 방식|CSR|SSR(+ CSR, SSG 선택 가능)|
|API 키 관리|Cloud Run/서버리스 함수 별도 필요|프로젝트 내부에서 처리|
|첫 화면 로딩|느림|빠름|
|SEO(검색 노출)|불리|유리|

---

## 핵심 개념 정리

|개념|설명|비유|
|---|---|---|
|서버리스(Serverless)|개발자가 서버를 직접 만들고 관리하지 않아도 되는 구조|점원 없이 운영되는 셀프 서비스 식당|
|BaaS(Backend as a Service)|백엔드 기능을 통째로 제공하는 클라우드 서비스 (Firebase 등)|셀프 식당 시스템 전체를 운영해주는 본사|
|보안 규칙(Security Rules)|데이터베이스 접근 권한을 정의한 규칙|셀프 창구에 붙은 출입 규정|
|빌드(Build)|개발용 코드를 브라우저용 파일로 변환하는 과정|외국어 원고를 출판용 책으로 인쇄하는 과정|
|CSR(Client Side Rendering)|브라우저가 화면을 직접 그리는 방식|손님이 매장에서 직접 음식을 조립|
|SSR(Server Side Rendering)|서버가 완성된 화면을 미리 만들어 보내는 방식|주방에서 완성된 음식을 그대로 전달|
|정적 웹 서버|빌드된 파일만 전달하는 단순 서버|식당 입구 안내원|
|환경 변수(Environment Variable)|코드 외부에 저장되어 서버 실행 시 주입되는 설정 값|금고에 보관된 비밀 열쇠|
|서버리스 함수|필요할 때만 잠깐 실행되는 서버 측 코드 단위|호출 시에만 출동하는 임시 직원|

---

> [!abstract] 전체 흐름 요약 AI Studio로 만든 앱은 백엔드 서버를 직접 만들지 않았는데도 정상 동작한다. 그 비밀은 서버의 역할이 사라진 것이 아니라 여러 클라우드 서비스로 분산되어 있다는 데 있다. Firebase(BaaS)가 데이터베이스와 보안을 담당하고, 빌드된 프론트엔드는 CSR 방식으로 브라우저에서 직접 화면을 그리며, Cloud Run이 정적 파일 전달과 API 키 관리를 자동으로 처리한다. 같은 앱을 Vercel이나 Next.js로 만들었다면 이 역할 분담의 모양이 달라지지만, "개발자가 서버를 직접 만들지 않아도 되는" 서버리스의 본질은 동일하다.