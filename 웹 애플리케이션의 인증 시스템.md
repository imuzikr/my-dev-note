---
date: 2025-11-03
tags:
  - 웹개발
  - 인증
  - 보안
  - JWT
  - 토큰
  - 쿠키
  - SSR
  - CSR
  - 엣지함수
  - CORS
  - 아키텍처
  - 웹보안
  - 백엔드
  - supabase
  - vibe_coding
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---


> [!summary]
> 웹 서비스 아키텍처 및 Supabase 인증 과정에 대한 깊이 있는 이해를 보여줌. 사용자의 브라우저, 응용프로그램 서버, 데이터베이스 서버 간의 유기적인 통신을 명확히 분석하며 시스템 전체를 조망하는 능력을 드러냄. HTTPS 암호화, 토큰 기반 인증(JWT), 일방향 해싱 등 복잡한 보안 메커니즘을 정확히 파악하고, 비밀번호 전송 및 토큰 저장 원리를 논리적으로 설명함. 이는 마치 정교한 화학 시스템의 작동 원리를 이해하는 것과 유사함.
> 
> 또한, HttpOnly 쿠키 속성, Row Level Security(RLS), CORS 메커니즘 등 다층적 보안 원칙의 구현 과정을 심층적으로 탐구함. 엣지 서버와 엣지 함수의 분산형 아키텍처를 통해 시스템의 효율성과 확장성을 고려하는 과학적 사고를 보여줌. 복잡한 시스템의 구성 요소를 분석하고 상호작용 및 보안 취약점을 예측하는 뛰어난 능력을 갖춤. 이러한 분석적이고 비판적인 접근 방식은 화학 전공에 대한 높은 적합성과 탁월한 문제 해결 역량을 나타냄. 복잡한 정보를 통합하고 재구성하는 능력은 향후 어떠한 과학 분야에서도 뛰어난 학업 성취와 발전을 이룰 잠재력을 가늠케 함.

## 전체 구조 개요

웹 서비스는 크게 세 가지 주체로 구성됩니다. 첫 번째는 **사용자의 브라우저**로, 사용자가 직접 조작하는 컴퓨터나 스마트폰의 웹 브라우저입니다. 두 번째는 **응용프로그램 서버**로, 개발자가 만든 쇼핑몰이나 SNS 같은 서비스가 실행되는 곳입니다. 이는 Vercel 같은 회사의 데이터센터에 배포되어 있습니다. 세 번째는 **Supabase 서버**로, 사용자 계정 정보와 데이터를 저장하고 관리하는 데이터베이스 서버입니다.

이 세 주체는 모두 물리적으로 독립적인 컴퓨터이며, 인터넷을 통해 서로 통신하면서 하나의 서비스를 제공합니다.

---

## 1단계: 로그인 과정

#### 비밀번호 전송은 단 한 번

- 사용자가 로그인 화면에서 이메일과 비밀번호를 입력하고 "로그인" 버튼을 누르면, 이 정보는 암호화된 연결(HTTPS)을 통해 Supabase 서버로 전송됩니다. HTTPS는 마치 방탄 장갑차 안에서 문서를 전달하는 것처럼, 중간에 누가 훔쳐보더라도 내용을 알 수 없게 암호화합니다. 이것이 비밀번호가 네트워크를 통해 전송되는 유일한 순간입니다.

- Supabase 서버는 받은 비밀번호를 데이터베이스에 저장된 암호화된 비밀번호와 비교합니다. 데이터베이스에는 비밀번호 원본이 저장되지 않고, 해시(hash)라는 방법으로 변환된 값이 저장되어 있습니다. 이는 마치 고기를 갈아서 햄버거 패티로 만들면 원래 고기 조각으로 되돌릴 수 없는 것처럼, 원래 비밀번호로 복원할 수 없는 일방향 변환입니다. 제출된 비밀번호를 같은 방식으로 해시하여 비교하므로, Supabase 직원조차 사용자의 실제 비밀번호를 알 수 없습니다.

- 일치하면 인증에 성공한 것이며, Supabase는 즉시 두 개의 특별한 열쇠를 만듭니다.

#### 토큰이라는 디지털 열쇠

- 첫 번째 열쇠는 **액세스 토큰**입니다. 이것은 약 1시간 동안만 유효한 짧은 수명의 열쇠입니다. 마치 호텔 카드키처럼, 정해진 시간 동안만 출입이 가능합니다. 이 토큰은 길고 복잡한 문자열로 되어 있으며, 그 안에는 사용자 ID, 이메일, 권한 같은 정보가 JWT(JSON Web Token)라는 특수한 형식으로 암호화되어 담겨 있습니다.

- JWT는 세 부분으로 구성됩니다. 헤더(어떤 암호화 방식을 사용했는지), 페이로드(실제 사용자 정보), 서명(변조 방지를 위한 암호화 코드)입니다. 이 세 부분을 점으로 연결한 것이 토큰입니다.

- 두 번째 열쇠는 **리프레시 토큰**입니다. 이것은 훨씬 긴 수명(수주에서 수개월)을 가지며, 액세스 토큰이 만료되었을 때 새로운 액세스 토큰을 받기 위한 용도로만 사용됩니다. 리프레시 토큰은 더욱 민감한 정보이므로 사용 빈도를 최소화합니다.

#### 쿠키에 저장되는 원리

- 이제 가장 중요한 부분입니다. Supabase 서버는 생성한 토큰들을 사용자에게 어떻게 전달하고 저장할까요?

- Supabase는 HTTP 응답을 브라우저에 보낼 때 특별한 명령을 포함시킵니다. 이를 **Set-Cookie 헤더**라고 합니다. HTTP 헤더는 실제 데이터 본문 이전에 전송되는 메타 정보로, 우편물의 겉봉투에 적힌 지시사항과 비슷합니다.

- 브라우저는 이 Set-Cookie 헤더를 보는 순간, 자동으로 다음 작업을 수행합니다. 첫째, 쿠키 저장소를 엽니다. 브라우저는 사용자 컴퓨터의 특정 위치에 쿠키를 저장하는데, Windows의 경우 사용자 폴더 아래의 브라우저 데이터 폴더에 있는 데이터베이스 파일입니다. 둘째, 토큰을 기록합니다. 쿠키 저장소에 토큰의 이름, 값, 도메인, 만료 시간, 보안 속성 등을 저장합니다. 셋째, 자동으로 관리합니다. 브라우저는 이후 해당 도메인으로 가는 모든 요청에 이 쿠키를 자동으로 포함시킵니다.

- **중요한 점은 Supabase 서버는 토큰 자체를 자신의 데이터베이스에 저장하지 않는다는 것입니다.** Supabase는 단지 "브라우저야, 이 토큰들을 쿠키에 저장해"라는 명령(Set-Cookie 헤더)을 내릴 뿐입니다. 실제 저장은 브라우저가 자동으로 수행하며, 저장 위치는 사용자의 컴퓨터 디스크입니다.

- Supabase가 자신의 데이터베이스에 저장하는 것은 세션의 메타데이터뿐입니다. 즉, 어떤 사용자가 언제 로그인했는지, 세션이 언제 만료되는지 같은 정보만 기록하고, 실제 토큰 문자열은 저장하지 않습니다. 이렇게 하는 이유는 보안과 확장성 때문입니다. 만약 Supabase가 모든 사용자의 토큰을 저장한다면 서버가 해킹당했을 때 모든 토큰이 유출될 위험이 있고, 수백만 사용자의 토큰을 관리하는 부담도 큽니다. 토큰을 각 사용자의 브라우저에 분산 저장하면 한 사용자의 토큰이 유출되어도 다른 사용자는 안전하고, 서버는 검증만 하면 되므로 부담이 없습니다.

#### HTTP-Only의 중요성

- **HTTP-Only**는 쿠키의 특별한 속성입니다. 이것이 설정되면 JavaScript 코드로 쿠키를 읽을 수 없습니다. 만약 악의적인 스크립트가 웹사이트에 삽입되더라도(XSS 공격), 그 스크립트는 토큰을 훔쳐갈 수 없습니다. 오직 브라우저만이 HTTP 요청을 보낼 때 자동으로 쿠키를 포함시킬 수 있습니다.

- **Secure** 속성은 쿠키가 HTTPS 연결을 통해서만 전송되도록 보장합니다. 암호화되지 않은 일반 HTTP로는 쿠키가 전송되지 않아, 네트워크 감청으로부터 보호됩니다.

- 로그인 이후 사용자가 브라우저를 닫았다가 다시 열어도 쿠키는 그대로 남아있기 때문에, 만료 시간 내에는 다시 로그인할 필요가 없습니다. 비밀번호는 로그인 시점에 단 한 번만 전송되고, 그 이후로는 절대 네트워크를 통해 전송되지 않습니다.

---

## 2단계: Supabase 클라이언트의 역할

#### Supabase 클라이언트란?

- Supabase 클라이언트는 응용프로그램 코드에 포함되어 있는 JavaScript 라이브러리입니다. 이것은 개발자가 npm이라는 도구를 통해 프로젝트에 추가한 코드 모음입니다. 이 클라이언트는 브라우저나 컴퓨터에 별도로 설치되는 것이 아니라, 웹페이지의 JavaScript 코드 일부로서 페이지를 방문할 때마다 다운로드되고 실행됩니다.

- Supabase 클라이언트는 **브라우저, 응용프로그램 서버, 엣지 함수 등 Supabase와 통신해야 하는 모든 곳에서 생성**됩니다. 다만 Supabase 서버 자체는 클라이언트를 생성하지 않습니다. Supabase 서버는 요청을 받는 쪽이므로, 자기 자신에게 연결하기 위한 클라이언트는 필요 없습니다. 마치 전화기에 비유하자면, 전화를 거는 쪽(브라우저, 응용프로그램 서버)은 전화기가 필요하지만, 전화를 받는 교환대(Supabase 서버)는 전화기가 필요 없는 것과 같습니다.

#### 클라이언트의 생성과 소멸

- 사용자가 웹페이지를 방문하면, 응용프로그램 서버는 HTML과 JavaScript 파일을 브라우저에 보냅니다. 이 JavaScript 안에 Supabase 클라이언트 코드가 포함되어 있고, 브라우저가 이 코드를 실행하면서 컴퓨터의 RAM(메모리)에 Supabase 클라이언트 객체가 생성됩니다.

- 이는 마치 워드 프로그램을 실행하면 메모리에 워드 프로세스가 로드되는 것과 같습니다. 프로그램을 종료하면 메모리에서 사라지듯이, 페이지를 닫거나 이동하면 Supabase 클라이언트 객체도 메모리에서 사라집니다.

- 서버 측에서도 마찬가지입니다. 사용자가 페이지를 요청할 때마다 서버는 새로운 Supabase 클라이언트 객체를 메모리에 생성합니다. 요청 처리가 끝나면 이 객체는 자동으로 메모리에서 사라집니다. 즉, Supabase 클라이언트는 영구적으로 존재하는 것이 아니라 필요할 때마다 만들어졌다가 사라지는 임시 도구입니다.

#### 클라이언트가 하는 일

- Supabase 클라이언트는 일종의 전화기와 같습니다. 전화기 자체에는 통화 내용이 저장되지 않지만, 상대방과 대화하기 위한 도구 역할을 합니다.

- 클라이언트가 생성되면 가장 먼저 브라우저의 쿠키 저장소를 확인합니다. 거기서 액세스 토큰과 리프레시 토큰을 찾아 읽어옵니다. 그러나 클라이언트는 이 토큰들을 자신의 내부에 저장하지 않습니다. 단지 필요할 때마다 쿠키에서 읽어와 사용할 뿐입니다.

- 사용자 정보를 조회하거나 데이터베이스에서 데이터를 가져와야 할 때, 클라이언트는 쿠키에서 액세스 토큰을 읽어와 HTTP 요청의 Authorization 헤더에 포함시킵니다. 그리고 이 요청을 Supabase 서버로 보냅니다. Supabase 클라이언트는 토큰을 저장하는 금고가 아니라, 토큰을 읽어서 전달하는 우편배달부 같은 역할입니다.

---

## 3단계: 페이지 접속 시 인증 확인

#### 서버 측 렌더링(SSR)에서의 인증

- 사용자가 대시보드 같은 보호된 페이지에 접속하려고 할 때, 브라우저는 응용프로그램 서버로 요청을 보냅니다. 이때 브라우저는 자동으로 쿠키에 저장된 토큰들을 HTTP 요청 헤더에 포함시킵니다. 개발자가 코드를 작성하지 않아도 브라우저가 알아서 처리하는 것입니다.

- 응용프로그램 서버는 이 요청을 받으면 즉시 Supabase 클라이언트 객체를 생성합니다. 클라이언트는 요청에 포함된 쿠키에서 액세스 토큰을 추출합니다. 그리고 이 토큰을 Supabase 서버로 보내면서 "이 사용자가 누구인지 확인해주세요"라고 요청합니다.

#### SSR의 정확한 의미

- SSR(Server-Side Rendering)은 전체 인증 시스템을 의미하는 것이 아니라, **서버가 페이지를 미리 완성해서 브라우저에 보내는 방식**을 의미합니다. 사용자가 새로운 페이지로 이동할 때(URL이 바뀔 때)마다 SSR이 작동할 수 있습니다.

- **SSR 방식**에서는 사용자가 페이지를 요청하면, 서버가 먼저 인증을 확인하고 데이터를 조회한 다음, 완성된 HTML을 생성하여 브라우저에 보냅니다. 브라우저는 즉시 완성된 화면을 표시할 수 있어 깜빡임이 없고 빠릅니다.

- **CSR(Client-Side Rendering) 방식**에서는 서버가 비어있는 HTML과 JavaScript만 보내고, 브라우저가 JavaScript를 실행하면서 인증을 확인하고 데이터를 조회합니다. 이 과정에서 로딩 화면이나 깜빡임이 발생할 수 있습니다.

- SSR은 홈페이지, 상품 목록, 상품 상세, 대시보드, 프로필 같은 거의 모든 페이지 이동 시 작동할 수 있습니다. 반면 로그인 버튼 클릭, 좋아요 버튼, 장바구니 담기 같은 페이지 내의 작은 액션은 SSR이 아니라 API 호출로 처리됩니다. 이런 경우는 페이지 전체를 새로 로드하지 않고 일부만 업데이트하므로 더 부드러운 경험을 제공합니다.

#### Supabase의 토큰 검증 원리

- Supabase 서버가 토큰을 검증하는 과정은 여러 단계의 보안 검사로 이루어져 있습니다.

- **1단계: 형식 검증**
	- Supabase는 받은 토큰이 올바른 JWT 형식인지 먼저 확인합니다. JWT는 헤더, 페이로드, 서명의 세 부분으로 구성되어야 하며, 각 부분은 Base64로 인코딩되어 있어야 합니다. 형식이 맞지 않으면 즉시 거부됩니다.

- **2단계: 서명 검증 (가장 중요)**
	- JWT의 세 번째 부분인 서명은 토큰의 진위를 증명하는 암호화된 봉인입니다. Supabase는 토큰을 발급할 때 비밀 키(Secret Key)를 사용하여 서명을 생성합니다. 이 비밀 키는 오직 Supabase 서버만 알고 있습니다.

	- 검증 과정은 이렇습니다. Supabase가 토큰의 헤더와 페이로드를 가져와서 비밀 키로 다시 서명을 계산합니다. 계산한 서명과 토큰에 포함된 서명을 비교합니다. 일치하면 이 토큰이 정말 Supabase가 발급한 것임을 확인할 수 있습니다. 불일치하면 누군가 토큰을 위조하거나 변조한 것이므로 즉시 거부합니다.

	- 이는 마치 서명 감정과 같습니다. 원본 서명과 제출된 서명을 비교하여 진위를 가리는 것입니다. 비밀 키 없이는 올바른 서명을 만들 수 없으므로, 아무리 토큰을 복사하거나 내용을 바꾸려 해도 서명이 맞지 않아 실패합니다.

- **3단계: 만료 시간 검증**
	- 토큰의 페이로드에는 만료 시각을 나타내는 필드가 있습니다. Supabase는 현재 시각과 이 만료 시각을 비교합니다. 현재 시각이 만료 시각을 지났다면, 아무리 서명이 유효해도 토큰을 거부합니다. 이는 호텔 카드키의 유효기간이 지나면 사용할 수 없는 것과 같습니다.

- **4단계: 세션 존재 확인**
	- Supabase는 자신의 데이터베이스에 있는 세션 테이블을 확인합니다. 토큰에 포함된 세션 ID가 이 테이블에 실제로 존재하는지 확인하는 것입니다.

	- 사용자가 로그아웃하면 Supabase는 이 테이블에서 해당 세션을 삭제합니다. 따라서 누군가 로그아웃 전에 토큰을 복사해뒀다 하더라도, 로그아웃 후에는 세션이 삭제되어 토큰이 무효화됩니다. 이를 통해 "즉시 로그아웃" 기능이 가능해집니다.

- **5단계: 사용자 정보 추출**
	- 모든 검증을 통과하면, Supabase는 토큰의 페이로드에서 사용자 정보를 추출합니다. 사용자 ID, 이메일 주소, 권한 정보가 담겨 있습니다.

	- Supabase는 이 정보를 응용프로그램 서버에 반환합니다. 응용프로그램 서버는 이 정보를 바탕으로 페이지를 생성합니다.

#### 검증 실패 시 처리

- 만약 검증의 어느 단계에서라도 실패하면, Supabase는 에러 응답을 보냅니다. 응용프로그램 서버는 이를 받아 사용자를 로그인 페이지로 리디렉션합니다. 페이지를 렌더링하지 않고 즉시 리디렉션 명령을 브라우저에 보내므로, 사용자는 인증되지 않은 콘텐츠를 단 한 순간도 보지 않게 됩니다.

#### 토큰 자동 갱신의 세부 과정

- 액세스 토큰은 1시간 후에 만료됩니다. 사용자가 웹사이트를 1시간 이상 사용하고 있다면 어떻게 될까요? Supabase 클라이언트는 이 상황을 자동으로 처리합니다.

- **갱신이 필요한 순간 감지**
	- 사용자가 페이지를 이동하거나 데이터를 요청하면, 클라이언트는 먼저 액세스 토큰으로 요청을 시도합니다. Supabase 서버가 "토큰이 만료되었습니다"라는 에러를 반환하면, 클라이언트는 즉시 갱신 절차를 시작합니다.

- **리프레시 토큰으로 새 토큰 발급**
	- 클라이언트는 쿠키에서 리프레시 토큰을 읽어와 Supabase 서버에 보냅니다. Supabase는 리프레시 토큰을 검증합니다. 리프레시 토큰도 액세스 토큰과 같은 방식으로 서명 검증, 만료 시간 확인, 세션 존재 확인을 거칩니다. 유효하다면 Supabase는 새로운 액세스 토큰과 새로운 리프레시 토큰을 생성합니다.

- **새 토큰의 쿠키 저장**
	- Supabase는 다시 Set-Cookie 헤더를 응답에 포함시켜 브라우저에 "새 토큰들을 쿠키에 저장하세요"라고 명령합니다. 브라우저는 이 명령에 따라 쿠키의 내용을 새 토큰으로 덮어씁니다.

- **원래 요청 재시도**
	- 클라이언트는 이제 새 액세스 토큰으로 원래 하려던 요청을 다시 시도합니다. 이번에는 토큰이 유효하므로 성공적으로 처리됩니다.

	- 이 모든 과정은 0.1~0.2초 정도에 이루어지며 사용자는 전혀 인지하지 못합니다. 그저 서비스가 계속 작동하는 것처럼 느껴질 뿐입니다. 화면에는 아무런 로딩 표시나 에러 메시지가 나타나지 않습니다.

---

## 4단계: 데이터 조회와 수정

#### 일반적인 데이터 요청

- 로그인 후 사용자가 자신의 주문 내역을 보고 싶어한다고 가정해봅시다. 브라우저 또는 서버에서 Supabase 클라이언트가 데이터베이스 조회를 요청하면, 클라이언트는 다시 쿠키에서 액세스 토큰을 읽어와 HTTP 요청에 포함시킵니다.

- Supabase 서버는 이 토큰을 앞서 설명한 5단계 검증 과정으로 확인합니다. 토큰에서 사용자 ID를 추출하고, 데이터베이스에서 해당 사용자의 주문 내역만 조회하여 반환합니다.

#### Row Level Security (행 수준 보안)

- 여기서 중요한 것은 **Row Level Security**라는 보안 정책입니다. 이것은 데이터베이스 자체에 설정된 규칙으로, "각 사용자는 자신의 데이터만 볼 수 있다"는 제약을 데이터베이스 수준에서 강제합니다.

- 악의적인 사용자가 다른 사람의 user_id로 쿼리를 보내려 해도, 데이터베이스는 토큰에서 추출한 실제 user_id와 비교하여 일치하지 않으면 데이터를 반환하지 않습니다. 응용프로그램 코드에 버그가 있어도 데이터베이스가 최후의 방어선 역할을 하는 것입니다.

#### Cross-Origin 문제와 해결

- 웹 보안에는 중요한 규칙이 하나 있습니다. 브라우저는 기본적으로 한 웹사이트의 JavaScript가 다른 웹사이트의 서버로 요청을 보내는 것을 차단합니다. 이를 **Same-Origin Policy**(동일 출처 정책)라고 합니다.

- **출처(Origin)의 정확한 의미**
	- "출처"라는 용어가 어색하게 느껴질 수 있지만, 이것은 **"브라우저가 현재 보고 있는 페이지의 도메인"**을 의미합니다. 브라우저 주소창에 표시된 URL의 도메인이 바로 출처입니다. 데이터가 어디서 오는지가 아니라, 요청을 보내는 코드가 어느 웹사이트에 속해 있는지를 나타냅니다.

	- 출처는 프로토콜, 도메인, 포트의 조합입니다. 예를 들어 `https://myshop.com:443`이 하나의 출처이며, 이 세 가지 중 하나라도 다르면 다른 출처로 간주됩니다.

- **Cross-Origin의 정확한 의미**
	- Cross-Origin은 "현재 브라우저가 보고 있는 페이지의 도메인과 요청 대상 서버의 도메인이 다른 상황"을 의미합니다. 요청하는 주체(브라우저)와 응답하는 주체(서버)가 다른 것이 아니라, **현재 페이지의 도메인과 요청 대상 도메인이 다른지**를 비교하는 것입니다.

	- 예를 들어, 브라우저 주소창이 `https://myshop.com/products`라면, 현재 출처는 `myshop.com`입니다. 이 페이지의 JavaScript가 `https://myshop.com/api/cart`로 요청을 보낸다면, 현재 출처와 요청 대상이 모두 `myshop.com`이므로 **Same-Origin**입니다. 하지만 `https://api.supabase.co/rest/v1/data`로 요청을 보낸다면, 현재 출처(`myshop.com`)와 요청 대상(`api.supabase.co`)이 다르므로 **Cross-Origin**입니다.

- **언제 Cross-Origin 문제가 발생하나**
	- Cross-Origin 문제는 오직 **브라우저가 현재 페이지의 도메인과 다른 도메인의 서버에 직접 요청을 보낼 때만** 발생합니다. 서버 간 통신에는 이러한 제약이 없습니다. 브라우저만의 보안 규칙이기 때문입니다.

	- SSR 방식에서는 Cross-Origin 문제가 발생하지 않습니다. 브라우저는 응용프로그램 서버(`myshop.com`)에만 요청을 보내고, 서버가 Supabase와 통신하기 때문입니다. 브라우저 입장에서는 같은 도메인과만 통신하는 것입니다.

	- CSR 방식에서 브라우저가 Supabase에 직접 요청을 보낼 때만 Cross-Origin 문제가 발생합니다.

- **보안상의 이유**
	- 브라우저가 이런 제약을 두는 이유는 보안 때문입니다. 만약 제약이 없다면, 사용자가 은행 사이트에 로그인한 상태에서 악의적인 웹사이트를 방문했을 때, 그 사이트의 JavaScript가 사용자 몰래 은행 사이트로 송금 요청을 보낼 수 있습니다. 브라우저는 자동으로 은행 사이트의 로그인 쿠키를 포함시키므로 송금이 성공하게 됩니다.

	- Cross-Origin 정책이 있으면, 악의적인 사이트(`evil.com`)의 JavaScript가 은행 사이트(`bank.com`)로 요청을 보내려 할 때 브라우저가 차단합니다. "현재 출처는 `evil.com`인데 `bank.com`으로 요청을 보내려고 하네? 다른 출처야! 차단!"

- **CORS의 역할**
	- 이 문제를 해결하기 위해 **CORS(Cross-Origin Resource Sharing)** 메커니즘이 있습니다. Supabase 서버는 응답에 특별한 헤더를 포함시킵니다. 이 헤더들은 브라우저에게 "특정 출처에서 오는 요청은 허용합니다. 인증 정보(쿠키)도 포함할 수 있습니다"라고 알려줍니다.

- **프리플라이트 요청**
	- 특히 민감한 요청의 경우, 브라우저는 실제 요청 전에 **프리플라이트(Preflight) 요청**을 먼저 보냅니다. 이는 OPTIONS 메서드를 사용하는 사전 확인 요청으로, "이런 요청을 보내도 되나요?"라고 먼저 물어보는 것입니다.

	- 브라우저가 프리플라이트 요청을 보내면, Supabase는 "이 출처의 이런 메서드는 허용합니다"라고 응답합니다. 브라우저는 허락을 받았으므로 실제 요청을 전송합니다.

	- Supabase 프로젝트 설정에서 허용할 도메인 목록을 지정하면, Supabase는 해당 도메인에서 오는 요청만 처리합니다. 이렇게 해서 서로 다른 도메인 간의 안전한 통신이 가능해집니다.

---

## 엣지 서버와 엣지 함수

#### 엣지 서버의 개념

- 지금까지 설명한 응용프로그램 서버는 보통 한 곳에만 배포됩니다. 예를 들어 서울의 데이터센터에만 서버가 있다면, 뉴욕에 있는 사용자가 접속할 때는 물리적 거리 때문에 느려질 수밖에 없습니다. 데이터가 태평양을 건너야 하기 때문입니다.

- **엣지 서버**는 이 문제를 해결하기 위해 전 세계 주요 도시에 분산 배치된 서버들입니다. 서울, 도쿄, 싱가포르, 런던, 뉴욕, 샌프란시스코 등 수십 개 도시에 동일한 서버가 복사되어 있습니다.

- 사용자가 웹사이트에 접속하면, **Anycast DNS**라는 특수한 시스템이 사용자의 위치를 파악하고 가장 가까운 엣지 서버의 주소를 알려줍니다. 이는 GPS와 비슷한 원리로 작동하며, 네트워크 경로를 분석하여 물리적으로 가장 가까운 서버를 찾아줍니다.

- 서울 사용자는 서울 엣지 서버로, 뉴욕 사용자는 뉴욕 엣지 서버로 자동으로 연결됩니다. 같은 웹사이트 주소지만 실제로는 다른 물리적 서버에 접속하는 것입니다. 사용자는 이 과정을 전혀 알 수 없으며, 그저 웹사이트가 빠르다고만 느낍니다.

#### 엣지 함수란?

- 그런데 응용프로그램 전체를 엣지 서버에 배포하는 것은 불가능합니다. 전체 앱은 수백 메가바이트의 크기를 가지고 있고, 많은 라이브러리와 복잡한 의존성을 가지고 있기 때문입니다. 엣지 서버는 크기 제한(보통 1~10MB), 실행 시간 제한(30초~150초), 메모리 제한(128MB~512MB) 같은 엄격한 제약이 있습니다.

- **엣지 함수**는 응용프로그램에서 특정 기능만 떼어내어 엣지 서버에 배포한 것입니다. 마치 본사에서는 모든 업무를 처리하지만, 각 도시의 지점에서는 고객 상담 같은 단순한 업무만 처리하는 것과 비슷합니다.

#### 엣지 함수의 생성 과정

- 개발자는 프로젝트 내에 특별한 폴더를 만들고, 그 안에 엣지 함수로 사용할 코드를 작성합니다. 예를 들어 이메일 발송, 이미지 리사이즈, SMS 알림, 쿠폰 검증 같은 기능들입니다. 이런 기능들의 공통점은 독립적이고, 단순하며, 빠른 응답이 중요하다는 것입니다.

- 이 코드는 일반 응용프로그램 코드와는 다른 형식으로 작성됩니다. Deno라는 특수한 JavaScript 런타임을 사용하며, 파일 크기가 보통 수 킬로바이트에서 수십 킬로바이트로 매우 작습니다. 외부 라이브러리 사용도 최소화하여 가볍게 유지합니다.

- 개발자가 배포 명령을 실행하면, Supabase는 이 코드를 전 세계 모든 엣지 서버에 동시에 배포합니다. 서울, 도쿄, 싱가포르, 런던, 뉴욕 등 수십 개 도시의 서버에 동일한 코드가 복사되는 것입니다. 배포는 보통 수 초 내에 완료되며, 배포가 끝나면 즉시 모든 엣지 서버에서 함수가 실행 가능한 상태가 됩니다.

#### Supabase URL과 Anon 키의 역할

- 엣지 함수가 Supabase 데이터베이스나 인증 시스템과 통신하려면 두 가지 정보가 필요합니다.

- **Supabase URL**은 프로젝트의 고유한 주소입니다. 이것은 해당 프로젝트의 데이터베이스와 서비스가 어디에 있는지 가리킵니다. 각 Supabase 프로젝트는 고유한 URL을 가지며, 이것으로 여러 프로젝트를 구분합니다.

- **Anon 키**(Anonymous Key)는 공개 API 키입니다. 이것은 프로젝트 설정에서 확인할 수 있으며, 브라우저의 JavaScript 코드에도 포함되어 있습니다. "나는 이 프로젝트에 접근하려고 합니다"라는 신분증 같은 것입니다.

- 중요한 점은 Anon 키는 공개되어도 안전하다는 것입니다. 이 키만으로는 데이터를 마음대로 조작할 수 없습니다. 실제 권한은 액세스 토큰으로 결정되기 때문입니다. Anon 키는 단지 "어느 프로젝트인지"를 식별할 뿐이며, Row Level Security 정책이 여전히 적용되므로 각 사용자는 자신의 데이터만 볼 수 있습니다.

- 반면 Service Role 키라는 것도 있는데, 이것은 모든 보안 정책을 우회할 수 있는 관리자 키입니다. 이 키는 절대 브라우저 코드에 포함되어서는 안 되며, 서버의 환경 변수에만 저장되어야 합니다.

- 엣지 함수는 환경 변수에 저장된 Supabase URL과 Anon 키를 사용하여 Supabase 클라이언트를 생성하고, 그것으로 데이터베이스와 통신합니다.

#### 엣지 함수의 작동 방식

- 사용자가 주문을 완료하는 전체 과정을 살펴보겠습니다.

- **1단계: 주문 처리 (메인 앱)**
	- 사용자가 "주문하기" 버튼을 클릭하면, 브라우저는 응용프로그램 서버로 요청을 보냅니다. 서울에 있는 서버가 요청을 받아 복잡한 주문 처리 로직을 실행합니다. 재고 확인, 할인 계산, 배송비 계산, 결제 처리, 데이터베이스에 주문 저장 등의 작업을 수행합니다.

- **2단계: 엣지 함수 호출**
	- 주문 처리가 완료되면, 서버는 "이제 고객에게 주문 완료 이메일을 보내야겠다"고 판단합니다. 이때 서버는 메인 앱 내에서 직접 이메일을 보내지 않고, 엣지 함수를 호출합니다.

	- DNS가 가장 가까운 엣지 서버를 찾아주므로, 이 요청은 서울 엣지 서버로 라우팅됩니다. 서울 엣지 서버에는 이메일 발송 함수가 배포되어 있습니다.

- **3단계: 외부 API 호출**
	- 엣지 함수가 실행되면, 이 함수는 **외부 API**를 호출합니다. 외부 API는 우리 서버에 저장되어 있는 것이 아닙니다. SendGrid나 Mailgun 같은 이메일 전문 회사의 서버에 있는 기능입니다.

	- 외부 API는 다른 회사나 서비스가 제공하는 기능을 인터넷을 통해 사용하는 것입니다. 우리가 직접 그 기능을 개발하거나 저장하는 것이 아니라, 전문 회사의 서비스를 빌려 쓰는 것입니다. 비유하자면, 내부 API는 우리 집 주방에서 요리하는 것이고, 외부 API는 배달 앱으로 음식을 주문하는 것입니다.

	- 엣지 함수는 인터넷을 통해 SendGrid 서버에 요청을 보냅니다. SendGrid API 키는 엣지 함수의 환경 변수에 저장되어 있습니다. 이 키는 "나는 SendGrid 고객입니다"라는 증명서이며, 이것으로 SendGrid의 이메일 발송 서비스를 이용할 수 있습니다.

	- SendGrid 서버가 실제로 이메일을 고객에게 발송하고, "발송 완료" 응답을 엣지 함수에 반환합니다. 엣지 함수는 이 결과를 응용프로그램 서버에 전달하고, 서버는 최종적으로 브라우저에 "주문이 완료되었습니다" 메시지를 표시합니다.

	- 외부 API 사용 시 필요한 것은 API 키(그 서비스의 고객임을 증명하는 비밀번호), API 문서(어떻게 요청을 보내야 하는지 설명서), 비용(사용량에 따라 해당 회사에 지불)입니다. 우리가 저장하는 것은 API 키뿐이고, 실제 기능은 항상 그 회사의 서버에 있습니다.

#### 엣지의 장점과 한계

- **속도 개선**
	- 전통적인 방식이라면 서울 사용자의 요청이 미국 본사 서버까지 갔다가 돌아와야 하므로 약 300~500밀리초가 걸립니다. 하지만 엣지 서버를 사용하면 서울 엣지에서 처리하므로 약 20~50밀리초면 충분합니다. 10배 이상 빠른 것입니다.

	- 특히 이미지 리사이즈나 간단한 데이터 변환 같은 작업은 엣지에서 처리하면 사용자가 체감할 수 있을 정도로 빨라집니다.

- **제약사항**
	- 다만 엣지 함수는 모든 기능에 적합한 것은 아닙니다. 복잡한 페이지 렌더링(여러 컴포넌트를 조합하고 데이터베이스를 여러 번 조회해야 하는 작업), 장시간 실행 작업(동영상 인코딩, 대용량 데이터 분석 같은 수 분 이상 걸리는 작업), 대용량 파일 처리(100MB 이상의 파일을 메모리에 로드하는 작업), 복잡한 비즈니스 로직(여러 테이블을 트랜잭션으로 수정하는 작업) 같은 것은 여전히 메인 응용프로그램 서버에서 처리해야 합니다.

	- 엣지 함수는 외부 API 호출, 간단한 데이터 변환, 이미지 처리, 웹훅 수신 같은 가볍고 빠른 작업에 적합합니다.

---

## 전체 흐름 요약

사용자가 웹사이트를 이용하는 전체 과정을 시간 순서대로 정리하면 이렇습니다.

#### 로그인 단계 (최초 1회)

- 사용자는 이메일과 비밀번호를 입력합니다. 이 정보는 HTTPS 암호화 연결을 통해 Supabase 서버로 전송됩니다. Supabase는 해시 함수로 비밀번호를 변환하여 데이터베이스의 값과 비교합니다. 검증에 성공하면 JWT 형식의 액세스 토큰과 리프레시 토큰을 생성합니다.

- Supabase는 HTTP 응답의 Set-Cookie 헤더에 이 토큰들을 포함시킵니다. 브라우저는 이 헤더를 읽고 자동으로 쿠키 저장소(컴퓨터 디스크)에 토큰을 저장합니다. HttpOnly와 Secure 속성이 설정되어 JavaScript로 접근할 수 없고 HTTPS에서만 전송됩니다.

- 비밀번호는 이 순간 이후로 절대 다시 전송되지 않습니다. 모든 후속 요청은 토큰만 사용합니다.

#### 페이지 접속 단계 (매번 반복)

- 사용자가 다른 페이지로 이동하면, 브라우저는 자동으로 쿠키의 토큰을 HTTP 요청 헤더에 포함시킵니다. 응용프로그램 서버는 요청을 받으면 Supabase 클라이언트 객체를 메모리에 생성합니다.

- 클라이언트는 요청의 쿠키에서 액세스 토큰을 추출하여 Supabase 서버로 전송합니다. Supabase는 5단계 검증을 수행합니다: 형식 검증, 서명 검증(비밀 키로 위조 여부 확인), 만료 시간 확인, 세션 존재 확인, 사용자 정보 추출.

- 검증을 통과하면 Supabase는 사용자 정보를 반환하고, 서버는 이 정보를 바탕으로 페이지를 생성하여 브라우저에 보냅니다. 검증 실패 시 즉시 로그인 페이지로 리디렉션합니다.

#### 데이터 작업 단계

- 사용자가 데이터를 조회하거나 수정할 때도 같은 방식입니다. 클라이언트가 쿠키에서 토큰을 읽어 전송하고, Supabase가 검증합니다. Row Level Security 정책이 적용되어 각 사용자는 자신의 데이터만 접근할 수 있습니다.

- 브라우저가 현재 페이지의 도메인과 다른 도메인의 서버에 직접 요청을 보내는 경우(Cross-Origin), 브라우저는 먼저 프리플라이트 요청을 보내 허가를 받습니다. Supabase는 CORS 헤더로 허용된 도메인을 명시하여 안전한 통신을 보장합니다. SSR 방식에서는 브라우저가 응용프로그램 서버와만 통신하고 서버가 Supabase와 통신하므로 Cross-Origin 문제가 발생하지 않습니다.

#### 엣지 함수 활용 단계

- 특정 작업(이메일 발송, 알림 전송, 이미지 처리 등)이 필요하면, 응용프로그램은 가장 가까운 엣지 서버의 엣지 함수를 호출합니다. DNS가 사용자 위치를 파악하여 최적의 엣지 서버를 선택합니다.

- 엣지 함수는 Supabase URL과 Anon 키로 Supabase 클라이언트를 생성하고, 필요하면 외부 API(SendGrid, Twilio 등)를 호출합니다. 외부 API는 전문 회사의 서버에 있는 기능으로, API 키로 접근합니다. 처리가 완료되면 결과를 빠르게 반환합니다.

#### 토큰 갱신 단계 (자동)

- 시간이 지나 액세스 토큰이 만료되면, Supabase 클라이언트가 자동으로 감지합니다. 클라이언트는 쿠키에서 리프레시 토큰을 읽어 Supabase에 새 토큰 발급을 요청합니다.

- Supabase는 리프레시 토큰을 검증하고, 유효하면 새로운 액세스 토큰과 리프레시 토큰을 생성합니다. Set-Cookie 헤더로 브라우저에게 쿠키 업데이트를 명령하고, 브라우저는 자동으로 새 토큰으로 쿠키를 덮어씁니다.

- 클라이언트는 새 토큰으로 원래 요청을 재시도합니다. 이 모든 과정은 0.1~0.2초에 이루어지며 사용자는 전혀 느끼지 못합니다. 화면에는 어떤 표시도 나타나지 않고 서비스가 끊김없이 계속됩니다.

---

## 보안 원칙 정리

이 모든 시스템은 몇 가지 핵심 보안 원칙을 따릅니다.

**최소 권한 원칙**: 각 주체는 자신의 역할에 필요한 최소한의 권한만 가집니다. 브라우저는 토큰을 저장만 하고, 클라이언트는 토큰을 전달만 하고, Supabase만 토큰을 검증합니다.

**심층 방어**: 여러 겹의 보안 장치가 있습니다. HTTPS 암호화, HttpOnly 쿠키, 토큰 서명 검증, 만료 시간, 세션 확인, Row Level Security가 모두 함께 작동합니다.

**토큰 기반 인증**: 비밀번호는 단 한 번만 전송되고, 이후 모든 요청은 토큰으로 처리됩니다. 토큰은 짧은 수명을 가지며 자동 갱신됩니다.

**분산 저장**: 토큰은 각 사용자의 브라우저에, 데이터는 Supabase 데이터베이스에, 코드는 응용프로그램 서버와 엣지 서버에 분리되어 저장됩니다. 한 곳이 침해되어도 전체 시스템이 무너지지 않습니다.

이렇게 역할을 분리하고 토큰 기반 인증을 사용함으로써, 안전하면서도 빠르고 확장 가능한 현대적 웹 서비스가 가능해집니다.