---
date: 2026-05-06
tags:
  - HTML
  - markup
contexts:
  - Knowledge
index:
  - Vibe Coding
summary: 마크업 언어란 본문 텍스트와 구별되는 특별한 기호(태그)를 사용해, 그 텍스트의 구조나 의미, 표현 방식을 표시해 주는 언어 체계이다.
author:
people:
YouTube_url:
scr_url:
---
## 마크업(Markup)의 어원

"마크업(markup)"이라는 단어는 사실 컴퓨터가 등장하기 훨씬 이전, 출판·인쇄 업계에서 먼저 사용되던 용어입니다. 편집자가 원고를 인쇄소에 넘기기 전에 빨간 펜으로 여백에 "여기는 굵게", "이 부분은 제목으로 크게", "이 줄은 들여쓰기" 같은 지시 사항을 적어 넣곤 했는데, 이렇게 원고 위에 **표시(mark)를 덧붙이는(up) 작업**을 마크업이라고 불렀습니다. 즉, 본문 내용 자체가 아니라 "이 내용을 어떻게 다루어야 하는지"를 알려주는 부가 표시가 마크업의 본질입니다.

## 마크업 언어의 의미

이 개념을 컴퓨터 문서에 그대로 옮겨온 것이 바로 마크업 언어입니다. 마크업 언어란 **본문 텍스트와 구별되는 특별한 기호(태그)를 사용해, 그 텍스트의 구조나 의미, 표현 방식을 표시해 주는 언어 체계**입니다. 일반적인 프로그래밍 언어가 "어떤 동작을 수행하라"고 컴퓨터에 명령을 내린다면, 마크업 언어는 "이 글의 이 부분은 제목이고, 저 부분은 목록이며, 여기는 강조해야 한다"고 문서의 구조를 서술하는 데 초점이 있습니다.

## 구체적인 예시

가장 익숙한 예시인 HTML로 살펴보면 이해가 쉽습니다. 다음과 같은 문장이 있다고 가정해 보겠습니다.

```html
<h1>오늘의 날씨</h1>
<p>서울은 <strong>맑음</strong>입니다.</p>
```

여기서 실제 사람이 읽을 내용은 "오늘의 날씨", "서울은 맑음입니다" 두 줄뿐입니다. 그런데 그 주변을 감싸고 있는 `<h1>`, `<p>`, `<strong>` 같은 기호들이 바로 **마크업**입니다. 각각 "이것은 큰 제목이다", "이것은 하나의 문단이다", "이 단어는 강조하라"는 의미를 담고 있지요. 본문 내용에 의미와 구조를 입혀주는 일종의 라벨인 셈입니다.

마크다운(Markdown)이라는 더 가벼운 마크업 언어에서는 같은 의도를 `# 오늘의 날씨`나 `**맑음**`처럼 더 단순한 기호로 표현하지만, "본문에 표시를 덧붙여 의미를 부여한다"는 핵심 원리는 동일합니다.

## 마크업 언어와 프로그래밍 언어의 차이

마크업 언어는 흔히 프로그래밍 언어와 혼동되지만, 성격이 분명히 다릅니다. 프로그래밍 언어는 변수와 조건문, 반복문 등을 통해 **연산과 동작을 기술**하는 도구인 반면, 마크업 언어에는 보통 그런 실행 흐름이 없고 **문서의 구조를 정적으로 묘사**하는 역할만 합니다. 비유하자면 프로그래밍 언어가 "요리하는 방법을 적은 레시피"라면, 마크업 언어는 "이 글에서 어디가 제목이고 어디가 부제인지 표시해 둔 편집 지시서"에 가깝습니다.

정리하자면 사용자님이 궁금해하신 "마크업"은 **본문 위에 덧붙여 그 내용에 구조나 의미를 부여하는 표시**를 뜻하고, 마크업 언어는 그러한 표시를 일정한 규칙(태그·기호 체계)으로 정형화하여 컴퓨터가 해석할 수 있게 만든 언어라고 이해하시면 됩니다.

## LLM이 다루는 것은 결국 "텍스트"이다

LLM 시대에 마크다운이 부상한 근본적인 이유는 LLM이 처리하는 단위가 토큰(token), 즉 텍스트 조각이라는 점에 있습니다. 모델은 이미지나 표를 시각적으로 보지 않고, 모든 입력을 일렬로 늘어선 문자의 흐름으로 받아들입니다. 그런데 사람이 다루는 정보는 단순한 평문 텍스트만 있는 게 아니라 제목, 목록, 강조, 표, 인용 같은 **구조**를 가지고 있지요.

여기서 마크다운이 가진 독특한 위치가 빛을 발합니다. 마크다운은 ==그 자체로 평범한 텍스트이면서도 구조 정보를 담을 수 있는== 거의 유일한 형식입니다. `# 제목`, `**강조**`, `- 목록` 같은 표시가 사람에게도 자연스럽게 읽히고, LLM에게도 별도의 파싱 없이 그대로 이해 가능한 토큰으로 전달됩니다. HTML이라면 `<h1>제목</h1>` 같은 장황한 태그를 모델이 일일이 처리해야 하지만, 마크다운은 `# 제목` 한 줄이면 끝납니다.