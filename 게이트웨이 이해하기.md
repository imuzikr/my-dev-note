---
date: 2026-01-07
tags:
  - HTML
  - DOM
  - API
  - 서버
  - 게이트웨이
  - Gateway
  - 도커
  - Docker
  - SSL
  - HTTPS
type:
contexts:
  - Knowledge
index:
  - Vibe Coding
YouTube:
scr_url:
scr:
---
# 웹 서비스 구조 완전 정리

## 1️⃣ 웹 서비스의 기본 출발점

가장 단순한 구조는 이것입니다.

```
사용자(브라우저) → 서버
```

브라우저가 요청을 보내면 서버가 응답합니다. 소규모 프로젝트라면 이 구조만으로도 충분합니다.

---

## 2️⃣ 서비스가 커지면 생기는 변화

기능이 늘어나면 서버 하나로 감당하기 어려워집니다. 예를 들어 AI 서비스라면 채팅 기능, 파일 업로드 기능, 모델 연산 기능이 필요한데, 이 기능들은 성격이 전혀 다릅니다. 그래서 내부적으로 서버를 나눕니다.

```
채팅 서버
파일 서버
모델 서버
```

이렇게 나누는 이유는 관리가 쉬워지고, 한 기능에 문제가 생겨도 전체가 멈추지 않으며, 필요한 기능만 선택적으로 확장할 수 있기 때문입니다.

---

## 3️⃣ 외부에서는 내부 구조를 모른다

사용자는 단 하나의 주소만 압니다.

```
https://example.com
```

채팅 서버가 몇 개인지, 모델 서버가 어디에 있는지, 내부 포트 번호가 무엇인지는 알 필요도 없고, 알 수도 없습니다. 그래서 중간에 **정문** 역할을 하는 무언가가 필요합니다.

---

## 4️⃣ 그 정문이 바로 게이트웨이(리버스 프록시)

구조는 이렇게 됩니다.

```
브라우저
   ↓
게이트웨이 (정문)
   ↓
채팅 서버 / 파일 서버 / 모델 서버
```

게이트웨이는 요청을 먼저 받고, 어느 서버로 보낼지 판단한 뒤, 응답을 다시 브라우저에 돌려줍니다. 즉, **내부 서버들을 대신해서 외부와 소통하는 창구**입니다.

> **용어 참고**: 실무에서 "게이트웨이"라고 부르는 것은 상황에 따라 두 가지로 나뉩니다.
> 
> - **리버스 프록시** (Nginx, Traefik 등): 요청을 내부 서버로 전달하는 역할. 이 문서에서 설명하는 "정문"이 바로 이것입니다.
> - **API 게이트웨이** (Kong, AWS API Gateway 등): 리버스 프록시 기능에 더해 인증, 요청 횟수 제한, 정책 관리 등 고수준 기능까지 담당합니다.
> 
> 처음에는 두 개념을 엄밀히 구분하지 않아도 됩니다. "외부와 내부 사이에서 정문 역할을 하는 것"으로 이해하면 충분합니다.

---

## 5️⃣ HTTPS와 SSL/TLS는 어디에 두는가?

### SSL과 TLS, 무엇이 다른가?

흔히 "SSL 인증서"라고 부르지만, 현재 실제로 사용되는 표준은 **TLS(Transport Layer Security)** 입니다. SSL은 이미 사용이 중단된 구버전 프로토콜입니다. 다만 업계 관행상 "SSL 인증서", "SSL 설정"이라는 표현이 여전히 널리 쓰이므로, 다른 자료에서 SSL이라는 단어를 만나도 TLS를 가리키는 것으로 이해하면 됩니다.

### 어디에 인증서를 설치하는가?

보통 이렇게 구성합니다.

```
브라우저 ⇄ (HTTPS 암호화 구간) ⇄ 게이트웨이 → 내부 서버
```

게이트웨이에 SSL/TLS 인증서를 설치하는 이유는 세 가지입니다. 외부와 통신하는 지점이 게이트웨이이기 때문에, 인증서를 한 곳에서만 관리할 수 있기 때문에, 내부 서버마다 개별 설정이 필요 없기 때문입니다.

### 내부 구간은 왜 암호화하지 않아도 되는가?

게이트웨이에서 내부 서버로 가는 구간은 **같은 사설 네트워크(Private Network) 안**에 있습니다. 외부 인터넷을 거치지 않는 내부 통신이므로 HTTP로 처리해도 일반적으로 안전합니다. 보안 요구 수준이 높은 환경에서는 내부 구간도 암호화하기도 하지만, 이는 필요에 따라 선택하는 사항입니다.

즉, **게이트웨이는 보안의 출입구**이기도 합니다.

---

## 6️⃣ 여기서 Docker와 컨테이너가 등장

서버를 여러 개로 나눴다고 생각해봅시다.

- 채팅 서버
- 모델 서버
- 파일 서버
- 게이트웨이

이것들을 그냥 컴퓨터 한 대에 설치할 수도 있습니다. 하지만 다음과 같은 문제가 생깁니다.

- 서로 다른 프로그램 버전이 충돌할 수 있습니다.
- 설치 과정이 복잡합니다.
- 서버를 다른 컴퓨터로 옮길 때 처음부터 다시 설정해야 합니다.

그래서 **컨테이너**를 사용합니다.

---

## 7️⃣ 컨테이너는 무엇인가?

> **컨테이너**: 프로그램과 그 실행 환경을 통째로 묶은 독립 공간

비유하면, 서버는 건물이고 컨테이너는 그 안의 독립된 방입니다. 각 방은 필요한 환경을 스스로 갖추고 다른 방과 섞이지 않습니다.

단, 일반적인 방과 결정적으로 다른 점이 있습니다. **컨테이너는 통째로 다른 건물(서버)로 이사할 수 있습니다.** 내부 환경을 그대로 유지한 채 어디서든 똑같이 실행된다는 것이 컨테이너의 핵심 장점, 즉 **이식성(Portability)** 입니다. 개발 환경에서 잘 돌아가던 것이 운영 서버에서 갑자기 오류가 나는 문제를 컨테이너가 근본적으로 해결해줍니다.

---

## 8️⃣ Docker는 무엇인가?

> **Docker**: 그 "방(컨테이너)"을 만들고 관리하는 도구

Docker는 특정 클라우드나 서버 환경에 종속되지 않습니다. AWS EC2, 일반 서버, 개인 컴퓨터 등 Docker가 설치된 곳이라면 어디서든 동일하게 동작합니다.

---

## 9️⃣ 전체 구조를 Docker로 표현하면

```
클라우드 서버 한 대 (예: AWS EC2)
 ├─ 게이트웨이 컨테이너
 ├─ 채팅 컨테이너
 ├─ 모델 컨테이너
 └─ 파일 컨테이너
```

외부에서는 오직 게이트웨이 컨테이너만 보입니다.

```
브라우저
   ↓  (HTTPS, TLS 적용)
게이트웨이 컨테이너
   ↓  (내부 HTTP 통신, 사설 네트워크)
채팅 / 모델 / 파일 컨테이너
```

---

## 🔟 모든 개념이 연결됩니다

1. 웹은 기본적으로 브라우저와 서버의 통신이다.
2. 기능이 많아지면 서버를 역할별로 나눈다.
3. 외부에는 하나의 출입구(게이트웨이)를 둔다.
4. 보안 암호화(HTTPS/TLS)는 그 출입구에서 처리한다. 내부 구간은 사설 네트워크이므로 별도 암호화 없이 운영할 수 있다.
5. 각 서버는 컨테이너로 독립 실행하면 환경 충돌이 없고 이식성이 높아진다.
6. Docker는 그 컨테이너를 만들고 관리하는 도구다.

---

## 📌 전체 그림 한 번에 보기

```
브라우저
   ↓  (HTTPS / TLS 암호화 구간)
게이트웨이 컨테이너  ← SSL/TLS 인증서 위치
   ↓  (HTTP, 사설 네트워크 내부 통신)
채팅 컨테이너
모델 컨테이너
파일 컨테이너
```

---

## 📝 핵심 개념 요약

|개념|한 줄 설명|
|---|---|
|**게이트웨이**|서비스의 정문. 외부 요청을 내부 서버로 연결한다.|
|**리버스 프록시**|게이트웨이의 구체적인 구현 방식 (Nginx, Traefik 등).|
|**SSL/TLS**|TLS가 현재 표준. 관행적으로 SSL이라 부르는 경우가 많다.|
|**HTTPS**|TLS로 암호화된 HTTP 통신. 인증서는 게이트웨이에 설치한다.|
|**내부 통신**|게이트웨이 ↔ 내부 서버 구간. 사설 네트워크라 HTTP로 운영 가능.|
|**컨테이너**|프로그램과 실행 환경을 묶은 독립 공간. 어디서든 동일하게 실행된다.|
|**Docker**|컨테이너를 만들고 관리하는 도구.|


