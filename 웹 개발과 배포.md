---
date: 2025-11-03
tags:
  - 프론트엔드
  - 백엔드
  - 서버
  - HTML
  - css
  - React
  - 데이터베이스
  - 빌드
  - 웹개발
  - 배포
  - vibe_coding
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---



 > [!summary]
> 웹 애플리케이션은 인터넷 브라우저에서 실행되는 프로그램으로, 프론트엔드, 백엔드, 데이터베이스라는 세 가지 핵심 요소로 구성됩니다. 이를 레스토랑에 비유하면, 프론트엔드는 손님이 직접 경험하는 홀, 백엔드는 주문을 처리하고 음식을 만드는 주방, 데이터베이스는 식재료를 보관하는 창고와 같습니다. 각 요소를 분리하는 이유는 보안, 성능, 유지보수, 확장성을 위함입니다.
> 
> **프론트엔드: 사용자가 보고 만지는 부분**
> 프론트엔드는 사용자의 디바이스(컴퓨터, 스마트폰 등)에서 실행되는 코드로, 사용자가 직접 보고 상호작용하는 모든 것을 담당합니다. 상품 목록 표시, 버튼 클릭 애니메이션, 검색 자동완성, 스크롤 로딩 등의 역할을 합니다. 개발자가 React, Vue 등으로 작성한 코드는 배포 전에 HTML, CSS, 순수 JavaScript 파일로 "빌드"되며, 이 빌드 과정은 개발자의 컴퓨터에서 단 한 번 실행됩니다. 사용자가 웹사이트에 접속하면 서버는 빌드된 이 파일들을 그대로 전달하고, 브라우저는 이 파일들을 다운로드하여 사용자의 디바이스에서 JavaScript를 실행합니다. 즉, 프론트엔드 코드는 전적으로 사용자의 브라우저에서 실행됩니다.
> 
> **백엔드: 보이지 않는 곳에서 일하는 두뇌**
> 백엔드는 개발자가 관리하는 서버에서 24시간 실행되는 애플리케이션 코드로, 데이터 처리, 보안, 비즈니스 규칙 적용 등 핵심 비즈니스 로직을 담당합니다. 사용자 로그인 검증, 권한 확인, 재고 확인, 결제 금액 계산, 주문 정보 저장, 이메일 발송 등이 백엔드의 역할입니다. 백엔드는 물리적 또는 가상의 컴퓨팅 자원인 '서버' 위에서 실행되는 '소프트웨어'입니다. 보안상 중요한 로직은 사용자가 접근할 수 없는 개발자의 서버에서만 실행됩니다.
> 
> **데이터베이스: 모든 정보가 저장되는 창고**
> 데이터베이스는 데이터를 영구적으로 저장하고 관리하는 독립적인 시스템입니다. 사용자 정보, 상품 정보, 주문 내역, 리뷰, 쿠폰 등 모든 정보가 저장됩니다. 데이터를 표 형태로 저장하고 테이블 간 관계를 정의하는 관계형 데이터베이스(SQL, 예: MySQL, PostgreSQL)와, 문서, 키-값 등 다양한 형태로 저장하며 유연한 비관계형 데이터베이스(NoSQL, 예: MongoDB)로 나뉩니다. 데이터베이스는 백엔드와 독립적인 시스템으로, 백엔드가 데이터를 요청하면 제공하고, 새로운 데이터가 생기면 저장하는 역할을 합니다. 백엔드 서버만이 데이터베이스에 접근하도록 설정하여 보안을 강화하는 것이 일반적입니다.
> 
> **세 가지 요소의 상호작용 및 개발-배포 과정**
> 이 세 가지 요소는 긴밀하게 협력하여 하나의 웹 애플리케이션 기능을 구현합니다. 예를 들어, 사용자 로그인 시 프론트엔드에서 입력된 정보를 기본 검증 후 백엔드로 요청하면, 백엔드는 데이터베이스에서 사용자 정보를 조회하여 비밀번호를 검증하고 토큰을 생성한 뒤 결과를 프론트엔드로 응답합니다. 프론트엔드는 이 응답을 받아 화면을 업데이트합니다.
> 
> 웹 애플리케이션 개발 과정은 개발자의 컴퓨터에서 코드를 작성하고(개발), 프론트엔드 코드를 브라우저가 이해하는 형태로 변환(빌드)한 뒤, 빌드된 파일과 백엔드 코드를 서버에 올리는(배포) 순서로 진행됩니다. 배포된 프론트엔드 파일은 정적 파일 호스팅 서비스에서 사용자의 브라우저로 전달되고, 백엔드 애플리케이션과 데이터베이스는 별도의 서버에서 24시간 실행되며 API 요청을 처리합니다. 사용자가 웹사이트에 접속하면 프론트엔드 파일이 다운로드 및 실행되고, 필요에 따라 백엔드 API를 통해 데이터를 요청하여 화면을 완성합니다.
> 
> **현대적인 개발 방식**
> 최근에는 BaaS(Backend as a Service, 예: Supabase, Firebase)를 활용하여 백엔드 개발 부담을 줄이거나, SSR(Server Side Rendering, 예: Next.js)을 통해 서버에서 프론트엔드 코드를 미리 실행하여 초기 로딩 속도와 검색 엔진 최적화를 개선하기도 합니다. 또한, 대규모 서비스에서는 마이크로서비스 아키텍처를 도입하여 백엔드를 여러 개의 작은 서비스로 분리, 관리하기도 합니다.
> 
> 결론적으로, 웹 애플리케이션은 프론트엔드, 백엔드, 데이터베이스가 각자의 역할을 독립적으로 수행하면서도 API를 통해 유기적으로 소통하며 완성되는 복합적인 시스템입니다. 이 구조를 이해하는 것은 웹 서비스의 동작 원리와 개발 효율성, 성능, 보안을 종합적으로 파악하는 데 중요합니다.

---

## 목차

1. [웹 애플리케이션이란 무엇인가](https://claude.ai/chat/773e60b7-3b6c-473b-993a-bbb3d45499e2#%EC%9B%B9-%EC%95%A0%ED%94%8C%EB%A6%AC%EC%BC%80%EC%9D%B4%EC%85%98%EC%9D%B4%EB%9E%80-%EB%AC%B4%EC%97%87%EC%9D%B8%EA%B0%80)
2. [프론트엔드: 사용자가 보고 만지는 부분](https://claude.ai/chat/773e60b7-3b6c-473b-993a-bbb3d45499e2#%ED%94%84%EB%A1%A0%ED%8A%B8%EC%97%94%EB%93%9C-%EC%82%AC%EC%9A%A9%EC%9E%90%EA%B0%80-%EB%B3%B4%EA%B3%A0-%EB%A7%8C%EC%A7%80%EB%8A%94-%EB%B6%80%EB%B6%84)
3. [백엔드: 보이지 않는 곳에서 일하는 두뇌](https://claude.ai/chat/773e60b7-3b6c-473b-993a-bbb3d45499e2#%EB%B0%B1%EC%97%94%EB%93%9C-%EB%B3%B4%EC%9D%B4%EC%A7%80-%EC%95%8A%EB%8A%94-%EA%B3%B3%EC%97%90%EC%84%9C-%EC%9D%BC%ED%95%98%EB%8A%94-%EB%91%90%EB%87%8C)
4. [데이터베이스: 모든 정보가 저장되는 창고](https://claude.ai/chat/773e60b7-3b6c-473b-993a-bbb3d45499e2#%EB%8D%B0%EC%9D%B4%ED%84%B0%EB%B2%A0%EC%9D%B4%EC%8A%A4-%EB%AA%A8%EB%93%A0-%EC%A0%95%EB%B3%B4%EA%B0%80-%EC%A0%80%EC%9E%A5%EB%90%98%EB%8A%94-%EC%B0%BD%EA%B3%A0)
5. [세 가지 요소의 상호작용](https://claude.ai/chat/773e60b7-3b6c-473b-993a-bbb3d45499e2#%EC%84%B8-%EA%B0%80%EC%A7%80-%EC%9A%94%EC%86%8C%EC%9D%98-%EC%83%81%ED%98%B8%EC%9E%91%EC%9A%A9)
6. [개발부터 배포까지의 전체 과정](https://claude.ai/chat/773e60b7-3b6c-473b-993a-bbb3d45499e2#%EA%B0%9C%EB%B0%9C%EB%B6%80%ED%84%B0-%EB%B0%B0%ED%8F%AC%EA%B9%8C%EC%A7%80%EC%9D%98-%EC%A0%84%EC%B2%B4-%EA%B3%BC%EC%A0%95)
7. [서버와 실행 환경 이해하기](https://claude.ai/chat/773e60b7-3b6c-473b-993a-bbb3d45499e2#%EC%84%9C%EB%B2%84%EC%99%80-%EC%8B%A4%ED%96%89-%ED%99%98%EA%B2%BD-%EC%9D%B4%ED%95%B4%ED%95%98%EA%B8%B0)
8. [현대적인 개발 방식](https://claude.ai/chat/773e60b7-3b6c-473b-993a-bbb3d45499e2#%ED%98%84%EB%8C%80%EC%A0%81%EC%9D%B8-%EA%B0%9C%EB%B0%9C-%EB%B0%A9%EC%8B%9D)

---

## 웹 애플리케이션이란 무엇인가
 
웹 애플리케이션은 인터넷 브라우저에서 실행되는 프로그램입니다. 쇼핑몰, 은행 앱, SNS, 이메일 서비스 등이 모두 웹 애플리케이션입니다. 이러한 웹 애플리케이션은 크게 세 가지 핵심 요소로 구성됩니다.

#### 레스토랑에 비유하면

웹 애플리케이션을 레스토랑에 비유하면 이해하기 쉽습니다.

- **프론트엔드는 홀**: 손님이 직접 보고 경험하는 공간입니다. 메뉴판, 인테리어, 테이블, 웨이터가 있습니다.
- **백엔드는 주방**: 손님은 볼 수 없지만 실제로 음식을 만들고 주문을 처리하는 곳입니다.
- **데이터베이스는 식재료 창고**: 모든 재료가 보관되어 있고, 필요할 때 주방에서 꺼내 쓰는 곳입니다.

손님이 스테이크를 주문하면(프론트엔드), 웨이터가 주방에 전달하고(API 요청), 주방에서 창고에서 고기를 꺼내 요리한 후(백엔드 처리와 데이터베이스 조회), 완성된 스테이크를 홀로 내보냅니다(API 응답). 손님은 주방에서 무슨 일이 일어나는지 모르고, 주방도 손님이 누구인지 자세히 알 필요가 없습니다.

#### 왜 세 가지로 나누는가

하나의 완전한 애플리케이션을 세 부분으로 나누는 이유는 명확합니다.

- **보안**: 민감한 정보 처리는 사용자가 볼 수 없는 백엔드와 데이터베이스에서 해야 합니다.
- **성능**: 무거운 작업은 강력한 서버에서, 가벼운 화면 표시는 사용자 디바이스에서 처리합니다.
- **유지보수**: 각 부분을 독립적으로 수정하고 개선할 수 있습니다.
- **확장성**: 필요한 부분만 늘리거나 줄일 수 있습니다.

---

## 프론트엔드: 사용자가 보고 만지는 부분

프론트엔드는 사용자의 디바이스(컴퓨터, 스마트폰, 태블릿)에서 실행되는 코드로, 사용자가 직접 보고 상호작용하는 모든 것을 담당합니다.

#### 프론트엔드가 하는 일

쇼핑몰을 예로 들면 프론트엔드는 다음과 같은 역할을 합니다.

- 상품 목록을 예쁘게 카드 형태로 배치해서 보여줍니다
- 장바구니 담기 버튼을 누르면 버튼 색이 바뀌고 애니메이션이 재생됩니다
- 장바구니 아이콘에 빨간색 배지로 담긴 상품 개수를 표시합니다
- 검색창에 글자를 입력하면 실시간으로 자동완성 제안을 보여줍니다
- 스크롤을 내리면 추가 상품이 자동으로 로딩됩니다
- 모바일에서는 햄버거 메뉴를, 데스크톱에서는 상단 네비게이션 바를 보여줍니다

#### 프론트엔드 개발의 본질

프론트엔드 개발은 **응용 프로그램 중에서 사용자의 디바이스에서 실행될 부분을 개발하는 것**입니다. 개발자는 React, Vue, Angular 같은 도구를 사용해서 코드를 작성하지만, 브라우저는 React 코드를 직접 이해하지 못합니다.

#### 빌드 과정: React에서 브라우저가 이해하는 코드로

개발자가 React로 작성한 코드는 특별한 변환 과정을 거쳐야 합니다. 브라우저는 오직 HTML, CSS, 순수 JavaScript만 이해할 수 있기 때문입니다.

**개발자의 컴퓨터에서 일어나는 일**

1. 개발자가 React로 코드를 작성합니다
2. 배포 전에 "빌드" 명령어를 실행합니다
3. Webpack, Babel 같은 도구가 자동으로 작동합니다
4. React 코드가 순수 JavaScript로 변환됩니다
5. 여러 파일이 하나로 합쳐지고 압축됩니다
6. 최종적으로 HTML, CSS, JavaScript 파일이 생성됩니다

이 빌드 과정은 개발자의 컴퓨터에서 단 한 번만 실행됩니다. 백엔드 서버나 사용자의 브라우저에서 일어나는 일이 아닙니다.

#### 사용자가 웹사이트에 접속하면

1. 사용자가 브라우저 주소창에 웹사이트 주소를 입력합니다
2. 브라우저가 서버에 파일을 요청합니다
3. 서버는 이미 빌드된 HTML, CSS, JavaScript 파일을 그대로 전달합니다
4. 브라우저가 이 파일들을 다운로드합니다
5. 다운로드된 JavaScript가 사용자의 브라우저에서 실행됩니다
6. 화면에 상품 목록, 버튼, 이미지 등이 나타납니다

중요한 점은 서버가 이 파일들을 "처리"하지 않는다는 것입니다. 서버는 단지 파일을 보관하고 있다가 요청받으면 그대로 전달할 뿐입니다. 마치 도서관이 이미 인쇄된 책을 보관하다가 대출해주는 것과 같습니다.

#### 프론트엔드가 실행되는 위치

프론트엔드 코드는 **사용자의 디바이스에서만 실행**됩니다. 개발자의 서버가 아니라 사용자의 컴퓨터, 스마트폰, 태블릿의 브라우저 안에서 실행됩니다. 이것이 백엔드와의 가장 근본적인 차이입니다.

예를 들어 상품 이미지를 확대하거나, 색상 옵션을 선택하거나, 장바구니 아이콘에 숫자 배지를 표시하는 모든 동작이 사용자의 브라우저에서 JavaScript로 실행됩니다. 이런 작업은 서버와 통신할 필요가 없습니다.

---

## 백엔드: 보이지 않는 곳에서 일하는 두뇌

백엔드는 개발자가 관리하는 서버에서 24시간 실행되는 애플리케이션 코드로, 데이터 처리, 보안, 비즈니스 규칙 적용 같은 핵심 로직을 담당합니다.

#### 백엔드가 하는 일

같은 쇼핑몰 예시에서 백엔드는 다음과 같은 역할을 합니다.

- 사용자가 로그인하면 아이디와 비밀번호가 맞는지 검증합니다
- 이 사용자는 이 상품을 볼 권한이 있는가를 판단합니다
- 사용자가 구매 버튼을 누르면 재고가 충분한지 확인합니다
- 쿠폰 코드를 입력하면 실제로 유효한 쿠폰인지, 이미 사용했는지 확인합니다
- 결제 금액을 계산합니다(상품 가격 + 배송비 - 할인금액)
- 주문 정보를 데이터베이스에 저장합니다
- 결제 API와 통신해서 실제 결제를 처리합니다
- 주문 확인 이메일을 발송합니다

#### 백엔드와 서버의 차이

많은 사람들이 백엔드와 서버를 같은 것으로 생각하지만, 이 둘은 명확히 다릅니다.

**서버**는 물리적 또는 가상의 컴퓨팅 자원입니다. CPU, 메모리, 저장공간을 가진 컴퓨터 시스템으로, 프로그램이 실행되는 환경을 제공합니다. AWS EC2 인스턴스, 회사 데이터센터의 물리 서버가 여기에 해당합니다.

**백엔드**는 서버에서 실행되는 애플리케이션 소프트웨어입니다. Node.js로 작성한 Express 앱, Python으로 작성한 Django 앱처럼 비즈니스 로직을 처리하는 프로그램 코드를 의미합니다.

비유하자면 서버는 "건물"이고, 백엔드는 "그 안에 사는 가족의 생활 방식"입니다. 같은 건물(서버)에 여러 가족(백엔드 애플리케이션)이 살 수도 있고, 한 가족(백엔드)이 여러 건물(서버)에 나뉘어 살 수도 있습니다.

#### 백엔드가 실행되는 위치

백엔드 코드는 **개발자의 서버에서만 실행**됩니다. 사용자의 브라우저는 이 코드를 절대 받지 않고, 볼 수도 없습니다. 이것이 보안에 매우 중요합니다.

예를 들어 비밀번호 검증, 할인율 계산, 재고 확인 같은 중요한 로직이 모두 서버에서만 실행됩니다. 만약 이런 코드가 사용자 브라우저로 전달된다면, 악의적인 사용자가 코드를 수정해서 무료로 물건을 구매하거나 다른 사람의 정보에 접근할 수 있을 것입니다.

---

## 데이터베이스: 모든 정보가 저장되는 창고

데이터베이스는 데이터를 영구적으로 저장하고 관리하는 독립적인 시스템입니다. MySQL, PostgreSQL, MongoDB 같은 별도의 소프트웨어로 운영되며, 자체적인 쿼리 언어와 최적화 메커니즘을 가지고 있습니다.

#### 데이터베이스가 저장하는 것들

쇼핑몰 예시에서 데이터베이스는 다음과 같은 정보를 저장합니다.

- 사용자 정보(이메일, 이름, 암호화된 비밀번호, 주소)
- 상품 정보(이름, 가격, 재고 수량, 설명, 이미지 경로)
- 주문 내역(주문 번호, 주문 날짜, 결제 금액, 배송 상태)
- 리뷰와 평점
- 장바구니에 담긴 상품들
- 쿠폰 정보와 사용 내역

#### 관계형 데이터베이스와 비관계형 데이터베이스

데이터베이스는 크게 두 가지 유형으로 나뉩니다.

**관계형 데이터베이스(SQL 데이터베이스)**

데이터를 표(테이블) 형태로 저장하고, 테이블 간의 관계를 정의합니다. 엑셀 스프레드시트를 떠올리면 이해하기 쉽습니다. PostgreSQL과 MySQL이 대표적입니다.

사용자 테이블 예시:

|id|email|name|created_at|
|---|---|---|---|
|1|user@example.com|홍길동|2024-01-15|
|2|another@example.com|김철수|2024-01-16|

주문 테이블 예시:

|id|user_id|total_price|status|created_at|
|---|---|---|---|---|
|1|1|50000|completed|2024-01-20|
|2|1|120000|pending|2024-01-25|

여기서 주문 테이블의 user_id는 사용자 테이블의 id를 참조합니다. 이렇게 테이블 간의 관계를 명확히 정의하는 것이 관계형 데이터베이스의 특징입니다.

**비관계형 데이터베이스(NoSQL 데이터베이스)**

데이터를 표가 아닌 문서, 키-값, 그래프 등 다양한 형태로 저장합니다. MongoDB가 대표적이며, JSON 형태로 데이터를 저장합니다.

구조를 미리 정의하지 않아도 되고, 각 데이터가 다른 필드를 가질 수 있어서 유연합니다. 빠르게 변하는 요구사항에 대응하기 쉽지만, 복잡한 관계 처리는 어렵습니다.

#### PostgreSQL과 MySQL의 차이

PostgreSQL과 MySQL 모두 표 형태로 데이터를 저장하는 관계형 데이터베이스입니다. 기본적인 사용법은 거의 동일하지만, 철학과 세부 기능에서 차이가 있습니다.

**MySQL**은 빠르고 간단함을 목표로 합니다. 웹 개발에 최적화되어 있고, 읽기 작업이 많은 경우에 특히 빠릅니다. WordPress 같은 블로그 플랫폼, 간단한 웹사이트에 주로 사용됩니다.

**PostgreSQL**은 표준을 준수하고 확장 가능함을 목표로 합니다. 복잡한 쿼리와 다양한 데이터 타입을 지원하고, JSON 데이터 처리가 뛰어나며, 지리 정보 처리 같은 고급 기능을 제공합니다. Supabase가 PostgreSQL을 기반으로 하는 이유도 이 때문입니다.

실무에서는 복잡한 비즈니스 로직과 데이터 정합성이 중요한 경우 PostgreSQL을, 간단한 웹 애플리케이션의 경우 MySQL을 선택하는 경우가 많습니다. 하지만 기본적인 기능만 사용한다면 둘의 차이를 거의 느끼지 못합니다.

#### 데이터베이스는 백엔드와 별개

많은 사람들이 데이터베이스를 백엔드의 일부로 생각하지만, 데이터베이스는 백엔드와는 독립적인 시스템입니다. 백엔드와 데이터베이스는 각각 다른 서버에서 실행되는 경우가 많으며, 다른 기술과 관리 방식을 가집니다.

백엔드는 데이터를 가공하고 비즈니스 로직을 실행하는 곳이고, 데이터베이스는 데이터를 저장하고 관리하는 곳입니다. 백엔드가 데이터가 필요할 때 데이터베이스에 요청해서 가져오고, 새로운 데이터가 생기면 데이터베이스에 저장을 요청합니다.

---

## 세 가지 요소의 상호작용

프론트엔드, 백엔드, 데이터베이스가 어떻게 협력해서 하나의 완전한 기능을 만드는지 구체적인 예시로 살펴보겠습니다.

#### 사용자 로그인 과정

**1단계: 화면 표시 (프론트엔드)**

사용자가 로그인 페이지에 접속하면, 브라우저에서 JavaScript가 실행되어 로그인 폼을 화면에 그립니다. 이메일 입력창, 비밀번호 입력창, 로그인 버튼이 보입니다.

**2단계: 사용자 입력 (프론트엔드)**

사용자가 이메일과 비밀번호를 입력하고 로그인 버튼을 클릭합니다. 프론트엔드 JavaScript 코드가 이 클릭 이벤트를 감지합니다.

**3단계: 기본 검증 (프론트엔드)**

프론트엔드에서 먼저 기본적인 검증을 합니다. 이메일 형식이 맞는지, 비밀번호 길이가 충분한지, 빈 칸은 없는지 확인합니다. 문제가 있으면 "이메일 형식이 올바르지 않습니다" 같은 메시지를 즉시 표시합니다. 이 과정은 서버와 통신하지 않고 브라우저에서만 처리됩니다.

**4단계: 서버에 요청 전송 (프론트엔드 → 백엔드)**

기본 검증을 통과하면 프론트엔드가 백엔드 서버에 API 요청을 보냅니다. 이메일과 비밀번호 정보를 네트워크를 통해 서버로 전송합니다.

**5단계: 요청 처리 (백엔드 → 데이터베이스)**

서버에서 실행 중인 백엔드 코드가 이 요청을 받습니다. 백엔드는 데이터베이스에 "이 이메일을 가진 사용자를 찾아주세요"라고 요청합니다.

**6단계: 데이터 조회 (데이터베이스)**

데이터베이스가 사용자 테이블을 검색해서 해당 이메일의 사용자 정보를 찾습니다. 저장된 암호화된 비밀번호를 포함한 사용자 정보를 백엔드에 돌려줍니다.

**7단계: 비밀번호 검증 및 토큰 생성 (백엔드)**

백엔드가 데이터베이스에서 받은 암호화된 비밀번호와 사용자가 입력한 비밀번호를 비교합니다. 일치하면 JWT 토큰을 생성하고, 로그인 시간을 데이터베이스에 기록합니다.

**8단계: 응답 반환 (백엔드 → 프론트엔드)**

백엔드가 처리 결과를 프론트엔드에 응답으로 보냅니다. 성공했다면 토큰과 사용자 정보를 포함하고, 실패했다면 에러 메시지를 포함합니다.

**9단계: 결과 처리 (프론트엔드)**

브라우저에서 실행 중인 프론트엔드 코드가 이 응답을 받아서 처리합니다. 성공한 경우 토큰을 브라우저에 저장하고, "홍길동님 환영합니다" 메시지를 표시하고, 메인 페이지로 이동합니다. 상단 헤더에 "로그인" 버튼 대신 "홍길동님" 프로필을 표시합니다.

#### 상품 검색 과정

**1단계: 검색창 표시 (프론트엔드)**

사용자의 브라우저에 검색창이 표시됩니다. 이것은 프론트엔드 코드가 그린 것입니다.

**2단계: 입력 감지 (프론트엔드)**

사용자가 "노트북"이라고 입력하면, 프론트엔드가 각 글자 입력을 감지합니다. 입력이 비어있으면 서버에 요청하지 않고 결과를 초기화합니다.

**3단계: 검색 요청 (프론트엔드 → 백엔드)**

입력이 있으면 프론트엔드가 백엔드에 "노트북을 검색해주세요"라고 API 요청을 보냅니다.

**4단계: 데이터베이스 검색 (백엔드 → 데이터베이스)**

백엔드가 데이터베이스에 "상품 이름에 '노트북'이 포함되고, 재고가 있고, 판매 중인 상품을 찾아주세요"라고 요청합니다.

**5단계: 검색 결과 반환 (데이터베이스 → 백엔드)**

데이터베이스가 조건에 맞는 상품 목록을 찾아서 백엔드에 돌려줍니다.

**6단계: 데이터 가공 및 로깅 (백엔드)**

백엔드가 검색 결과를 정리하고, 필요한 정보만 추출합니다. 그리고 이 검색 기록을 데이터베이스에 저장합니다(나중에 인기 검색어 분석 등에 사용).

**7단계: 응답 전송 (백엔드 → 프론트엔드)**

백엔드가 가공된 검색 결과를 프론트엔드에 보냅니다.

**8단계: 화면 업데이트 (프론트엔드)**

프론트엔드가 받은 데이터로 화면을 업데이트합니다. 검색 결과를 예쁜 카드 형태로 배치하고, 상품 이미지와 가격을 표시합니다.

이 전체 과정에서 프론트엔드는 사용자의 브라우저에서, 백엔드는 서버에서, 데이터베이스는 또 다른 서버에서 각자의 역할을 수행합니다.

---

## 개발부터 배포까지의 전체 과정

웹 애플리케이션이 어떻게 만들어지고, 어떻게 사용자에게 전달되는지 전체 과정을 살펴보겠습니다.

#### 1단계: 개발 (개발자의 컴퓨터)

**프론트엔드 개발**

개발자가 자신의 컴퓨터에서 React, Vue 같은 도구로 프론트엔드 코드를 작성합니다. 사용자 인터페이스, 버튼 클릭 이벤트, 애니메이션 등을 만듭니다.

개발하는 동안에는 개발 모드로 실행됩니다. 코드를 수정하면 즉시 브라우저에 반영되고, 에러가 발생하면 자세한 메시지가 나타납니다. 하지만 이것은 개발 편의를 위한 것이고, 실제 사용자에게는 이렇게 제공되지 않습니다.

**백엔드 개발**

개발자가 자신의 컴퓨터에서 Node.js, Python 같은 언어로 백엔드 코드를 작성합니다. API 엔드포인트를 만들고, 비즈니스 로직을 구현하고, 데이터베이스 쿼리를 작성합니다.

**데이터베이스 설계**

데이터베이스의 테이블 구조를 설계하고, 테이블 간의 관계를 정의합니다. 어떤 데이터를 어떻게 저장할지, 어떤 인덱스를 만들지 결정합니다.

#### 2단계: 빌드 (개발자의 컴퓨터)

배포하기 전에 프론트엔드 코드를 "빌드"합니다. 이것은 매우 중요한 단계입니다.

**빌드가 하는 일**

- React 같은 특수한 코드를 브라우저가 이해할 수 있는 순수 JavaScript로 변환합니다
- 여러 개의 파일을 하나 또는 몇 개의 파일로 합칩니다
- 코드를 압축해서 파일 크기를 줄입니다
- 사용하지 않는 코드를 제거합니다
- 이미지를 최적화합니다

빌드 결과물은 HTML, CSS, JavaScript 파일들입니다. 이 파일들은 브라우저가 바로 실행할 수 있는 형태입니다.

**중요한 점**

이 빌드 과정은 개발자의 컴퓨터에서 단 한 번만 실행됩니다. 백엔드 서버에서 일어나지 않고, 사용자의 브라우저에서도 일어나지 않습니다. 빌드는 배포 전의 준비 작업입니다.

#### 3단계: 배포

빌드된 파일과 백엔드 코드를 서버에 올리는 과정입니다.

**프론트엔드 파일 배포**

빌드된 프론트엔드 파일(HTML, CSS, JavaScript)을 정적 파일 호스팅 서비스에 업로드합니다.

- Vercel, Netlify 같은 전문 서비스 사용
- AWS S3 + CloudFront 같은 클라우드 스토리지 사용
- Nginx 같은 웹 서버에 업로드

이 서버들은 그저 파일을 보관하고, 사용자가 요청하면 그대로 전달하는 역할만 합니다. 어떤 처리나 변환도 하지 않습니다.

**백엔드 애플리케이션 배포**

백엔드 코드를 서버에 배포하고 실행합니다.

- AWS EC2, Google Cloud 같은 클라우드 서버에 배포
- Heroku, Railway 같은 플랫폼 사용
- 자체 데이터센터 서버 사용

백엔드 서버는 24시간 계속 실행되면서 API 요청을 기다립니다.

**데이터베이스 설정**

데이터베이스를 별도의 서버에 설치하고 설정합니다.

- AWS RDS, Google Cloud SQL 같은 관리형 서비스 사용
- MongoDB Atlas, Supabase 같은 전문 서비스 사용
- 자체 서버에 PostgreSQL, MySQL 설치

데이터베이스는 백엔드 서버와 통신할 수 있도록 네트워크 설정을 합니다. 보안을 위해 외부에서 직접 접근할 수 없도록 설정하는 경우가 많습니다.

#### 4단계: 사용자 접속

사용자가 웹사이트에 접속하면 다음과 같은 일이 일어납니다.

**첫 번째 요청: 프론트엔드 파일 다운로드**

1. 사용자가 브라우저에서 웹사이트 주소를 입력합니다
2. 브라우저가 프론트엔드 파일 서버에 요청을 보냅니다
3. 서버가 HTML 파일을 그대로 전달합니다
4. 브라우저가 HTML을 읽고, 추가로 필요한 CSS와 JavaScript 파일을 요청합니다
5. 서버가 이 파일들도 그대로 전달합니다
6. 브라우저가 모든 파일을 다운로드합니다

**프론트엔드 실행**

1. 다운로드된 JavaScript가 사용자의 브라우저에서 실행되기 시작합니다
2. 화면에 빈 틀이 나타나고, "로딩 중..." 같은 메시지가 표시됩니다
3. 프론트엔드 코드가 백엔드 API에 데이터를 요청합니다

**백엔드 및 데이터베이스 처리**

1. 백엔드 서버가 API 요청을 받습니다
2. 필요한 경우 데이터베이스에 데이터를 요청합니다
3. 데이터베이스가 데이터를 찾아서 백엔드에 돌려줍니다
4. 백엔드가 데이터를 가공하고 검증합니다
5. 백엔드가 결과를 프론트엔드에 응답으로 보냅니다

**화면 완성**

1. 브라우저에서 실행 중인 프론트엔드가 응답 데이터를 받습니다
2. 받은 데이터로 화면을 업데이트합니다
3. 상품 목록, 이미지, 가격 등이 화면에 나타납니다
4. 사용자가 완전한 웹페이지를 볼 수 있게 됩니다

---

## 서버와 실행 환경 이해하기

#### 프론트엔드 파일 서버

프론트엔드 파일을 제공하는 서버는 매우 단순한 역할을 합니다. 파일을 보관하고 있다가, 요청받으면 그대로 전달하는 것이 전부입니다.

이 서버는 Node.js나 Python 같은 프로그래밍 언어가 필요 없을 수도 있습니다. 그냥 파일을 저장하고 네트워크로 전송할 수 있는 기능만 있으면 됩니다. Nginx 같은 웹 서버나, AWS S3 같은 클라우드 스토리지가 이 역할을 합니다.

**중요한 점:** 프론트엔드 파일 서버는 코드를 "실행"하지 않습니다. 단지 파일을 전달할 뿐입니다. 실제 코드 실행은 사용자의 브라우저에서 일어납니다.

#### 백엔드 API 서버

백엔드 서버는 24시간 실행되는 프로그램입니다. Node.js, Python, Java 같은 언어로 작성된 애플리케이션이 서버에서 계속 돌아가면서 API 요청을 기다립니다.

요청이 오면 코드를 실행해서 처리하고, 데이터베이스와 통신하고, 결과를 응답으로 보냅니다. 이 모든 과정이 서버에서 일어나며, 사용자는 결과만 받을 뿐입니다.

#### 데이터베이스 서버

데이터베이스도 별도의 서버에서 실행되는 프로그램입니다. PostgreSQL, MySQL 같은 데이터베이스 소프트웨어가 24시간 실행되면서 데이터를 관리합니다.

백엔드 서버만 데이터베이스 서버에 접근할 수 있도록 설정하는 것이 일반적입니다. 프론트엔드나 외부에서 직접 데이터베이스에 접근하지 못하도록 보안 설정을 합니다.

#### 물리적 배치 예시

실제 운영 환경에서는 이렇게 배치될 수 있습니다.

**소규모 스타트업:**

- 프론트엔드: Vercel(자동으로 전 세계에 분산)
- 백엔드: Heroku 또는 AWS EC2 하나
- 데이터베이스: AWS RDS 또는 Supabase

**중규모 서비스:**

- 프론트엔드: AWS CloudFront + S3(전 세계 CDN)
- 백엔드: AWS EC2 서버 3대(로드 밸런서로 분산)
- 데이터베이스: AWS RDS(마스터 1대 + 읽기 전용 복제본 2대)

**대규모 서비스:**

- 프론트엔드: 전 세계 여러 지역의 CDN
- 백엔드: 수십 개의 서버(지역별, 기능별 분산)
- 데이터베이스: 샤딩으로 여러 데이터베이스 서버에 분산
- 캐시: Redis 서버 여러 대

---

## 현대적인 개발 방식

#### BaaS(Backend as a Service)

Supabase, Firebase 같은 서비스를 사용하면 백엔드 서버를 직접 만들지 않아도 됩니다.

**전통적인 방식:**

- 백엔드 개발자가 API 서버 코드를 작성합니다
- 모든 API 엔드포인트를 직접 만듭니다
- 서버를 직접 관리하고 유지보수합니다

**BaaS 방식:**

- 프론트엔드 코드에서 직접 데이터베이스에 접근합니다
- Supabase나 Firebase가 제공하는 SDK를 사용합니다
- 별도의 백엔드 서버가 필요 없습니다

**하지만 여전히 필요한 것들:**

- 데이터베이스 스키마 설계
- 보안 규칙 설정(누가 어떤 데이터를 볼 수 있는지)
- 복잡한 비즈니스 로직(Database Functions나 Edge Functions)
- 성능 최적화

간단한 프로젝트는 프론트엔드 개발자 혼자서도 충분히 만들 수 있지만, 복잡한 프로젝트는 여전히 백엔드/데이터베이스 전문가가 필요합니다. BaaS는 백엔드 개발자를 없애는 것이 아니라, 반복적인 작업을 줄여주는 도구입니다.

#### 서버 사이드 렌더링(SSR)

Next.js 같은 프레임워크를 사용하면 프론트엔드 코드를 서버에서도 실행할 수 있습니다.

**전통적인 방식(CSR - Client Side Rendering):**

1. 브라우저가 빈 HTML과 JavaScript를 다운로드합니다
2. 브라우저에서 JavaScript가 실행됩니다
3. 데이터를 API로 요청합니다
4. 화면이 완성됩니다

**SSR 방식:**

1. 서버에서 프론트엔드 코드를 먼저 실행합니다
2. 데이터를 미리 가져와서 HTML을 완성합니다
3. 완성된 HTML을 브라우저에 전달합니다
4. 브라우저에서 인터랙티브하게 만듭니다

SSR의 장점은 검색 엔진 최적화(SEO)와 초기 로딩 속도 개선입니다. 하지만 설정과 관리가 더 복잡합니다.

#### 마이크로서비스 아키텍처

대규모 서비스는 백엔드를 여러 개의 작은 서비스로 나누기도 합니다.

**모놀리식(전통적 방식):**

- 하나의 큰 백엔드 애플리케이션
- 모든 기능이 한 곳에 있음

**마이크로서비스:**

- 사용자 관리 서비스
- 상품 관리 서비스
- 주문 처리 서비스
- 결제 서비스
- 알림 서비스

각 서비스가 독립적으로 개발되고 배포됩니다. 한 서비스에 문제가 생겨도 다른 서비스는 계속 작동할 수 있습니다.

---

## 마치며

웹 애플리케이션은 프론트엔드, 백엔드, 데이터베이스라는 세 가지 핵심 요소가 협력해서 만들어집니다.

**프론트엔드**는 사용자의 디바이스에서 실행되며, 사용자가 보고 상호작용하는 모든 것을 담당합니다. React 같은 도구로 개발하지만, 빌드 과정을 거쳐 브라우저가 이해할 수 있는 HTML, CSS, JavaScript로 변환됩니다.

**백엔드**는 개발자의 서버에서 실행되며, 데이터 처리와 비즈니스 로직을 담당합니다. 사용자는 이 코드를 볼 수 없고, 결과만 API를 통해 받습니다.

**데이터베이스**는 별도의 시스템으로, 모든 데이터를 영구적으로 저장하고 관리합니다. 백엔드만 데이터베이스에 접근할 수 있습니다.

이 세 가지는 각자의 영역에서 독립적으로 실행되지만, API를 통해 소통하면서 하나의 완전한 애플리케이션을 만들어냅니다. 개발자는 이 구조를 이해하고, 각 부분을 적절히 설계하고 구현해서 사용자에게 가치 있는 서비스를 제공합니다.