---
date: 2026-01-18
tags:
  - vibe_coding
  - API서버
  - 미들웨어
  - Proxy
  - CORS
  - 요청
  - 동일출처정책
  - 라우터
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
author:
people:
YouTube:
Lilys AI:
scr_url:
scr:
byAI:
  - "[[✨Claude]]"
---



## CORS 문제란 무엇인가?

CORS(Cross-Origin Resource Sharing)는 브라우저의 보안 정책입니다. 간단히 말하면, 브라우저가 "다른 도메인의 서버에서 데이터를 가져오는 것을 기본적으로 차단"하는 규칙입니다.

#### 실제 상황 예시

사용자님이 `https://mywebsite.com`에서 웹사이트를 만들었는데, `https://api.external.com`의 데이터를 가져오려고 하면:

```
브라우저의 판단:
"mywebsite.com에서 api.external.com으로 요청을 보내려고 하네?"
"도메인이 다르네! 보안상 위험할 수 있으니 차단하자!"
→ CORS 에러 발생
```

이것은 브라우저가 사용자를 보호하기 위해 만든 규칙입니다. 악의적인 웹사이트가 다른 사이트의 데이터를 몰래 가져가는 것을 막기 위함이죠.

## CORS 문제가 발생하는 이유

#### 브라우저의 보안 정책

웹 브라우저는 기본적으로 "같은 출처(Same-Origin)"에서만 데이터를 주고받을 수 있게 합니다. 출처가 다르다는 것은:

- **도메인이 다른 경우**: `example.com` → `api.other.com`
- **포트가 다른 경우**: `localhost:3000` → `localhost:5000`
- **프로토콜이 다른 경우**: `http://` → `https://`

이 중 하나라도 다르면 브라우저는 "다른 출처"로 판단하고 차단합니다.

#### 개발 환경에서 자주 발생하는 이유

로컬에서 개발할 때 프론트엔드는 `localhost:5173`에서, 백엔드는 `localhost:3000`에서 실행되는 경우가 많습니다. 포트 번호가 다르므로 브라우저는 이를 "다른 출처"로 보고 CORS 에러를 발생시킵니다.

## CORS 문제 해결 방법

CORS 문제를 해결하는 핵심은 **서버가 "이 도메인의 요청은 받아들이겠다"고 브라우저에게 알려주는 것**입니다.

#### 해결의 핵심: 응답 헤더

서버는 응답을 보낼 때 "헤더(Header)"라는 부가 정보를 함께 보냅니다. 헤더는 실제 데이터 앞에 붙는 메타정보로, 편지의 봉투에 적힌 발신자·수신자 정보와 같은 역할입니다.

```
[요청 헤더] (브라우저 → 서버)
GET /api/users HTTP/1.1
Host: api.example.com        ← 어느 서버로 가는지
User-Agent: Chrome/120.0     ← 어떤 브라우저인지
Accept: application/json     ← 어떤 형식을 원하는지

[응답 헤더] (서버 → 브라우저)
HTTP/1.1 200 OK
Content-Type: application/json                    ← 데이터 형식
Access-Control-Allow-Origin: https://myapp.com    ← CORS 허용 정보

[본문]
{"users": [...]}  ← 실제 데이터
```

CORS 문제를 해결하려면 서버가 응답 헤더에 `Access-Control-Allow-Origin`을 추가해야 합니다.

## API 서버와 미들웨어

CORS 문제를 해결하기 위해 "API 서버를 만든다"는 말을 자주 듣게 됩니다. 이것이 정확히 무엇을 의미하는지 살펴보겠습니다.

#### API 서버란?

API 서버는 클라이언트(브라우저, 앱 등)의 요청을 받아서 데이터를 처리하고 응답을 돌려주는 프로그램입니다. 웹사이트의 백엔드(뒷단)를 담당합니다.

```
프론트엔드 (브라우저에서 보는 화면)
    ↕ HTTP 요청/응답
백엔드 (API 서버)
    ↕
데이터베이스
```

#### API 서버의 구성 요소

API 서버는 여러 부품들이 모여서 만들어집니다:

**1. 서버 프레임워크**: Express, Fastify 같은 기본 틀
**2. 미들웨어**: 공통 처리 작업을 담당하는 부품들
**3. 라우터**: 요청을 적절한 처리 함수로 연결하는 시스템
**4. 라우트 핸들러**: 실제 작업을 수행하는 개별 함수들

## 미들웨어의 역할

#### 미들웨어란?

미들웨어는 요청과 응답 사이에서 작동하는 **중간 처리 계층**입니다. 공장의 생산 라인에서 각 단계마다 특정 작업을 처리하는 것처럼, 미들웨어는 요청이 최종 목적지에 도달하기 전에 필요한 작업들을 수행합니다.

```
클라이언트의 요청 도착
    ↓
미들웨어 1: 로그 기록
    ↓
미들웨어 2: 사용자 인증 확인
    ↓
미들웨어 3: CORS 헤더 추가
    ↓
라우트 핸들러: 실제 데이터 처리
    ↓
응답 전송
```

#### CORS 미들웨어의 작동

CORS 미들웨어는 모든 응답에 `Access-Control-Allow-Origin` 헤더를 자동으로 추가합니다:

```
미들웨어가 하는 일:
"응답을 보낼 때 헤더에 이걸 추가하자"
→ Access-Control-Allow-Origin: https://mywebsite.com

브라우저가 받는 응답:
HTTP/1.1 200 OK
Access-Control-Allow-Origin: https://mywebsite.com  ← 미들웨어가 추가
Content-Type: application/json

{"data": "실제 데이터"}  ← 라우트 핸들러가 생성
```

#### 미들웨어의 출처

미들웨어는 두 가지 방식으로 사용됩니다:

**개발자가 직접 작성**: 프로젝트의 특수한 요구사항에 맞춰 만든 커스텀 미들웨어

**외부 패키지 사용**: 다른 개발자들이 만들어서 npm에 공개한 미들웨어 (예: `cors` 패키지)

대부분의 경우 CORS 처리는 이미 잘 만들어진 `cors` 패키지를 설치해서 사용합니다. 직접 만들 필요가 없습니다.

#### 미들웨어의 작동 위치

**미들웨어는 서버에서 작동합니다.** 클라이언트(브라우저)가 아닌 백엔드 서버의 코드 일부입니다.

```
[클라이언트 영역 - 브라우저]
- React, Vue 같은 프론트엔드 코드
- HTML, CSS, JavaScript

    ↕ HTTP 요청/응답

[서버 영역 - 여기서 미들웨어 작동]
- Node.js/Express 서버
- CORS 미들웨어
- 인증 미들웨어
- 로깅 미들웨어
- 라우트 핸들러
```

## 라우터와 라우트 핸들러

#### 라우터 vs 라우트 핸들러

많은 분들이 혼동하는 개념입니다. 간단히 정리하면:

**라우터(Router)**: 요청을 받아서 적절한 처리 함수로 분배하는 **전체 시스템**

**라우트 핸들러(Route Handler)**: 특정 경로와 HTTP 메서드에 대응하는 **개별 처리 함수**

```
┌──────────────────────────────────────┐
│           라우터 (전체 시스템)                                             │
│                                                                            │
│  ┌──────────────────────────────────┐  │
│  │ GET /api/users                                                     │  │
│  │ → 사용자 목록 조회 핸들러                                         │  │
│  └──────────────────────────────────┘  │
│                                                                            │
│  ┌──────────────────────────────────┐  │
│  │ POST /api/users                                                    │  │
│  │ → 새 사용자 생성 핸들러                                           │  │
│  └──────────────────────────────────┘  │
│                                                                            │
│  ┌──────────────────────────────────┐  │
│  │ DELETE /api/users/:id                                              │  │
│  │ → 사용자 삭제 핸들러                                              │  │
│  └──────────────────────────────────┘  │
└──────────────────────────────────────┘
```

라우터는 전화 교환대처럼 "이 요청은 어느 담당자에게 보낼까?"를 판단하고, 라우트 핸들러는 실제로 전화를 받아서 업무를 처리하는 담당자입니다.

#### 라우트 핸들러의 역할

라우트 핸들러는 **요청의 최종 처리자**입니다. 미들웨어가 준비 작업을 마치면, 라우트 핸들러가 실제 비즈니스 로직을 수행하고 응답을 생성합니다.

```
요청: GET /api/users

1. 라우터의 판단:
   "GET 메서드에 /api/users 경로구나"
   "getUsersHandler를 실행하자"

2. getUsersHandler (라우트 핸들러):
   - 데이터베이스에서 사용자 목록 조회
   - JSON 형태로 변환
   - 응답 전송
```

라우트 핸들러는 다른 서버로 요청을 전달하는 것이 아니라, **자기 자신이 직접 데이터를 처리하고 응답을 만들어내는 최종 목적지**입니다.

## 전체 흐름 정리

CORS 문제가 발생하고 해결되는 전체 과정을 정리하면:

#### 1. CORS 문제 발생

```
브라우저(https://myapp.com)
    ↓ 요청: GET /api/users
외부 API(https://api.other.com)
    ↓ 응답 (CORS 헤더 없음)
브라우저
    → CORS 에러! "Access-Control-Allow-Origin 헤더가 없네!"
```

#### 2. 해결책: API 서버 구축

자체 백엔드 서버를 만들어서 CORS 헤더를 추가합니다:

```
메인 앱 (최상위 라우터)
├── CORS 미들웨어 ← "모든 응답에 CORS 헤더 추가"
├── JSON 파싱 미들웨어 ← "요청 본문을 JSON으로 변환"
├── /api/users → userRouter (하위 라우터)
│   ├── GET / → getUsersHandler (라우트 핸들러)
│   ├── POST / → createUserHandler (라우트 핸들러)
│   └── GET /:id → getUserHandler (라우트 핸들러)
└── /api/products → productRouter (하위 라우터)
    ├── GET / → getProductsHandler (라우트 핸들러)
    └── POST / → createProductHandler (라우트 핸들러)
```

#### 3. 요청 처리 과정

```
1. 브라우저에서 요청
   GET https://myserver.com/api/users

2. 서버에 요청 도착

3. CORS 미들웨어 실행
   → 응답 객체에 헤더 추가 준비
   → Access-Control-Allow-Origin: https://myapp.com

4. JSON 파싱 미들웨어 실행
   → 요청 본문을 JSON으로 변환

5. 라우터가 경로 확인
   → "GET /api/users는 getUsersHandler로 보내자"

6. getUsersHandler (라우트 핸들러) 실행
   → 데이터베이스에서 사용자 목록 조회
   → JSON 응답 생성

7. 브라우저로 응답 전송 (CORS 헤더 포함)
   HTTP/1.1 200 OK
   Access-Control-Allow-Origin: https://myapp.com
   Content-Type: application/json
   
   {"users": [...]}

8. 브라우저가 CORS 헤더 확인
   → "myapp.com이 허용되어 있네! 응답을 받아들이자"
```

## 개발 환경 vs 운영 환경

#### 개발 시 프록시 서버

개발할 때는 프론트엔드 개발 서버(Vite, Create React App 등)가 제공하는 프록시 기능을 사용합니다:

```
브라우저 → 프록시(localhost:5173) → 외부 API
         ← (CORS 헤더 추가)      ← (원본 응답)
```

프록시는 중간에서 요청을 받아 외부 API로 전달하고, 응답을 받아올 때 CORS 헤더를 추가해서 브라우저에 돌려줍니다.

**하지만 프록시는 개발 환경에서만 작동합니다.** 실제로 배포하면 프록시는 사라지고 CORS 문제가 다시 발생합니다.

#### 운영 환경: 실제 API 서버

배포 후에는 자체 백엔드 서버에 미들웨어를 추가하여 CORS를 처리해야 합니다:

```
브라우저 → API 서버 (미들웨어로 CORS 처리)
         ← (CORS 헤더 포함 응답)
```

이 서버는 개발할 때도, 배포해도 동일하게 작동합니다.

## 핵심 개념 요약

#### CORS 문제
브라우저가 다른 도메인의 서버에서 데이터를 가져오는 것을 차단하는 보안 정책

#### 해결 방법
서버가 응답 헤더에 `Access-Control-Allow-Origin`을 추가하여 "이 도메인은 허용한다"고 브라우저에게 알림

#### API 서버
클라이언트의 요청을 받아 처리하고 응답을 돌려주는 백엔드 프로그램 전체

#### 미들웨어
서버 내부에서 요청과 응답 사이에 실행되는 중간 처리 함수. CORS 헤더 추가, 인증 확인, 로깅 등의 공통 작업 수행

#### 라우터
요청을 받아서 적절한 라우트 핸들러로 분배하는 전체 시스템

#### 라우트 핸들러
특정 경로와 HTTP 메서드(GET, POST 등)에 대응하는 개별 처리 함수. 실제 비즈니스 로직을 수행하고 응답 생성

#### 헤더
HTTP 메시지의 앞부분에 위치한 메타정보 영역. 실제 데이터에 대한 설명이나 처리 규칙을 담음

이 모든 요소들이 함께 작동하여 안전하고 효율적인 웹 애플리케이션을 만들어냅니다.

