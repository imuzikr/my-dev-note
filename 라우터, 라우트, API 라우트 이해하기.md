---
date: 2026-01-31
tags:
  - vibe_coding
  - API서버
  - 라우터
  - 라우트
  - API라우트
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---


## 핵심 요약

- **라우터(Router)**: 전체 교통 관리 시스템
- **라우트(Route)**: 구체적인 경로 규칙 하나하나
- **API 라우트(API Route)**: 데이터를 주고받기 위한 URL 주소

## 1. 라우터(Router)란?

### 기본 개념

라우터는 **"어떤 페이지를 보여줄지 결정하는 교통정리 담당자"**입니다. 전체 시스템을 관리하는 관리자 역할을 합니다.

### 실생활 비유

**백화점 안내 데스크**를 떠올려보세요.

고객이 "화장품 매장은 어디에 있나요?"라고 물으면, 안내 직원이 "3층입니다"라고 알려주듯이, 라우터는 사용자가 요청한 주소를 보고 어떤 화면을 보여줄지 결정합니다.

### 작동 방식

1. 사용자가 웹사이트 주소를 입력하거나 링크를 클릭
2. 라우터가 입력된 주소를 확인
3. 여러 규칙(라우트) 중에서 맞는 것을 찾음
4. 해당하는 화면을 보여줌

### 예시

사용자가 `my-app.com/books`라고 주소창에 입력하면:

- 라우터가 "/books"라는 주소를 확인
- "아, 책 목록 페이지를 보여줘야겠구나" 판단
- 책 목록 화면을 띄워줌

## 2. 라우트(Route)란?

### 기본 개념

라우트는 **"이 주소로 가면 이 화면을 보여줘"라는 구체적인 규칙**입니다. 하나하나의 경로를 의미합니다.

### 실생활 비유

**건물의 층별 안내판**을 생각해보세요.

```
1층 → 로비
2층 → 도서관
3층 → 카페
4층 → 사무실
```

각 층마다 무엇이 있는지 정해놓은 규칙이 바로 라우트입니다.

### 라우터와 라우트의 관계

**라우터**: 전체 안내 시스템 **라우트**: 구체적인 층별 안내 규칙

```
라우터 (건물 안내 시스템)
  ├─ 라우트 1: 1층 → 로비
  ├─ 라우트 2: 2층 → 도서관
  ├─ 라우트 3: 3층 → 카페
  └─ 라우트 4: 4층 → 사무실
```

### 웹사이트 예시

독서 앱에서 라우트는 다음과 같이 구성됩니다:

```
라우터 (우리 반 독서 대장 앱)
  ├─ 라우트: / → 메인 페이지
  ├─ 라우트: /books → 책 목록 페이지
  ├─ 라우트: /search → 책 검색 페이지
  └─ 라우트: /mypage → 내 정보 페이지
```

### 작동 단계

**1단계**: 사용자가 `/books` 링크를 클릭

**2단계**: 라우터가 모든 라우트 규칙을 확인

- "/" 규칙? 아니야
- "/books" 규칙? 맞아! 이거야!
- "/search" 규칙? 아니야

**3단계**: 찾은 라우트에 따라 책 목록 화면을 보여줌

**결과**: 화면이 메인 페이지에서 책 목록 페이지로 전환됨

## 3. API 라우트(API Route)란?

### 기본 개념

API 라우트는 **"데이터를 주고받기 위한 URL 주소"**입니다. 화면을 보여주는 게 아니라, 데이터만 주고받습니다.

### 페이지 라우트와의 차이

|구분|페이지 라우트|API 라우트|
|---|---|---|
|목적|화면 전환|데이터 송수신|
|사용자가 보는 것|화면이 바뀜|화면은 그대로|
|결과|새로운 페이지|데이터(JSON)|
|예시|`/books`|`/.netlify/functions/search-books`|

### 실생활 비유

**페이지 라우트 = 지하철 타기**

- 강남역에 가려면 2호선을 타요
- 실제로 장소가 바뀝니다
- 보이는 풍경이 완전히 달라집니다

**API 라우트 = 은행 창구 이용**

- 1번 창구: 입금 업무
- 2번 창구: 출금 업무
- 3번 창구: 대출 상담

창구에서 업무만 처리하고, 내가 있는 위치(은행 로비)는 그대로입니다. 다만 받아온 서류(데이터)만 달라집니다.

### 작동 단계

**사용자가 책 검색 버튼을 클릭한 경우:**

**1단계**: 검색 버튼 클릭 → "해리포터" 검색

**2단계**: 브라우저가 API 라우트로 요청 전송

```
요청 주소: /.netlify/functions/search-books
요청 내용: "해리포터를 검색해주세요"
```

**3단계**: 서버에 있는 검색 함수가 실행됨

- 네이버 API에 "해리포터" 검색 요청
- 네이버가 책 목록 데이터를 돌려줌

**4단계**: 서버가 받은 데이터를 브라우저에 전달

```
받은 데이터: [
  {제목: "해리포터와 마법사의 돌", 저자: "J.K. 롤링"},
  {제목: "해리포터와 비밀의 방", 저자: "J.K. 롤링"},
  ...
]
```

**5단계**: 브라우저가 받은 데이터를 화면에 표시

**결과**:

- URL 주소는 그대로 (화면 안 바뀜)
- 검색 결과만 화면에 나타남

### 왜 API 라우트가 필요할까?

**보안 문제 때문입니다.**

네이버 API를 사용하려면 비밀 키(Client Secret)가 필요한데, 이걸 웹페이지 코드에 직접 넣으면 누구나 볼 수 있습니다. 웹페이지는 F12 키만 눌러도 모든 코드가 보이기 때문입니다.

**해결 방법:**

- 비밀 키는 서버에만 안전하게 보관
- 브라우저는 서버의 API 라우트로만 요청
- 서버가 네이버 API를 대신 호출
- 결과만 브라우저에 전달

**비유:** 집 금고 비밀번호를 가족에게 알려주지 않고, 필요한 물건만 꺼내서 전달해주는 것과 같습니다.

## 4. 세 개념 종합 비교

### 우체국 배달 시스템 비유

**라우트(Route)**: 배달 규칙 하나하나

- "서울시 강남구 → A 배달원"
- "서울시 송파구 → B 배달원"
- "경기도 성남시 → C 배달원"

**라우터(Router)**: 우체국 분류 시스템

- 들어온 편지의 주소를 확인
- 어떤 규칙을 적용할지 판단
- 적절한 배달원에게 배정

**API 라우트**: 우체국 업무 창구

- 등기 우편 신청 창구
- 택배 접수 창구
- 요금 문의 창구

편지를 배달하는 것(페이지 라우트)과 우체국에서 업무를 처리하는 것(API 라우트)은 다른 일입니다.

### 독서 앱 실제 예시

**상황**: 사용자가 독서 앱에서 책을 검색하는 경우

#### 페이지 라우트 사용 (화면 전환)

**1단계**: 사용자가 "책 검색" 메뉴 클릭

**2단계**: 라우터가 작동

- 현재 주소: `my-app.com/`
- 이동할 주소: `my-app.com/search`

**3단계**: `/search` 라우트 규칙 적용

- "검색 페이지를 보여줘"

**4단계**: 화면 전환

- 메인 페이지 → 검색 페이지

**결과**:

- URL 변경됨
- 완전히 새로운 화면 표시

#### API 라우트 사용 (데이터 처리)

**1단계**: 검색 페이지에서 "해리포터" 입력 후 검색 버튼 클릭

**2단계**: API 라우트로 요청

- 주소: `/.netlify/functions/search-books?query=해리포터`
- 현재 화면은 그대로 유지

**3단계**: 서버의 검색 함수 실행

- 네이버 API 호출
- 책 데이터 수신

**4단계**: 데이터 반환

- 서버 → 브라우저로 검색 결과 전달

**5단계**: 화면 업데이트

- 받은 데이터를 검색 결과 영역에 표시

**결과**:

- URL 변경 없음
- 같은 페이지 내에서 내용만 업데이트

## 5. 자주 하는 질문

### Q1. 왜 이름이 비슷해서 헷갈릴까요?

둘 다 "경로"라는 의미의 "Route"를 사용하기 때문입니다.

- 페이지 라우트: 화면으로 가는 경로
- API 라우트: 데이터 처리 함수로 가는 경로

목적지가 다를 뿐, 둘 다 "어디론가 가는 길"이라는 점에서는 같습니다.

### Q2. 언제 페이지 라우트를 쓰고, 언제 API 라우트를 쓰나요?

**페이지 라우트 사용**:

- 완전히 다른 화면으로 이동할 때
- 메뉴를 클릭해서 다른 페이지로 갈 때
- URL이 바뀌어야 할 때

**API 라우트 사용**:

- 현재 화면에서 데이터만 가져올 때
- 버튼을 눌러서 새로운 정보를 불러올 때
- 폼 제출이나 저장 같은 데이터 처리를 할 때

### Q3. 서버리스 함수와 API 라우트는 같은 건가요?

거의 같은 개념입니다.

- **서버리스 함수**: 서버에서 실행되는 함수 자체
- **API 라우트**: 그 함수에 접근하기 위한 URL 주소

비유하자면:

- 서버리스 함수 = 은행 창구 직원
- API 라우트 = 그 창구의 번호

## 6. 핵심 정리

### 라우터(Router)

- **역할**: 전체 교통 관리 시스템
- **비유**: 백화점 안내 데스크
- **기능**: 주소를 보고 어떤 페이지를 보여줄지 결정

### 라우트(Route)

- **역할**: 구체적인 경로 규칙
- **비유**: 건물의 층별 안내 (1층→로비, 2층→도서관)
- **기능**: "이 주소면 이 화면을 보여줘"라는 규칙

### API 라우트(API Route)

- **역할**: 데이터 송수신 전용 주소
- **비유**: 은행 업무 창구 번호
- **기능**: 화면은 그대로, 데이터만 주고받음

### 가장 중요한 차이점

**페이지 라우트 vs API 라우트**

| |페이지 라우트|API 라우트|
|---|---|---|
|**화면 전환**|✅ 바뀜|❌ 안 바뀜|
|**URL 변경**|✅ 바뀜|❌ 안 바뀜|
|**결과물**|HTML 페이지|JSON 데이터|
|**사용 예시**|메뉴 클릭|검색 버튼 클릭|

## 7. 마무리

웹 애플리케이션은 이 세 가지 개념을 조합해서 작동합니다:

1. **라우터**가 전체를 관리하고
2. **라우트** 규칙에 따라 화면을 전환하며
3. **API 라우트**를 통해 필요한 데이터를 주고받습니다

마치 우리가 백화점에서:

- 안내 데스크(라우터)에 물어보고
- 층별 안내판(라우트)을 보고 이동하며
- 각 층의 서비스 창구(API 라우트)에서 업무를 처리하는 것과 같습니다

이 세 가지만 이해하면 웹 애플리케이션의 핵심 구조를 파악할 수 있습니다!