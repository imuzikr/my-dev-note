---
date: 2026-01-07
tags:
  - HTML
  - DOM
  - SPA
  - CSR
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---
DOM이란 무엇인가?
DOM(Document Object Model)은 웹 페이지를 프로그래밍적으로 다루기 위한 인터페이스입니다. 쉽게 말하면, HTML로 작성된 웹 페이지를 브라우저가 이해하고 조작할 수 있는 형태로 변환한 것입니다.
HTML과 DOM의 관계
웹 페이지를 만들 때 우리는 HTML이라는 언어로 코드를 작성합니다. 예를 들어 다음과 같은 간단한 HTML 코드가 있다고 가정해보겠습니다.

<!DOCTYPE html>
<html>
  <head>
    <title>나의 웹페이지</title>
  </head>
  <body>
    <h1>안녕하세요</h1>
    <p>이것은 문단입니다.</p>
  </body>
</html>


브라우저는 이 HTML 파일을 단순히 텍스트로만 읽지 않습니다. 브라우저는 이 HTML을 분석해서 트리 구조의 객체 모델로 변환합니다. 마치 가계도처럼 부모-자식 관계로 연결된 구조를 만드는 것입니다. 이것이 바로 DOM입니다.
위의 HTML은 DOM에서 다음과 같은 트리 구조로 표현됩니다.

Document
  └─ html
      ├─ head
      │   └─ title
      │       └─ "나의 웹페이지"
      └─ body
          ├─ h1
          │   └─ "안녕하세요"
          └─ p
              └─ "이것은 문단입니다."


각 HTML 태그는 ‘노드’라는 객체가 되고, 이 노드들이 부모-자식 관계로 연결되어 트리를 형성합니다.
DOM이 필요한 이유
HTML은 정적인 텍스트 파일입니다. 하지만 현대의 웹페이지는 사용자의 클릭, 입력, 스크롤 등에 반응하며 내용이 실시간으로 변경됩니다. 이러한 동적인 변화를 가능하게 만드는 것이 바로 DOM입니다.
실생활 비유
DOM을 이해하기 위해 실생활에 비유해보겠습니다. 건축 설계도면과 실제 건물의 관계를 생각해보세요. HTML은 설계도면과 같고, DOM은 그 설계도면을 바탕으로 만들어진 실제 건물과 같습니다. 설계도면 자체는 종이에 그려진 그림일 뿐이지만, 실제 건물은 그것을 기반으로 만들어진 물리적 구조물입니다. 그리고 건물은 나중에 리모델링할 수 있지만, 원본 설계도면은 그대로 보관됩니다.
마찬가지로 HTML 원본 파일은 그대로 유지되지만, 브라우저 메모리에 생성된 DOM은 JavaScript를 통해 계속 수정될 수 있습니다.
DOM 조작의 실제 사례
간단한 예시: 텍스트 변경하기
버튼을 클릭했을 때 문단의 텍스트가 변경되는 상황을 생각해보겠습니다. JavaScript는 DOM을 통해 이런 작업을 수행합니다.

// DOM에서 문단 요소를 찾습니다
const paragraph = document.querySelector('p');

// 해당 요소의 텍스트를 변경합니다
paragraph.textContent = '내용이 변경되었습니다!';


이 코드가 실행되면 브라우저는 DOM 트리에서 문단 노드를 찾아 그 내용을 변경하고, 화면에 즉시 반영합니다. 중요한 점은 HTML 파일 자체는 전혀 변하지 않는다는 것입니다. 오직 메모리상의 DOM 트리만 변경되고, 이것이 화면에 보이는 것입니다.
좀 더 복잡한 예시: 항목 추가하기
사용자가 버튼을 클릭하면 목록에 새 항목이 추가되는 상황을 살펴보겠습니다.

<div id="container">
  <ul id="itemList">
    <li>항목 1</li>
    <li>항목 2</li>
  </ul>
  <button id="addButton">항목 추가</button>
</div>


JavaScript로 다음과 같이 작성하면 버튼 클릭 시 새 항목이 추가됩니다.

const button = document.getElementById('addButton');
const list = document.getElementById('itemList');

button.addEventListener('click', function() {
  // 새로운 항목 요소를 생성합니다
  const newItem = document.createElement('li');
  
  // 텍스트를 설정합니다
  newItem.textContent = '새 항목';
  
  // DOM 트리에 이 요소를 추가합니다
  list.appendChild(newItem);
});


이 과정을 단계별로 보면 다음과 같습니다.
	1.	DOM 트리에서 버튼과 목록을 찾습니다
	2.	메모리상에 새로운 항목 노드를 생성합니다
	3.	이 노드를 DOM 트리의 적절한 위치에 연결합니다
	4.	브라우저가 변경된 DOM을 감지하고 화면을 다시 그립니다
SPA에서 DOM의 역할
SPA(Single Page Application)에서 DOM의 역할을 이해하면 현대 웹 애플리케이션의 작동 방식을 더 명확히 알 수 있습니다.
원본 HTML은 변하지 않는다
SPA에서 브라우저가 서버로부터 처음 받는 HTML 파일은 매우 간단합니다. 대략 다음과 같은 형태입니다.

<!DOCTYPE html>
<html>
  <head>
    <title>My App</title>
  </head>
  <body>
    <div id="root"></div>
    <script src="bundle.js"></script>
  </body>
</html>


거의 비어있는 컨테이너와 JavaScript 파일 링크만 있습니다. 이 원본 HTML은 사용자가 페이지를 아무리 탐색하고 다양한 데이터를 보더라도 절대 변경되지 않습니다.
DOM에서 모든 변화가 일어난다
실제로 변경되고 화면에 반영되는 것은 브라우저 메모리에 생성된 DOM 트리입니다. 과정을 살펴보면 다음과 같습니다.
초기 로딩 단계에서 브라우저가 HTML을 받아 거의 빈 DOM 트리를 생성합니다. 이때는 <div id="root"></div>만 존재합니다.
JavaScript 실행 단계에서 다운로드된 JavaScript가 실행되면서 이 빈 div 안에 수많은 요소들을 동적으로 생성하여 DOM에 추가합니다. 헤더, 내비게이션, 메인 콘텐츠 등 모든 화면 요소가 이때 만들어집니다.
데이터 업데이트 단계에서 사용자가 다른 페이지로 이동하거나 상호작용하면, JavaScript가 서버에서 새 데이터를 받아와 DOM의 특정 부분만 수정합니다. 예를 들어, 사용자 프로필 페이지로 이동하면 서버가 {name: "김철수", age: 30} 같은 JSON 데이터를 보내고, JavaScript가 DOM에서 해당 영역을 찾아 이 정보로 채웁니다.
원본 HTML과 현재 DOM의 차이 확인하기
브라우저의 개발자 도구를 사용하면 이 차이를 직접 확인할 수 있습니다.
“페이지 소스 보기” 기능을 사용하면 서버가 전송한 원본 HTML이 그대로 보입니다. 아무리 페이지를 조작해도 이것은 변하지 않습니다.
“요소 검사” 또는 Elements 탭을 사용하면 현재 메모리상의 DOM 트리를 볼 수 있습니다. JavaScript에 의해 동적으로 생성되고 수정된 모든 요소가 여기에 나타납니다.
예를 들어, React로 만든 웹사이트에서 “페이지 소스 보기”를 하면 거의 빈 HTML만 보이지만, “요소 검사”를 하면 복잡한 구조가 모두 보입니다. 이것이 바로 원본 HTML은 그대로지만 DOM은 계속 변경되고 있다는 증거입니다.
화면에 그려지는 과정
브라우저가 우리 눈에 보이는 화면을 그리는 과정을 간단히 설명하면 다음과 같습니다.
	1.	DOM 트리 구축: HTML을 분석하여 DOM 트리를 만듭니다
	2.	스타일 계산: CSS를 분석하여 각 요소의 스타일을 결정합니다
	3.	레이아웃 계산: 각 요소가 화면의 어디에 얼마나 크게 배치될지 계산합니다
	4.	화면 그리기: 실제로 픽셀을 화면에 그립니다
JavaScript가 DOM을 수정하면 이 과정이 다시 실행됩니다. 다만 브라우저는 효율적으로 작동하기 때문에 변경된 부분만 다시 계산하고 그립니다. 이 모든 과정에서 원본 HTML은 전혀 관여하지 않으며, 오직 메모리상의 DOM 트리만이 화면 렌더링의 기준이 됩니다.
Virtual DOM의 개념
현대적인 프레임워크인 React, Vue 등에서는 Virtual DOM이라는 개념을 사용합니다. 실제 DOM을 직접 조작하는 것은 브라우저에게 많은 계산 부담을 줍니다. 특히 많은 변경사항이 한꺼번에 발생하면 성능이 크게 저하될 수 있습니다.
Virtual DOM은 실제 DOM의 가벼운 복사본입니다. 변경사항이 생기면 먼저 이 Virtual DOM에서 변경하고, 이전 상태와 비교하여 실제로 바뀐 부분만 찾아냅니다. 그런 다음 최종적으로 실제 DOM에 한 번에 반영합니다. 이렇게 하면 불필요한 DOM 조작을 줄여 성능을 크게 향상시킬 수 있습니다.
건물 리모델링에 비유하자면, 실제 건물을 여러 번 뜯어고치는 대신 모형으로 먼저 시뮬레이션해보고 최종 설계가 완성되면 한 번에 시공하는 것과 비슷합니다.
핵심 정리
DOM을 이해하기 위한 핵심 개념을 정리하면 다음과 같습니다.
HTML은 웹 페이지의 설계도면이고, DOM은 브라우저가 이 설계도면을 바탕으로 메모리에 만든 실제 구조물입니다. 원본 HTML 파일은 변경되지 않지만, DOM은 JavaScript를 통해 언제든지 수정될 수 있습니다. 브라우저는 DOM을 기준으로 화면을 그리므로, DOM이 변경되면 화면도 변경됩니다.
SPA에서는 처음 받은 HTML이 거의 비어있고, JavaScript가 DOM을 동적으로 구성하여 풍부한 사용자 경험을 제공합니다. 페이지 이동이나 데이터 업데이트는 모두 DOM 조작을 통해 이루어지며, 새로운 HTML 페이지를 받지 않습니다. 이것이 바로 “Single Page”라는 이름의 의미입니다. HTML 페이지는 하나지만, DOM이 계속 변경되면서 마치 여러 페이지가 있는 것처럼 작동하는 것입니다.​​​​​​​​​​​​​​​​
