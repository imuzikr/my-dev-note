---
date: 2025-10-17
tags:
  - CORS
  - 프록시
  - 동일출처정책
  - 웹보안
  - HTTP헤더
  - 프론트엔드
  - 백엔드
  - API
  - Nginx
  - vibe_coding
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---
## 웹 앱 기획자 필독: 깐깐한 보안관, 브라우저와 CORS 완벽 해설서

이 문서는 웹 앱 제작에 관심 있는 비개발자를 위해 **Cross-Origin 이슈(CORS)**와 브라우저의 핵심 보안 원칙을 친절하고 상세하게 설명합니다. 이 개념을 이해하면 개발자와의 소통이 원활해지고, "분명 데이터는 있는데 화면에 왜 안 나오지?"와 같은 문제 발생 시 원인을 정확히 파악할 수 있습니다.

---

### 1. 문제의 발단: "데이터는 있는데, 왜 화면에는 안 보일까요?"

웹 프로젝트를 진행하다 보면 서버에는 분명 데이터가 있는데, 웹 페이지에는 아무것도 보이지 않는 상황을 마주하게 됩니다. 이때 개발자 도구(F12) 콘솔 창을 열어보면, 다음과 같은 낯선 에러 메시지가 표시될 가능성이 높습니다.

> `Access to fetch at '...' from origin '...' has been blocked by CORS policy...`

이 현상은 버그나 실수가 아니라, 인터넷 세상을 안전하게 지키기 위한 **웹 브라우저**의 아주 중요한 규칙 때문에 발생합니다.

### 2. 우리를 지키는 깐깐한 보안관: 동일 출처 정책 (Same-Origin Policy, SOP)

웹 브라우저는 인터넷이라는 거대한 도시를 지키는 '깐깐한 보안관'과 같습니다. 이 보안관은 **‘동일 출처 정책(Same-Origin Policy, SOP)’**이라는 매우 중요한 원칙을 가지고 있습니다.

> [!NOTE] 핵심 원칙: 동일 출처 정책 (SOP)
> 
> "네가 처음 온 그곳(출처)의 데이터만 자유롭게 가져올 수 있어!"

이 정책이 없다면, 여러분이 A은행 사이트에 로그인한 상태에서 악성코드가 심어진 B사이트에 접속했을 때, B사이트가 멋대로 A은행에 요청을 보내 금융 정보를 탈취할 수 있습니다. 동일 출처 정책은 이처럼 위험한 상황을 원천적으로 차단하여 사용자를 보호하는 **필수적인 방어막**입니다.

### 3. 보안관의 검문 기준: '출처(Origin)'란 정확히 무엇일까요?

보안관이 '같은 곳'인지 아닌지를 판단하는 기준인 **'출처(Origin)'**는 다음 세 가지 요소가 **모두 정확히 일치**해야 '동일 출처'로 인정됩니다.

1. **프로토콜 (Protocol):** `http://` 와 `https://`
2. **도메인 (Domain):** `www.example.com`
3. **포트 (Port):** `:80`, `:443`, `:3000`
    

> [!NOTE] URL 주소에서 도메인 뒤에 따라오는 경로(`/products`)나 파라미터(`?id=1`)는 출처를 판단하는 데 영향을 주지 않습니다.

#### 다른 출처로 판정되는 흔한 사례

기준 URL: `http://localhost:3000`

|**비교 URL**|**판정**|**차이점**|
|---|---|---|
|`https://localhost:3000`|❌ 다름|**프로토콜** 불일치 (http ≠ https)|
|`http://127.0.0.1:3000`|❌ 다름|**도메인** 불일치 (브라우저는 'localhost'와 '127.0.0.1'을 다른 문자열로 인식)|
|`http://localhost:8080`|❌ 다름|**포트** 불일치 (3000 ≠ 8080)|

### 4. 왜 자꾸 보안관에게 걸릴까? 현대 웹 개발의 구조

현대 웹 앱은 일반적으로 **프론트엔드(사용자 화면)**와 **백엔드(데이터 처리)**를 분리하여 개발합니다.

- **프론트엔드 (예: `http://localhost:3000`)**: 사용자가 직접 보고 상호작용하는 화면(UI)을 담당합니다.
- **백엔드 (예: `http://localhost:8080`)**: 데이터 처리, 회원가입, 로그인 등 눈에 보이지 않는 핵심 로직을 담당합니다.
    

이렇게 역할을 나누면 개발 효율성이 높아지지만, 필연적으로 두 서버의 **출처가 달라지는 결과**를 낳습니다. 바로 이 지점에서 프론트엔드(`:3000`)가 백엔드(`:8080`)에 데이터를 요청하면, 브라우저 보안관이 "어? 포트 번호가 다르네! 다른 출처야!"라고 판단하고 요청을 차단하는 **CORS 에러**가 발생합니다.

### 5. 해결사 등장: CORS (Cross-Origin Resource Sharing)

CORS는 이 엄격한 동일 출처 정책을 넘어, 다른 출처의 자원을 안전하게 요청하고 공유하기 위한 **'규칙이자 합의 시스템'**입니다. 즉, 보안관(브라우저)이 외부인(다른 출처)의 출입을 허가해주기 위해 확인하는 **'방문 허가증'**과 같습니다.

#### CORS 동작 방식: 보안관의 검문 (Preflight)

`PUT`, `DELETE` 같은 복잡한 요청이 발생하면, 브라우저는 실제 요청을 보내기 전에 서버에게 사전 확인을 합니다.

1. **사전 요청 (Preflight)**: 브라우저가 `OPTIONS` 메서드를 사용해 **"이런 요청을 보내도 괜찮을까?"**라고 서버에 미리 물어봅니다.
2. **서버의 허가 응답**: 데이터를 제공하는 서버는 허용할 출처 목록을 확인하고, 허용한다면 `Access-Control-Allow-Origin`과 같은 CORS 헤더('방문 허가증')를 응답에 포함하여 회신합니다.
3. **실제 요청 또는 차단**: 브라우저는 '허가증'을 확인하고 나서야 실제 데이터 요청을 보냅니다. 만약 허가증이 없으면, 브라우저는 응답을 **차단**하고 콘솔에 CORS 에러를 띄웁니다.
    

> [!NOTE] 중요한 점
> 
> 서버는 요청을 성공적으로 처리하고 200 OK 응답을 보냈을 수 있습니다. 하지만 브라우저가 보안 정책에 따라 최종적으로 응답을 차단하기 때문에 우리 눈에는 에러로 보이는 것입니다.

### 6. CORS 이슈 해결 방법: 허가증을 얻는 두 가지 방법

CORS 에러가 발생했다는 것은 **데이터를 제공하는 서버가 우리 웹 앱을 허용 목록에 추가해주지 않았다**는 뜻입니다.

#### 방법 1: 서버에서 정식 허가증 발급하기 (CORS 헤더 설정)

가장 근본적이고 올바른 해결책입니다. 실제 서비스(프로덕션) 환경에서는 반드시 이 방법을 사용해야 합니다. 백엔드 개발자가 서버 코드에 아래와 같은 '허가증'을 설정하여 브라우저에게 정식으로 허가를 내주는 것입니다.

|**헤더**|**설명**|**예시**|
|---|---|---|
|`Access-Control-Allow-Origin`|**요청을 허용할 클라이언트의 출처**를 명시합니다. (가장 중요!)|`https://my-awesome-app.com`|
|`Access-Control-Allow-Methods`|허용할 HTTP 메서드(GET, POST, PUT 등)를 명시합니다.|`GET, POST, PUT, DELETE`|
|`Access-Control-Allow-Credentials`|쿠키 등 인증 정보를 포함한 요청을 허용할지 결정합니다.|`true`|

> [!DANGER] 보안 주의사항
> 
> Access-Control-Allow-Origin에 와일드카드(*)를 사용하는 것은 모든 사이트의 접근을 허용하므로 심각한 보안 위협이 될 수 있습니다.

#### 방법 2: 같은 출처인 척하기 (프록시 서버 사용)

**프록시(Proxy)**는 클라이언트와 서버 사이에서 요청을 대신 전달하는 **'대리인'** 또는 **'중개자'** 역할을 하는 서버입니다.

- **작동 원리**: 브라우저는 API 요청을 자신의 **같은 출처**(예: `localhost:3000`)로 보냅니다. 그러면 프론트엔드 개발 서버에 설정된 프록시가 이 요청을 받아, 브라우저 몰래 실제 백엔드 서버(`localhost:8080`)로 전달하고 응답을 다시 브라우저에게 돌려줍니다.
- **결론**: 브라우저 입장에서는 계속 같은 출처와 통신한 것으로 착각하기 때문에 CORS 문제가 발생하지 않습니다.
    

> [!WARNING] 프록시의 한계
> 
> 이 방법은 CORS를 근본적으로 해결하는 것이 아니라 우회하는 방식입니다. 주로 백엔드 서버를 수정할 수 없는 개발 환경에서 사용하는 편리한 임시 방편일 뿐입니다.

### 7. 누가, 그리고 언제 적용될까?

- **판단 주체**: CORS 정책을 적용하고 출처를 판단하는 주체는 오직 **웹 브라우저(Chrome, Firefox 등)**입니다. 서버는 출처가 다른지 신경 쓰지 않습니다.
- **적용되지 않는 환경**: 이 규칙은 웹 브라우저 안에서만 작동하므로, 아래 환경에서는 CORS 문제가 발생하지 않습니다.
    - **스마트폰 네이티브 앱** (iOS, Android)
    - **Postman, curl** 같은 API 테스트 도구
    - 서버와 서버 간의 통신
        

### 8. 비개발자를 위한 최종 요약: 이것만 기억하세요!

1. **브라우저는 우리를 지키는 깐깐한 보안관입니다.** 기본적으로 외부와의 소통을 막는 '동일 출처 정책'을 가지고 있으며, 이는 악성 사이트로부터 우리를 보호하기 위함입니다.
2. **'출처'는 '프로토콜 + 도메인 + 포트' 3종 세트입니다.** 하나라도 다르면 보안관에게는 '다른 곳'으로 보입니다.
3. **CORS 에러는 우리 코드의 버그가 아닙니다.** "데이터를 요청한 상대방 서버가 '당신은 들어와도 좋습니다'라는 허가증을 발급해주지 않았습니다"라는 뜻입니다.
4. **이 모든 규칙은 '웹 브라우저' 안에서만 작동합니다.** 스마트폰에 설치해서 사용하는 앱이나 PC 프로그램은 이 규칙의 적용을 받지 않습니다.
    
---
### 용어 정리

| **용어**           |                                                    **설명**                                                     |
| ------------------ |:---------------------------------------------------------------------------------------------------------------:|
| **CORS**           | **C**ross-**O**rigin **R**esource **S**haring. 교차 출처 리소스 공유. 다른 출처 간의 통신을 허용하기 위한 규칙. |
| **SOP**            |                   **S**ame-**O**rigin **P**olicy. 동일 출처 정책. 브라우저의 핵심 보안 규칙.                    |
| **Origin (출처)**  |                              프로토콜, 도메인, 포트로 구성되는 서비스의 고유 주소.                              |
| **Preflight**      |                 복잡한 요청 전 브라우저가 서버에 보내는 사전 확인 요청 (`OPTIONS` 메서드 사용).                 |
| **Proxy (프록시)** |                    클라이언트와 서버 사이의 중개 서버. 개발 중 CORS 문제를 우회하는 데 유용.                    |
