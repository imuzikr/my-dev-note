---
date: 2026-01-22
tags:
  - CSR
  - SSR
  - MPA
  - SPA
  - 자바스크립트파싱
  - 파싱
type: knowledge
contexts:
  - "[[🏷️Vibe Coding]]"
index:
  - "[[🎶Web Development]]"
  - "[[🔆인공지능]]"
YouTube:
scr_url:
scr:
---



# CSR(Client-Side Rendering) 방식의 웹 페이지 렌더링 원리

## 들어가며

CSR(Client-Side Rendering)은 웹 페이지의 콘텐츠를 사용자의 브라우저에서 JavaScript를 통해 동적으로 생성하는 렌더링 방식입니다. React, Vue, Angular 같은 현대적인 프론트엔드 프레임워크에서 주로 사용되는 방식으로, SPA(Single Page Application)와 밀접한 관련이 있습니다.

## 1. SPA와 CSR의 관계

### SPA(Single Page Application)란?

SPA는 단일 HTML 페이지에서 JavaScript를 통해 동적으로 콘텐츠를 업데이트하는 애플리케이션 구조를 의미합니다. 전통적인 웹사이트처럼 페이지를 이동할 때마다 서버에서 새로운 HTML을 받아오는 것이 아니라, 한 번 로드된 후에는 필요한 데이터만 받아와서 화면을 갱신합니다.

### SPA는 항상 CSR 방식을 사용하나요?

전통적으로 SPA를 제작할 때 CSR 방식을 주로 사용해왔습니다. 사용자가 처음 접속하면 거의 빈 HTML과 JavaScript 번들을 받고, 브라우저에서 JavaScript가 실행되면서 화면을 그리는 방식입니다.

하지만 SPA라고 해서 반드시 CSR만 사용해야 하는 것은 아닙니다. 최근에는 SSR(Server-Side Rendering)이나 SSG(Static Site Generation)를 함께 활용하는 하이브리드 방식이 널리 사용됩니다. Next.js나 Nuxt.js 같은 프레임워크는 초기 페이지는 서버에서 렌더링하여 빠르게 보여주고, 이후 페이지 전환은 클라이언트에서 처리하는 방식을 지원합니다.

## 2. CSR 방식의 전체 로딩 과정

CSR 방식의 웹 페이지가 로드되는 전체 과정은 다음과 같습니다.

```
사용자 접속
    ↓
1. HTML 다운로드 (빈 껍데기)
    ↓
2. JavaScript 파일 다운로드
    ↓
3. JavaScript 파싱(Parsing)
    ↓
4. JavaScript 컴파일
    ↓
5. JavaScript 실행
    ↓
6. 화면 렌더링 완료
```

각 단계를 자세히 살펴보겠습니다.

## 3. 단계별 상세 설명

### 1단계: HTML 다운로드

사용자가 웹사이트에 접속하면 브라우저는 가장 먼저 HTML 파일을 다운로드합니다. 그런데 CSR 방식의 HTML은 거의 빈 껍데기입니다.

**전형적인 CSR 방식의 HTML 구조:**

```html
<!DOCTYPE html>
<html>
  <head>
    <title>My App</title>
  </head>
  <body>
    <div id="root"></div>
    <script src="main.js"></script>
  </body>
</html>
```

실제 콘텐츠가 없고 `<div id="root"></div>` 같은 빈 컨테이너만 있습니다. 이 단계에서는 사용자에게 빈 화면이 보입니다.

### 2단계: JavaScript 파일 다운로드

HTML이 다운로드되고 브라우저가 `<script src="main.js"></script>` 태그를 만나면, 그제야 JavaScript 파일을 다운로드하기 시작합니다.

**중요한 점:**

- HTML과 JavaScript가 동시에 다운로드되는 것이 아니라 순차적으로 진행됩니다.
- 현대 웹 앱의 JavaScript 번들은 수 MB에 달할 수 있어 다운로드에 시간이 소요됩니다.
- 네트워크가 느린 환경에서는 이 시간이 더욱 길어집니다.

### 3단계: JavaScript 파싱(Parsing)

JavaScript 파일이 모두 다운로드되었다고 해서 바로 실행되는 것은 아닙니다. 브라우저는 먼저 **파싱** 과정을 거쳐야 합니다.

#### 파싱이란 무엇인가?

파싱은 텍스트 형태로 작성된 JavaScript 코드를 브라우저가 이해하고 실행할 수 있는 구조로 변환하는 과정입니다. 단순히 코드를 읽고 이해하는 수준을 넘어서, 실제로 다른 형태의 데이터 구조로 변형하는 작업입니다.

**파싱의 핵심 개념:**

- 개발자가 작성한 코드는 결국 텍스트 문자열입니다.
- 브라우저는 이 텍스트를 그대로 실행할 수 없습니다.
- 텍스트를 AST(Abstract Syntax Tree)라는 트리 구조의 데이터로 변환해야 합니다.

#### 파싱의 구체적 과정

예를 들어, 다음과 같은 간단한 코드가 있다고 가정해봅시다.

```javascript
function add(a, b) {
  return a + b;
}
```

파싱 과정을 거치면 이 텍스트는 다음과 같은 트리 구조로 변형됩니다.

```
함수 선언 노드
├─ 함수 이름: add
├─ 매개변수: [a, b]
└─ 함수 본문
   └─ return 문
      └─ 이항 연산(+)
         ├─ 피연산자: a
         └─ 피연산자: b
```

원래의 텍스트는 사라지고, 이러한 트리 구조의 데이터가 브라우저 메모리에 생성됩니다. 브라우저는 이 트리 구조를 보고 각 부분이 무엇을 의미하는지 알 수 있습니다.

#### 파싱과 실행의 차이

많은 사람들이 파싱과 실행을 혼동하지만, 이 둘은 명확히 구분됩니다.

**파싱 단계에서는:**

- 코드의 구조를 이해하고 내부 표현으로 변환합니다.
- 문법 오류를 검사합니다.
- 변수의 스코프를 파악합니다.
- 아직 코드가 실행되지는 않습니다.

**실행 단계에서는:**

- 파싱된 코드를 실제로 동작시킵니다.
- 변수에 값이 할당됩니다.
- 함수가 호출됩니다.
- 화면에 변화가 나타납니다.

위의 `add` 함수 예시에서, 파싱 단계에서는 "이 코드가 두 개의 매개변수를 받아서 더하는 함수를 정의한다"는 구조만 파악합니다. 실제로 `add(2, 3)`을 호출하여 5라는 결과를 얻는 것은 실행 단계에서 일어납니다.

#### 파싱에 시간이 걸리는 이유

대규모 웹 애플리케이션의 JavaScript 번들은 수십만 줄의 코드를 포함할 수 있습니다. React 라이브러리 자체, 각종 외부 패키지, 애플리케이션 코드 등이 모두 합쳐진 결과물입니다.

브라우저는 이 모든 코드를 한 줄씩 읽고 분석하여 실행 가능한 형태로 변환해야 하므로 상당한 시간이 소요됩니다. 특히 모바일 기기처럼 처리 능력이 제한된 환경에서는 파싱 시간이 더욱 길어집니다.

**구체적인 예시:** 3G 네트워크 환경에서 3MB 크기의 JavaScript 번들을 가진 앱에 접속한다고 가정해봅시다.

- 0.1초: HTML 다운로드 완료 (빈 화면 표시)
- 3초: JavaScript 파일 다운로드
- 1초: JavaScript 파싱 및 컴파일
- 0.5초: JavaScript 실행 및 API 호출
- **총 4.6초: 사용자가 실제 콘텐츠를 보기까지 걸리는 시간**

### 4단계: JavaScript 컴파일

파싱이 완료되면 브라우저는 AST를 바탕으로 바이트코드나 기계어로 컴파일합니다. 최신 JavaScript 엔진들(Chrome의 V8, Firefox의 SpiderMonkey)은 JIT(Just-In-Time) 컴파일 방식을 사용하여 코드를 최적화된 형태로 변환합니다.

### 5단계: JavaScript 실행

컴파일이 완료되면 드디어 코드가 실행됩니다. React 같은 프레임워크는 이 단계에서:

- 가상 DOM을 구성합니다.
- 컴포넌트를 초기화합니다.
- 필요한 데이터를 API로 요청합니다.
- 상태(state)를 관리합니다.

### 6단계: 화면 렌더링

JavaScript 실행이 완료되면 비로소 실제 콘텐츠가 화면에 나타납니다. 빈 `<div id="root"></div>` 안에 React가 생성한 DOM 요소들이 채워지면서 사용자는 최종 화면을 보게 됩니다.

## 4. 빌드 과정과 파싱의 관계

많은 사람들이 배포 시 진행되는 빌드 과정과 브라우저의 파싱을 혼동하지만, 이 둘은 완전히 별개의 독립적인 과정입니다.

### 빌드 과정 (배포 전, 개발자 환경)

**언제:** 개발자의 컴퓨터나 CI/CD 서버에서 배포 전에 한 번 실행됩니다.

**무엇을:** Webpack, Vite 같은 도구들이 여러 개의 소스 파일을 하나 또는 몇 개의 번들 파일로 합치고, 코드를 압축하며, 불필요한 부분을 제거합니다.

**결과물:** 최적화된 JavaScript 텍스트 파일들입니다.

**목적:** 코드를 프로덕션 환경에 적합하게 준비하는 것입니다. 파일 크기를 줄이고, 브라우저 호환성을 확보하며, 개발 편의를 위한 문법들(TypeScript, JSX)을 표준 JavaScript로 변환합니다.

### 파싱 과정 (실행 시, 사용자 브라우저)

**언제:** 각 사용자의 브라우저에서 매번 실행됩니다.

**무엇을:** 다운로드한 JavaScript 텍스트 파일을 AST라는 트리 구조로 변환하고, 이를 바이트코드나 기계어로 컴파일합니다.

**결과물:** 브라우저가 실행할 수 있는 내부 데이터 구조입니다.

**목적:** 텍스트를 컴퓨터가 실행할 수 있는 형태로 변환하는 것입니다.

### 왜 둘 다 필요한가?

빌드 과정에서 아무리 최적화를 해도, 결과물은 여전히 JavaScript 텍스트입니다. 사용자 브라우저는 이 텍스트를 받아서 처음부터 파싱 과정을 수행해야 합니다.

왜 서버에서 미리 파싱된 결과를 전달하지 않을까요? 이유는 보안과 호환성 때문입니다. 각 브라우저마다 JavaScript 엔진이 다르고 내부 표현 방식도 다릅니다. 표준화된 텍스트 형태로 전달하고 각 브라우저가 자신의 방식으로 처리하는 것이 안전하고 호환성이 높습니다.

### 구체적인 흐름 예시

React 앱을 만드는 전체 과정을 살펴보면:

1. **개발 단계:** 개발자가 JSX와 ES6+ 문법으로 코드를 작성합니다.
2. **빌드 단계:** 배포 전에 빌드 명령을 실행하면 Babel이 코드를 변환하고, Webpack이 번들링하며, Terser가 압축합니다. 이 과정은 한 번만 일어납니다.
3. **배포:** 결과물인 `main.js` 같은 파일이 서버에 업로드됩니다.
4. **사용자 A 접속 (오전 10시):** 브라우저가 `main.js`를 다운로드하고 파싱하여 실행합니다.
5. **사용자 B 접속 (오후 3시):** 똑같은 `main.js` 파일을 다시 다운로드하고 파싱합니다.

빌드는 한 번만 일어났지만, 파싱은 각 사용자마다 독립적으로 발생합니다.

## 5. 파싱이 다시 일어나는 경우

많은 사람들이 "JavaScript가 한 번 로드되면 계속 사용되는 것 아닌가?"라고 생각하지만, 상황에 따라 다릅니다.

### 파싱이 다시 일어나는 경우

**1. 페이지 새로고침**

- 브라우저가 모든 것을 처음부터 다시 시작합니다.
- JavaScript 파일을 다시 다운로드하고(캐시가 있으면 캐시에서), 다시 파싱하고, 다시 실행합니다.
- 브라우저 메모리에 있던 파싱 결과가 모두 사라지고 새롭게 시작합니다.

**2. 동적으로 코드를 로드하는 경우**

- `import()` 함수를 사용해 동적으로 모듈을 불러오는 경우
- 코드 스플리팅으로 분리된 청크를 처음 로드하는 경우
- `<script>` 태그를 동적으로 생성해서 새로운 JavaScript 파일을 추가하는 경우

### 파싱이 다시 일어나지 않는 경우

**1. SPA에서 페이지 이동**

- 이미 첫 페이지 로드 시에 모든 JavaScript가 다운로드되고 파싱되었습니다.
- 페이지 이동은 이미 파싱된 코드 중에서 해당 컴포넌트를 실행하는 것일 뿐입니다.
- 새로운 JavaScript 파일을 받는 것이 아니므로 파싱도 일어나지 않습니다.

**2. 버튼 클릭이나 상호작용**

- 이미 파싱되고 컴파일된 코드를 실행하는 것이기 때문에 파싱이 필요하지 않습니다.
- 예: 장바구니 버튼을 클릭하면 이미 메모리에 로드된 함수가 바로 실행됩니다.

### 브라우저 캐싱의 역할

브라우저는 파싱 결과 자체를 캐싱하기도 합니다. 최신 브라우저들은 자주 사용되는 스크립트의 파싱 결과를 디스크에 저장해두었다가 재사용하는 최적화를 수행합니다. 따라서 같은 웹사이트를 여러 번 방문하면, 첫 방문보다 이후 방문이 더 빠른 이유 중 하나가 이런 파싱 캐시 덕분입니다.

## 6. CSR 방식의 장단점

### 장점

**1. 부드러운 사용자 경험** 한 번 로드된 후에는 페이지 전환이 매우 빠릅니다. 전체 페이지를 다시 불러올 필요 없이 필요한 데이터만 받아와서 화면을 갱신하기 때문입니다.

**2. 서버 부하 감소** 서버는 단순히 API 데이터만 제공하면 되므로, HTML을 매번 생성할 필요가 없어 서버 자원을 절약할 수 있습니다.

**3. 풍부한 인터랙션** 클라이언트에서 모든 것을 제어하므로, 복잡한 애니메이션이나 실시간 업데이트 같은 풍부한 사용자 경험을 구현하기 쉽습니다.

### 단점

**1. 느린 초기 로딩 속도** JavaScript를 다운로드하고, 파싱하고, 실행하는 전체 과정이 완료되어야 화면을 볼 수 있어 초기 로딩이 느립니다.

**2. SEO(검색 엔진 최적화) 어려움** 검색 엔진 크롤러가 JavaScript를 실행하기 전의 빈 HTML만 보게 되어, 콘텐츠를 제대로 인덱싱하기 어렵습니다.

**3. JavaScript 의존성** JavaScript가 비활성화되어 있거나 로드에 실패하면 아무것도 보이지 않습니다.

### CSR이 적합한 경우

- 관리자 대시보드
- 내부 업무 도구
- 로그인이 필요한 개인화된 애플리케이션
- SEO가 중요하지 않은 서비스

### CSR이 적합하지 않은 경우

- 공개된 콘텐츠를 제공하는 블로그나 뉴스 사이트
- 검색 노출이 중요한 전자상거래 사이트
- 초기 로딩 속도가 중요한 랜딩 페이지

## 7. 현대적인 접근 방식

최근의 웹 개발에서는 순수 CSR 방식보다는 여러 렌더링 방식을 혼합하여 사용하는 추세입니다.

### 하이브리드 렌더링

Next.js나 Nuxt.js 같은 프레임워크는 다음과 같은 방식을 지원합니다.

- 초기 페이지는 서버에서 렌더링(SSR)하여 빠르게 보여줍니다.
- 이후 페이지 전환은 클라이언트에서 처리(CSR)하여 부드러운 경험을 제공합니다.
- 정적인 콘텐츠는 빌드 시에 미리 생성(SSG)하여 성능을 극대화합니다.

### 구체적인 예시

블로그 사이트를 만든다면:

- 글 목록 페이지: SSG로 미리 생성
- 글 내용 페이지: SSR로 빠르게 보여주기
- 댓글 영역: CSR로 동적 처리
- 페이지 간 이동: CSR로 부드러운 전환

전자상거래 사이트라면:

- 상품 상세 페이지: SSR로 빠르게 보여주기 (SEO 중요)
- 장바구니 조작: CSR로 즉각적인 반응
- 필터링 기능: CSR로 동적 처리
- 결제 페이지: SSR로 안정성 확보

## 8. 성능 최적화 방법

CSR 방식을 사용할 때 성능을 개선하는 방법들입니다.

### 1. 코드 스플리팅(Code Splitting)

JavaScript 번들을 여러 개로 나누어 필요한 부분만 먼저 로드합니다.

**효과:**

- 초기 로드 시 다운로드할 코드량 감소
- 파싱해야 할 코드량 감소
- 초기 화면 표시 시간 단축

### 2. 레이지 로딩(Lazy Loading)

당장 필요하지 않은 컴포넌트나 이미지는 나중에 로드합니다.

**효과:**

- 초기 로딩 속도 개선
- 사용자가 실제로 필요로 할 때만 리소스 다운로드

### 3. 번들 크기 최적화

- 불필요한 라이브러리 제거
- Tree Shaking으로 사용하지 않는 코드 제거
- Minification으로 코드 압축

**효과:**

- 다운로드 시간 단축
- 파싱 시간 단축

### 4. 로딩 인디케이터 제공

기술적으로 성능을 개선하는 것은 아니지만, 사용자 경험 측면에서 중요합니다.

- 로딩 스피너나 스켈레톤 UI를 보여줌
- 사용자가 로딩 중임을 인지하고 기다릴 수 있음

## 9. 정리

CSR 방식의 핵심을 정리하면 다음과 같습니다.

### 작동 원리

1. 빈 HTML이 먼저 로드됩니다.
2. JavaScript 파일이 순차적으로 다운로드됩니다.
3. 브라우저가 JavaScript를 파싱하여 실행 가능한 형태로 변환합니다.
4. JavaScript가 실행되면서 화면을 동적으로 생성합니다.

### 선택 기준

- SEO와 초기 로딩 속도가 중요하다면: SSR이나 SSG 고려
- 복잡한 인터랙션과 부드러운 경험이 중요하다면: CSR 적합
- 대부분의 경우: 하이브리드 방식이 최선

현대 웹 개발에서는 순수 CSR만 고집하기보다는, 프로젝트의 특성에 맞게 다양한 렌더링 방식을 조합하여 사용하는 것이 좋습니다.